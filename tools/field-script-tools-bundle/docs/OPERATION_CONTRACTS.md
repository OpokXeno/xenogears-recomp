# Field VM operation contracts

Generated by `tools/build_dsl_documentation.py` from the opcode table and
the executable operand forms. Every dispatcher entry has its own contract.
Descriptions of unclassified operands deliberately state only their encoded
position and range; a byte is not a proven independent semantic parameter.
XGS contains operations and symbolic arguments, without encoding/layout annotations.
The compiler constructs native operand islands from semantic values. No XGA
or original binary is consulted, and no raw instruction is emitted.
The same per-instruction encoder and layout relaxation run on every source.
All comments are ignored; encoding and layout are derived only from code.
Lossless forms retain reserved fields, dispatch variants and movement phases as typed source arguments.
Both concise and lossless spellings are valid when authoring new code.
Use the concise spelling to omit its additional format fields together; the defaults below apply in that case.
The lossless spelling uses the named fields in the displayed order. The decompiler emits it when needed for byte identity.
See SHARED_SCRIPT_BYTES.md for the native lowering of multi-phase operations.

`-> (state)` means a VM slot is written, not a Python-style return value.
No operation returns a value as an expression; some change control flow.

## Primary instructions

All 256 primary dispatch slots have handler identities and effect summaries; 163 have an explicit input schema here. Other slots use dedicated flow/state/output forms, take no operand, or retain unclassified encoded bytes. Unverified bytes are called out at their handlers below.

### `00` — `Stop`

Handler: `0x800A1B70`; encoded length: 1 byte(s).

**Effect:** stops or yields the current Field script.

**XGS form:** `stop;`

**Inputs:**
- None.

### `01` — `Jmp`

Handler: `0x800A1E74`; encoded length: 3 byte(s).

**Effect:** unconditional VM jump handler.

**XGS form:** `goto <target>;`

**Inputs:**
- `target` (byte +1): control-flow destination; symbolic bytecode target, relocated during linking.

### `02` — `ConditionalJmp`

Handler: `0x800A1BD0`; encoded length: 8 byte(s).

**Effect:** conditional VM jump handler.

**XGS form:** `if (!(<left> == <right>)) goto <target>;`

**Inputs:**
- `left` (byte +1): left comparison/bit-test value; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `right` (byte +3): right comparison/bit-test value; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.
- `target` (byte +6): control-flow destination; symbolic bytecode target, relocated during linking.

**Handler evidence:** 800A1BD0 accepts only high nibbles 00/40/80/C0. Other modes leave both values zero without reading variables; standalone source uses the equivalent constant comparison. Conditions 1/7 and 6/9 are aliases.

### `03` — `OpenDialogueMode2`

Handler: `0x8009C104`; encoded length: 4 byte(s).

**Effect:** opens the selected dialogue block through the common creation path in fixed-size mode 2 using the trailing control byte.

**XGS form:** `dialogue.open_dialogue_mode2(<dialogue_id>, <dialogue_control>);`

**Inputs:**
- `dialogue_id` (byte +1): primary handler 0x8009C104 operand: dialogue id; unsigned 16-bit integer (0..65535).
- `dialogue_control` (byte +3): primary handler 0x8009C104 operand: dialogue control; encoded byte (0..255); may be a subfield of a larger value.

### `04` — `StopAndRedirectWaiters`

Handler: `0x800A1A8C`; encoded length: 1 byte(s).

**Effect:** redirects waiters before stopping.

**XGS form:** `event.stop_and_redirect_waiters();`

**Inputs:**
- None.

### `05` — `CallRelative3`

Handler: `0x800A17F4`; encoded length: 3 byte(s).

**Effect:** pushes IP+3 and calls a relative script routine.

**XGS form:** `call <target>;`

**Inputs:**
- `target` (byte +1): control-flow destination; symbolic bytecode target, relocated during linking.

### `06` — `CallRelative5`

Handler: `0x800A1730`; encoded length: 5 byte(s).

**Effect:** pushes IP+5 and calls a relative script routine.

**XGS form:** `call <target>;`

**Lossless form:** `flow.call_with_reserved_word(<target>, reserved=<reserved>);`

- Additional format field at byte +3: `u16 (0..65535)`; encoded from the named argument; concise default `0`.

**Inputs:**
- `target` (byte +1): control-flow destination; symbolic bytecode target, relocated during linking.

**Handler evidence:** 800A1730 reads only the target at +1 and pushes PC+5. flow.call_with_reserved_word(target, reserved=word) preserves its skipped u16 as an explicit format field; the linker relocates target and return continuation.

### `07` — `StartActorScript`

Handler: `0x8009EB78`; encoded length: 3 byte(s).

**Effect:** starts a target actor script without waiting.

**XGS form:** `start <actor>.routine[<routine>] priority <priority> async;`

**Inputs:**
- `actor` (byte +1): target actor; actor selector (including self/party selectors).
- `routine` (byte +2): routine ID and priority packed in one byte; routine ID (0..31) in the low five bits.
- `priority` (byte +2): priority in the same packed routine byte; priority (0..7) in the high three bits.

### `08` — `StartActorScriptAndWait`

Handler: `0x8009ED68`; encoded length: 3 byte(s).

**Effect:** starts and waits for actor script completion.

**XGS form:** `start <actor>.routine[<routine>] priority <priority> wait;`

**Inputs:**
- `actor` (byte +1): target actor; actor selector (including self/party selectors).
- `routine` (byte +2): routine ID and priority packed in one byte; routine ID (0..31) in the low five bits.
- `priority` (byte +2): priority in the same packed routine byte; priority (0..7) in the high three bits.

### `09` — `StartActorScriptAndWaitExtended`

Handler: `0x8009F0A0`; encoded length: 3 byte(s).

**Effect:** extended start/wait state machine.

**XGS form:** `start <actor>.routine[<routine>] priority <priority> wait_extended;`

**Inputs:**
- `actor` (byte +1): target actor; actor selector (including self/party selectors).
- `routine` (byte +2): routine ID and priority packed in one byte; routine ID (0..31) in the low five bits.
- `priority` (byte +2): priority in the same packed routine byte; priority (0..7) in the high three bits.

### `0A` — `CallTriggerZone2D`

Handler: `0x8009533C`; encoded length: 4 byte(s).

**Effect:** conditionally calls the encoded target when the physically controlled actor lies inside the indexed trigger's inclusive XZ quadrilateral, otherwise advances four bytes.

**XGS form:** `if (inside_trigger_2d(<trigger>)) call <target>;`

**Inputs:**
- `trigger` (byte +1): indexed 2D/3D trigger record; encoded byte (0..255); may be a subfield of a larger value.
- `target` (byte +2): control-flow destination; symbolic bytecode target, relocated during linking.

### `0B` — `InitializeNpcActor`

Handler: `0x800A1624`; encoded length: 3 byte(s).

**Effect:** initializes the current NPC from a selected field graphic, synchronizes placement, enables updates and visibility, and advances three bytes.

**XGS form:** `actor.initialize_npc_actor(<field_graphic>);`

**Inputs:**
- `field_graphic` (byte +1): primary handler 0x800A1624 operand: field graphic; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `0C` — `UpdatePlayerCharacterPreserveIP`

Handler: `0x8009F5A8`; encoded length: 1 byte(s).

**Effect:** runs the player-control update and restores the caller's VM instruction pointer afterward.

**XGS form:** `actor.process_player_control_if_owned_preserve_ip();`

**Inputs:**
- None.

### `0D` — `Return`

Handler: `0x800A18B8`; encoded length: 1 byte(s).

**Effect:** pops the Field script call stack.

**XGS form:** `return;`

**Inputs:**
- None.

### `0E` — `Opcode0EAdvance`

Handler: `0x80092404`; encoded length: 1 byte(s).

**Effect:** advances one byte without changing other state.

**XGS form:** `event.opcode0e_advance();`

**Inputs:**
- None.

### `0F` — `Opcode0FAdvance`

Handler: `0x800923E4`; encoded length: 1 byte(s).

**Effect:** advances one byte without changing other state.

**XGS form:** `event.opcode0f_advance();`

**Inputs:**
- None.

### `10` — `MoveActorToPosition`

Handler: `0x80098C00`; encoded length: 2 or 9 byte(s).

**Effect:** initializes unrestricted interpolated actor movement.

**XGS:** `movement.begin_actor_move(x, z, y);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.continue_actor_move(x, z, y);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.begin_actor_move(x, z, y, reserved_flags=reserved_flags);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.
- `reserved_flags`: reserved format flags, subset of 0x1F; concise default `0`.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.continue_actor_move(x, z, y, phase=phase);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.
- `phase`: byte value; concise default `1`.

**Result:** Native VM state effects; no expression value.


**Lowering:** Operands and control flow are rebuilt from this source using private native data and jumps. There is no dependency on a neighboring source instruction or original encoding. See [shared-byte lowering](SHARED_SCRIPT_BYTES.md).

### `11` — `MoveActorToPositionWithLimit`

Handler: `0x80098C3C`; encoded length: 4 or 9 byte(s).

**Effect:** phase zero initializes bounded interpolated movement and advances nine bytes; nonzero phases continue it, reread coordinates nine bytes earlier on completion, and resume four bytes after the continuation. The wrapper reads a limit at current IP+11 when its counter is uninitialized.

**XGS:** `movement.begin_actor_move_with_step_limit(x, z, y, step_limit);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.
- `step_limit`: 0..32767 immediate or VM variable.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.continue_actor_move_with_step_limit(x, z, y, limit_if_uninitialized);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.
- `limit_if_uninitialized`: 0..32767 immediate or VM variable.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.begin_actor_move_with_step_limit(x, z, y, step_limit, reserved_flags=reserved_flags);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.
- `step_limit`: 0..32767 immediate or VM variable.
- `reserved_flags`: reserved format flags, subset of 0x1F; concise default `0`.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.continue_actor_move_with_step_limit(x, z, y, limit_if_uninitialized, phase=phase, initial_limit=initial_limit);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.
- `limit_if_uninitialized`: 0..32767 immediate or VM variable.
- `phase`: byte value; concise default `0`.
- `initial_limit`: 0..32767 immediate or VM variable; concise default `state.unresolved`.

**Result:** Native VM state effects; no expression value.


**Lowering:** Operands and control flow are rebuilt from this source using private native data and jumps. There is no dependency on a neighboring source instruction or original encoding. See [shared-byte lowering](SHARED_SCRIPT_BYTES.md).

### `12` — `StartCustomFieldTransition`

Handler: `0x80093200`; encoded length: 9 byte(s).

**Effect:** waits for coordinator readiness, stages the destination field and entry parameter, records the transition mode and fade length, and yields for in-place map replacement.

**XGS form:** `world.start_custom_field_transition(<destination_field>, <entry_point>, <transition_mode>, <transition_duration>);`

**Inputs:**
- `destination_field` (byte +1): primary handler 0x80093200 operand: destination field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `entry_point` (byte +3): primary handler 0x80093200 operand: entry point; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `transition_mode` (byte +5): primary handler 0x80093200 operand: transition mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `transition_duration` (byte +7): primary handler 0x80093200 operand: transition duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `13` — `Nop`

Handler: `0x800A2FC0`; encoded length: 1 byte(s).

**Effect:** Field script no-op handler.

**XGS form:** `nop;`

**Inputs:**
- None.

### `14` — `DisableRandomEncounters`

Handler: `0x80093C48`; encoded length: 1 byte(s).

**Effect:** disables encounters.

**XGS form:** `world.encounters.enabled = false;`

**Inputs:**
- None.

### `15` — `EnableRandomEncounters`

Handler: `0x80093C6C`; encoded length: 1 byte(s).

**Effect:** enables random encounters.

**XGS form:** `world.encounters.enabled = true;`

**Inputs:**
- None.

### `16` — `InitializePlayableActor`

Handler: `0x800A08B8`; encoded length: 3 byte(s).

**Effect:** initializes a playable-character actor, updates player and party mappings, selects party or replacement graphics, applies entry placement, and handles missing-party fallback.

**XGS form:** `actor.bind_playable_character(<character_selector>);`

**Inputs:**
- `character_selector` (byte +1): primary handler 0x800A08B8 operand: character selector; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `17` — `SetupMovementBoundingZone`

Handler: `0x8009E91C`; encoded length: 18 byte(s).

**Effect:** allocates and loads four movement-boundary vertices.

**XGS form:** `movement.setup_movement_bounding_zone(<vertex_1_x>, <vertex_1_z>, <vertex_2_x>, <vertex_2_z>, <vertex_3_x>, <vertex_3_z>, <vertex_4_x>, <vertex_4_z>);`

**Inputs:**
- `vertex_1_x` (byte +1): primary handler 0x8009E91C operand: vertex 1 x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +17 bit 0x80 selects immediate.
- `vertex_1_z` (byte +3): primary handler 0x8009E91C operand: vertex 1 z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +17 bit 0x40 selects immediate.
- `vertex_2_x` (byte +5): primary handler 0x8009E91C operand: vertex 2 x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +17 bit 0x20 selects immediate.
- `vertex_2_z` (byte +7): primary handler 0x8009E91C operand: vertex 2 z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +17 bit 0x10 selects immediate.
- `vertex_3_x` (byte +9): primary handler 0x8009E91C operand: vertex 3 x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +17 bit 0x08 selects immediate.
- `vertex_3_z` (byte +11): primary handler 0x8009E91C operand: vertex 3 z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +17 bit 0x04 selects immediate.
- `vertex_4_x` (byte +13): primary handler 0x8009E91C operand: vertex 4 x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +17 bit 0x02 selects immediate.
- `vertex_4_z` (byte +15): primary handler 0x8009E91C operand: vertex 4 z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +17 bit 0x01 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +17 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `18` — `SetActorCollisionDimensions`

Handler: `0x8009E83C`; encoded length: 5 byte(s).

**Effect:** sets actor collision bounds.

**XGS form:** `movement.set_actor_collision_dimensions(<collision_half_width_x>, <collision_half_width_z>, <collision_height>, <solid_contact_range>);`

**Inputs:**
- `collision_half_width_x` (byte +1): primary handler 0x8009E83C operand: collision half width x; encoded byte (0..255); may be a subfield of a larger value.
- `collision_half_width_z` (byte +2): primary handler 0x8009E83C operand: collision half width z; encoded byte (0..255); may be a subfield of a larger value.
- `collision_height` (byte +3): primary handler 0x8009E83C operand: collision height; encoded byte (0..255); may be a subfield of a larger value.
- `solid_contact_range` (byte +4): primary handler 0x8009E83C operand: solid contact range; encoded byte (0..255); may be a subfield of a larger value.

### `19` — `SetCoordinatesAndClearMovementState`

Handler: `0x8009E4BC`; encoded length: 6 byte(s).

**Effect:** applies coordinates and clears movement state.

**XGS form:** `movement.set_coordinates_and_clear_movement_state(<x>, <z>);`

**Lossless form:** `movement.set_coordinates_and_clear_movement_state(<x>, <z>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x8009E4BC operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x8009E4BC operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `1A` — `SetWalkmeshAtCurrentPosition`

Handler: `0x8009E428`; encoded length: 2 byte(s).

**Effect:** changes walkmesh at the actor's current position.

**XGS form:** `movement.set_walkmesh_at_current_position(<walkmesh>);`

**Inputs:**
- `walkmesh` (byte +1): primary handler 0x8009E428 operand: walkmesh; encoded byte (0..255); may be a subfield of a larger value.

### `1B` — `SetWalkmeshAndCoordinates`

Handler: `0x8009E35C`; encoded length: 7 byte(s).

**Effect:** assigns walkmesh and evaluated actor coordinates.

**XGS form:** `movement.set_walkmesh_and_coordinates(<x>, <z>, <walkmesh>);`

**Lossless form:** `movement.set_walkmesh_and_coordinates(<x>, <z>, <walkmesh>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +6: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x8009E35C operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x8009E35C operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x40 selects immediate.
- `walkmesh` (byte +5): primary handler 0x8009E35C operand: walkmesh; encoded byte (0..255); may be a subfield of a larger value.

**Encoding:** The primary handler's separate control byte(s) at +6 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `1C` — `ApplySingleCoordinate`

Handler: `0x8009E2C8`; encoded length: 4 byte(s).

**Effect:** applies one evaluated actor coordinate.

**XGS form:** `movement.apply_single_coordinate(<actor_y>);`

**Lossless form:** `movement.apply_single_coordinate(<actor_y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +3: `flag subset 0x7F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `actor_y` (byte +1): primary handler 0x8009E2C8 operand: actor y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +3 bit 0x80 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +3 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `1D` — `ApplyThreeImmediateCoordinates`

Handler: `0x8009E248`; encoded length: 7 byte(s).

**Effect:** applies three immediate actor coordinates.

**XGS form:** `movement.apply_three_immediate_coordinates(<x>, <z>, <y>);`

**Inputs:**
- `x` (byte +1): primary handler 0x8009E248 operand: x; signed 16-bit integer (-32768..32767).
- `z` (byte +3): primary handler 0x8009E248 operand: z; signed 16-bit integer (-32768..32767).
- `y` (byte +5): primary handler 0x8009E248 operand: y; signed 16-bit integer (-32768..32767).

### `1E` — `ResetActorElevationTracking`

Handler: `0x8009E208`; encoded length: 1 byte(s).

**Effect:** clears the actor elevation offset, anchors elevation to the current Y position, and enables elevation updates.

**XGS form:** `actor.reset_actor_elevation_tracking();`

**Inputs:**
- None.

### `1F` — `SetActorLowFlagsFromPackedByte`

Handler: `0x8009E1A0`; encoded length: 2 byte(s).

**Effect:** replaces low actor flags from a packed byte.

**XGS form:** `actor.set_actor_low_flags_from_packed_byte(<packed_actor_flags>);`

**Inputs:**
- `packed_actor_flags` (byte +1): primary handler 0x8009E1A0 operand: packed actor flags; encoded byte (0..255); may be a subfield of a larger value.

### `20` — `SetCurrentActorFlags`

Handler: `0x8009E10C`; encoded length: 3 byte(s).

**Effect:** decodes a script mask into current-actor flags.

**XGS form:** `actor.set_current_actor_flags(<actor_flags>);`

**Inputs:**
- `actor_flags` (byte +1): primary handler 0x8009E10C operand: actor flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `21` — `SetActorMovementSpeed`

Handler: `0x8009E094`; encoded length: 3 byte(s).

**Effect:** writes the evaluated speed to SceneActorRecord+0x76, propagates it to the actor's bound field entity, and advances three bytecode bytes.

**XGS form:** `movement.set_actor_movement_speed(<movement_speed>);`

**Inputs:**
- `movement_speed` (byte +1): primary handler 0x8009E094 operand: movement speed; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `22` — `ShowActor`

Handler: `0x8009DF10`; encoded length: 1 byte(s).

**Effect:** shows the current actor.

**XGS form:** `actor.self.visible = true;`

**Inputs:**
- None.

### `23` — `HideActor`

Handler: `0x8009E040`; encoded length: 1 byte(s).

**Effect:** hides the current actor.

**XGS form:** `actor.self.visible = false;`

**Inputs:**
- None.

### `24` — `ShowActorById`

Handler: `0x8009DDEC`; encoded length: 2 byte(s).

**Effect:** shows an actor by ID.

**XGS form:** `<actor>.visible = true;`

**Inputs:**
- `actor` (byte +1): actor selector; actor selector (including self/party selectors).

### `25` — `HideActorById`

Handler: `0x8009DE94`; encoded length: 2 byte(s).

**Effect:** hides an actor by ID.

**XGS form:** `<actor>.visible = false;`

**Inputs:**
- `actor` (byte +1): actor selector; actor selector (including self/party selectors).

### `26` — `Sleep`

Handler: `0x8009DD34`; encoded length: 3 byte(s).

**Effect:** initializes the current slot's byte timer from the evaluated operand, yields on every dispatch, and advances after N+1 scheduler selections for timer value N.

**XGS form:** `flow.sleep(<delay_duration>);`

**Inputs:**
- `delay_duration` (byte +1): primary handler 0x8009DD34 operand: delay duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `27` — `StopAndDisableActorVM`

Handler: `0x8009DC4C`; encoded length: 2 byte(s).

**Effect:** stops movement and disables actor VM state.

**XGS form:** `actor.stop_and_disable_actor_vm(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009DC4C operand: actor; actor selector (including self/party selectors).

### `28` — `EnableActorVM`

Handler: `0x8009DBC8`; encoded length: 2 byte(s).

**Effect:** enables an actor VM.

**XGS form:** `actor.enable_actor_vm(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009DBC8 operand: actor; actor selector (including self/party selectors).

### `29` — `DisableAndHideActorVM`

Handler: `0x8009DAC4`; encoded length: 2 byte(s).

**Effect:** disables, hides, and closes actor dialogue.

**XGS form:** `actor.disable_and_hide_actor_vm(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009DAC4 operand: actor; actor selector (including self/party selectors).

### `2A` — `DisableDialogActivation`

Handler: `0x8009DA1C`; encoded length: 1 byte(s).

**Effect:** disables dialog.

**XGS form:** `actor.self.dialogue_enabled = false;`

**Inputs:**
- None.

### `2B` — `EnableDialogActivation`

Handler: `0x8009DA44`; encoded length: 1 byte(s).

**Effect:** enables dialog.

**XGS form:** `actor.self.dialogue_enabled = true;`

**Inputs:**
- None.

### `2C` — `PlayAnimation`

Handler: `0x8009A130`; encoded length: 2 byte(s).

**Effect:** starts an actor animation.

**XGS form:** `event.play_animation(<animation_id>);`

**Inputs:**
- `animation_id` (byte +1): primary handler 0x8009A130 operand: animation id; encoded byte (0..255); may be a subfield of a larger value.

### `2D` — `GetActorPosition`

Handler: `0x8009A024`; encoded length: 8 byte(s).

**Effect:** gets actor position.

**XGS form:** `movement.get_actor_position(<actor>) -> (<actor_position_x>, <actor_position_z>, <actor_position_y>);`

**Inputs:**
- `actor` (byte +1): actor selector; actor selector (including self/party selectors).

**VM writes:**
- `actor_position_x` (byte +2): VM output: actor position x; declared VM slot.
- `actor_position_z` (byte +4): VM output: actor position z; declared VM slot.
- `actor_position_y` (byte +6): VM output: actor position y; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `2E` — `GetActorDirection`

Handler: `0x80099FC4`; encoded length: 3 byte(s).

**Effect:** gets actor direction.

**XGS form:** `movement.get_actor_direction() -> (<actor_direction>);`

**Inputs:**
- None.

**VM writes:**
- `actor_direction` (byte +1): VM output: actor direction; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `2F` — `WriteCurCharacterID`

Handler: `0x80099EF8`; encoded length: 3 byte(s).

**Effect:** writes current character ID.

**XGS form:** `actor.write_cur_character_id() -> (<current_character_id>);`

**Inputs:**
- None.

**VM writes:**
- `current_character_id` (byte +1): VM output: current character id; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `30` — `WritePartyLeaderCharacterID`

Handler: `0x80099F48`; encoded length: 3 byte(s).

**Effect:** writes leader ID.

**XGS form:** `actor.write_party_leader_character_id() -> (<party_leader_character_id>);`

**Inputs:**
- None.

**VM writes:**
- `party_leader_character_id` (byte +1): VM output: party leader character id; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `31` — `CheckCurrentInputMask`

Handler: `0x800961A0`; encoded length: 5 byte(s).

**Effect:** advances five bytes when the evaluated mask intersects currently held controller input, otherwise branches to the encoded destination.

**XGS form:** `if ((input.held & <input_mask>) == 0) goto <target>;`

**Inputs:**
- `input_mask` (byte +1): primary handler 0x800961A0 operand: input mask; unsigned 16-bit integer (0..65535).
- `target` (byte +3): control-flow destination; symbolic bytecode target, relocated during linking.

### `32` — `CheckAccumulatedInputMask`

Handler: `0x800961C8`; encoded length: 5 byte(s).

**Effect:** advances five bytes when the evaluated mask intersects accumulated controller input, otherwise branches to the encoded destination.

**XGS form:** `if ((input.accumulated & <input_mask>) == 0) goto <target>;`

**Inputs:**
- `input_mask` (byte +1): primary handler 0x800961C8 operand: input mask; unsigned 16-bit integer (0..65535).
- `target` (byte +3): control-flow destination; symbolic bytecode target, relocated during linking.

### `33` — `ResetAccumulatedInput`

Handler: `0x800961F0`; encoded length: 1 byte(s).

**Effect:** clears the accumulated controller-input mask and advances one byte.

**XGS form:** `input.accumulated = 0;`

**Inputs:**
- None.

### `34` — `WriteInventoryObjectQuantity`

Handler: `0x80096214`; encoded length: 5 byte(s).

**Effect:** writes the encoded object's quantity to the requested script variable, using zero when the object is absent, and advances five bytes.

**XGS form:** `inventory.write_inventory_object_quantity(<inventory_object>) -> (<inventory_object_quantity>);`

**Inputs:**
- `inventory_object` (byte +1): primary handler 0x80096214 operand: inventory object; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `inventory_object_quantity` (byte +3): VM output: inventory object quantity; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `35` — `VariableAssign`

Handler: `0x8009D9A4`; encoded length: 6 byte(s).

**Effect:** assigns a variable.

**XGS form:** `<destination> = <source>;`

**Lossless form:** `state.assign(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `36` — `VariableSetTrue`

Handler: `0x8009D960`; encoded length: 3 byte(s).

**Effect:** sets a variable true.

**XGS form:** `<destination> = true;`

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.

### `37` — `VariableSetFalse`

Handler: `0x8009D91C`; encoded length: 3 byte(s).

**Effect:** clears a variable.

**XGS form:** `<destination> = false;`

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.

### `38` — `VariableAdd`

Handler: `0x8009D890`; encoded length: 6 byte(s).

**Effect:** adds to a variable.

**XGS form:** `<destination> += <source>;`

**Lossless form:** `state.add(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `39` — `VariableSub`

Handler: `0x8009D804`; encoded length: 6 byte(s).

**Effect:** subtracts from a variable.

**XGS form:** `<destination> -= <source>;`

**Lossless form:** `state.subtract(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `3A` — `VariableSetBit`

Handler: `0x8009D644`; encoded length: 6 byte(s).

**Effect:** evaluates a zero-based bit index, masks it to five bits, and sets that bit in the selected script variable.

**XGS form:** `<destination> |= 1 << <source>;`

**Lossless form:** `state.set_bit(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `3B` — `VariableUnsetBit`

Handler: `0x8009D408`; encoded length: 6 byte(s).

**Effect:** evaluates a zero-based bit index, masks it to five bits, and clears that bit in the selected script variable.

**XGS form:** `<destination> &= ~(1 << <source>);`

**Lossless form:** `state.clear_bit(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `3C` — `IncVariable`

Handler: `0x8009D340`; encoded length: 3 byte(s).

**Effect:** increments a variable.

**XGS form:** `<destination>++;`

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.

### `3D` — `DecVariable`

Handler: `0x8009D3A4`; encoded length: 3 byte(s).

**Effect:** decrements a variable.

**XGS form:** `<destination>--;`

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.

### `3E` — `VariableAND`

Handler: `0x8009D5B8`; encoded length: 6 byte(s).

**Effect:** applies variable AND.

**XGS form:** `<destination> &= <source>;`

**Lossless form:** `state.and(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `3F` — `VariableOR`

Handler: `0x8009D52C`; encoded length: 6 byte(s).

**Effect:** applies variable OR.

**XGS form:** `<destination> |= <source>;`

**Lossless form:** `state.or(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `40` — `VariableXOR`

Handler: `0x8009D4A0`; encoded length: 6 byte(s).

**Effect:** applies variable XOR.

**XGS form:** `<destination> ^= <source>;`

**Lossless form:** `state.xor(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `41` — `LShiftVariable`

Handler: `0x8009D2D0`; encoded length: 5 byte(s).

**Effect:** shifts a variable left.

**XGS form:** `<destination> <<= <count>;`

**Inputs:**
- `destination` (byte +1): VM slot being shifted; declared VM slot.
- `count` (byte +3): shift count; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `42` — `RShiftVariable`

Handler: `0x8009D260`; encoded length: 5 byte(s).

**Effect:** shifts a variable right.

**XGS form:** `<destination> >>= <count>;`

**Inputs:**
- `destination` (byte +1): VM slot being shifted; declared VM slot.
- `count` (byte +3): shift count; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `43` — `RandVariable`

Handler: `0x8009D198`; encoded length: 3 byte(s).

**Effect:** writes a random variable.

**XGS form:** `state.rand_variable() -> (<random_value>);`

**Inputs:**
- None.

**VM writes:**
- `random_value` (byte +1): VM output: random value; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `44` — `MoveActorAlongAngleWithStepLimit`

Handler: `0x80098184`; encoded length: 5 byte(s).

**Effect:** continues planar walking along a scripted angle until the step limit expires or the generated target is reached.

**XGS form:** `movement.move_actor_along_angle_with_step_limit(<angle>, <step_limit>);`

**Inputs:**
- `angle` (byte +1): primary handler 0x80098184 operand: angle; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `step_limit` (byte +3): primary handler 0x80098184 operand: step limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `45` — `MoveActorAlongAngle3DWithStepLimit`

Handler: `0x80097864`; encoded length: 8 byte(s).

**Effect:** continues three-axis walking toward a point 32 units along a scripted angle with a vertical offset until the destination or step limit is reached.

**XGS form:** `movement.move_actor_along_angle3d_with_step_limit(<angle>, <height_offset>, <step_limit>);`

**Lossless form:** `movement.move_actor_along_angle3d_with_step_limit(<angle>, <height_offset>, <step_limit>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +7: `flag subset 0x7F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `angle` (byte +1): primary handler 0x80097864 operand: angle; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `height_offset` (byte +3): primary handler 0x80097864 operand: height offset; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x80 selects immediate.
- `step_limit` (byte +5): primary handler 0x80097864 operand: step limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +7 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `46` — `StartYawDirectedMovement`

Handler: `0x80092808`; encoded length: 1 byte(s).

**Effect:** derives current-actor X and Z movement components from the field entity's Y rotation, marks scripted movement active, and advances one byte.

**XGS form:** `movement.start_yaw_directed_movement();`

**Inputs:**
- None.

### `47` — `WalkPlayerAndChangeField`

Handler: `0x80092EA0`; encoded length: 6 byte(s).

**Effect:** waits for transition systems, enables scripted player control, and walks the player toward the current actor's offset exit point while committing the pending field transition.

**XGS form:** `world.walk_player_and_change_field(<destination_field>, <entry_point>) -> (<map_entry_point>);`

**Lossless form:** `world.walk_player_and_change_field(<destination_field>, <entry_point>, reserved=<reserved>) -> (<map_entry_point>);`

- Additional format field at byte +1: `u8 (0..255)`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination_field` (byte +2): primary handler 0x80092EA0 operand: destination field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `entry_point` (byte +4): primary handler 0x80092EA0 operand: entry point; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `map_entry_point` (fixed VM slot 0x0002): fixed VM output: map entry point; declared VM slot (implicit).

**Handler evidence:** 80092EA0 delegates to 80092894: the words at +2/+4 are evaluated; byte +1 has no verified semantic read.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `48` — `WriteScriptU8ToVariable`

Handler: `0x80093CD0`; encoded length: 7 byte(s).

**Effect:** writes a script byte to a variable.

**XGS form:** `state.write_script_u8_to_variable(<data_base>, <index>) -> (<script_byte>);`

**Inputs:**
- `data_base` (byte +1): script-data base offset; not a branch target; symbolic script-data address, relocated during linking.
- `index` (byte +5): evaluated index added to script-data base; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `script_byte` (byte +3): VM output: script byte; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `49` — `WriteScriptS16ToVariable`

Handler: `0x80093D48`; encoded length: 8 byte(s).

**Effect:** writes a script halfword to a variable.

**XGS form:** `state.read_script_u16(<data_base>, <index>) -> (<script_halfword>);`

**Mode 0 (8 bytes):** `state.read_script_u16(<data_base>, <index>) -> (<script_halfword>);`
**Mode 1 (8 bytes):** `state.read_script_s16(<data_base>, <index>) -> (<script_halfword>);`
**Mode 2 (8 bytes):** `state.read_script_s16(<data_base>, <index>) -> (<script_halfword>);`
**Mode 3 (8 bytes):** `state.read_script_s16(<data_base>, <index>) -> (<script_halfword>);`

**Inputs:**
- `data_base` (byte +1): script-data base offset; not a branch target; symbolic script-data address, relocated during linking.
- `index` (byte +5): evaluated index added to script-data base; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `script_halfword` (byte +3): VM output: script halfword; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `4A` — `MoveActorToPositionUnlimited`

Handler: `0x80099980`; encoded length: 6 byte(s).

**Effect:** starts planar movement toward evaluated absolute coordinates with no step limit and waits for arrival.

**XGS form:** `movement.move_actor_to_position_unlimited(<x>, <z>);`

**Lossless form:** `movement.move_actor_to_position_unlimited(<x>, <z>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x80099980 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x80099980 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `4B` — `MoveActorToPositionWithStepLimit`

Handler: `0x80098430`; encoded length: 8 byte(s).

**Effect:** continues planar walking toward scripted absolute coordinates until the destination or step limit is reached.

**XGS form:** `movement.move_actor_to_position_with_step_limit(<x>, <z>, <step_limit>);`

**Lossless form:** `movement.move_actor_to_position_with_step_limit(<x>, <z>, <step_limit>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x80098430 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x80098430 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.
- `step_limit` (byte +6): primary handler 0x80098430 operand: step limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `4C` — `MoveActorToPosition3D`

Handler: `0x800979F0`; encoded length: 8 byte(s).

**Effect:** continues three-axis walking toward scripted absolute coordinates until the destination is reached.

**XGS form:** `movement.move_actor_to_position3d(<x>, <z>, <y>);`

**Lossless form:** `movement.move_actor_to_position3d(<x>, <z>, <y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x800979F0 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x800979F0 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.
- `y` (byte +6): primary handler 0x800979F0 operand: y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x20 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `4D` — `MoveActorToPosition3DWithStepLimit`

Handler: `0x80097954`; encoded length: 10 byte(s).

**Effect:** continues three-axis walking toward scripted absolute coordinates until the destination or step limit is reached.

**XGS form:** `movement.move_actor_to_position3d_with_step_limit(<x>, <z>, <y>, <step_limit>);`

**Lossless form:** `movement.move_actor_to_position3d_with_step_limit(<x>, <z>, <y>, <step_limit>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x80097954 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x80097954 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.
- `y` (byte +6): primary handler 0x80097954 operand: y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x20 selects immediate.
- `step_limit` (byte +8): primary handler 0x80097954 operand: step limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `4E` — `MoveActorByOffset`

Handler: `0x80098370`; encoded length: 6 byte(s).

**Effect:** continues planar walking toward coordinates relative to the starting position until the destination is reached.

**XGS form:** `movement.move_actor_by_offset(<x>, <z>);`

**Lossless form:** `movement.move_actor_by_offset(<x>, <z>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x80098370 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x80098370 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `4F` — `MoveActorByOffsetWithStepLimit`

Handler: `0x80098274`; encoded length: 8 byte(s).

**Effect:** continues planar walking toward coordinates relative to the starting position until the destination or step limit is reached.

**XGS form:** `movement.move_actor_by_offset_with_step_limit(<x>, <z>, <step_limit>);`

**Lossless form:** `movement.move_actor_by_offset_with_step_limit(<x>, <z>, <step_limit>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x80098274 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x80098274 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.
- `step_limit` (byte +6): primary handler 0x80098274 operand: step limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `50` — `MoveActorByOffset3D`

Handler: `0x800977A4`; encoded length: 8 byte(s).

**Effect:** continues three-axis walking toward a position relative to the starting coordinates until the destination is reached.

**XGS form:** `movement.move_actor_by_offset3d(<x>, <z>, <y>);`

**Lossless form:** `movement.move_actor_by_offset3d(<x>, <z>, <y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x800977A4 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x800977A4 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.
- `y` (byte +6): primary handler 0x800977A4 operand: y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x20 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `51` — `MoveActorByOffset3DWithStepLimit`

Handler: `0x800976A8`; encoded length: 10 byte(s).

**Effect:** continues three-axis walking toward a position relative to the starting coordinates until the destination or scripted step limit is reached.

**XGS form:** `movement.move_actor_by_offset3d_with_step_limit(<x>, <z>, <y>, <step_limit>);`

**Lossless form:** `movement.move_actor_by_offset3d_with_step_limit(<x>, <z>, <y>, <step_limit>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x800976A8 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x800976A8 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.
- `y` (byte +6): primary handler 0x800976A8 operand: y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x20 selects immediate.
- `step_limit` (byte +8): primary handler 0x800976A8 operand: step limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `52` — `FollowActor`

Handler: `0x800980FC`; encoded length: 2 byte(s).

**Effect:** continues planar walking toward a selected actor until contact range is reached.

**XGS form:** `actor.follow_actor(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x800980FC operand: actor; actor selector (including self/party selectors).

### `53` — `FollowActorWithStepLimit`

Handler: `0x80098038`; encoded length: 4 byte(s).

**Effect:** continues planar walking toward a selected actor until contact range or the scripted step limit is reached.

**XGS form:** `actor.follow_actor_with_step_limit(<actor>, <step_limit>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x80098038 operand: actor; actor selector (including self/party selectors).
- `step_limit` (byte +2): primary handler 0x80098038 operand: step limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `54` — `MoveTowardActorUnlimited`

Handler: `0x800975C0`; encoded length: 5 byte(s).

**Effect:** copies a selected actor's position into the movement target, uses an unlimited step count, moves toward the target, and advances five bytes upon completion.

**XGS form:** `movement.move_toward_actor_unlimited(<actor>, <height_offset>);`

**Lossless form:** `movement.move_toward_actor_unlimited(<actor>, <height_offset>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +4: `flag subset 0x7F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `actor` (byte +1): primary handler 0x800975C0 operand: actor; actor selector (including self/party selectors).
- `height_offset` (byte +2): primary handler 0x800975C0 operand: height offset; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +4 bit 0x80 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +4 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `55` — `MoveTowardActorWithStepLimit`

Handler: `0x8009749C`; encoded length: 7 byte(s).

**Effect:** copies a selected actor's position into the movement target, initializes the current slot's step limit, moves toward the target, and advances seven bytes upon completion.

**XGS form:** `movement.move_toward_actor_with_step_limit(<actor>, <height_offset>, <step_limit>);`

**Lossless form:** `movement.move_toward_actor_with_step_limit(<actor>, <height_offset>, <step_limit>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +4: `flag subset 0x7F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `actor` (byte +1): primary handler 0x8009749C operand: actor; actor selector (including self/party selectors).
- `height_offset` (byte +2): primary handler 0x8009749C operand: height offset; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +4 bit 0x80 selects immediate.
- `step_limit` (byte +5): primary handler 0x8009749C operand: step limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +4 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `56` — `StartWorldMapTransition`

Handler: `0x80093014`; encoded length: 10 byte(s).

**Effect:** waits for loading and audio readiness, persists current Field state, disables encounters, stores the return field, initial position, camera yaw, and World Map mode, requests the World Map handoff, and advances ten bytes.

**XGS form:** `world.start_world_map_transition(<return_field>, <start_position>, <camera_yaw>, <world_map_mode>);`

**Lossless form:** `world.start_world_map_transition(<return_field>, <start_position>, <camera_yaw>, <world_map_mode>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +9: `flag subset 0x0F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `return_field` (byte +1): primary handler 0x80093014 operand: return field; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +9 bit 0x80 selects immediate.
- `start_position` (byte +3): primary handler 0x80093014 operand: start position; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +9 bit 0x40 selects immediate.
- `camera_yaw` (byte +5): primary handler 0x80093014 operand: camera yaw; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +9 bit 0x20 selects immediate.
- `world_map_mode` (byte +7): primary handler 0x80093014 operand: world map mode; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +9 bit 0x10 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +9 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `57` — `ContinueBallisticActorMove`

Handler: `0x80099214`; encoded length: 2 or 11 byte(s).

**Effect:** initializes or continues a timed parabolic move to scripted coordinates, optionally derives elevation from the walkmesh, updates facing and position each frame, and supports walkmesh-only refresh mode.

**XGS:** `movement.finish_ballistic_actor_move(x, z, y, false);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.finish_ballistic_actor_move(x, z, y, true);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.finish_ballistic_actor_move(x, z, y, false, phase=phase);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.
- `phase`: byte value; concise default `3`.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.finish_ballistic_actor_move(x, z, y, true, phase=phase);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `y`: signed 16-bit value or VM variable.
- `phase`: byte value; concise default `3`.

**Result:** Native VM state effects; no expression value.

Long modes still use `movement.continue_ballistic_actor_move(mode, x, z, y, duration)`; mode 15 uses `movement.refresh_actor_walkmesh()`. Finish takes a final boolean selecting absolute Y or walkmesh height.

**Lowering:** Operands and control flow are rebuilt from this source using private native data and jumps. There is no dependency on a neighboring source instruction or original encoding. See [shared-byte lowering](SHARED_SCRIPT_BYTES.md).

### `58` — `SetCurrentActorAxisRotation`

Handler: `0x80094918`; encoded length: 4 byte(s).

**Effect:** assigns one current-actor axis.

**XGS form:** `movement.set_current_actor_axis_rotation(<axis_rotation>, <axis>);`

**Inputs:**
- `axis_rotation` (byte +1): primary handler 0x80094918 operand: axis rotation; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `axis` (byte +3): primary handler 0x80094918 operand: axis; encoded byte (0..255); may be a subfield of a larger value.

### `59` — `RandomTurnWithSpecialDirection`

Handler: `0x8009F4CC`; encoded length: 1 byte(s).

**Effect:** handles signed random turns.

**XGS form:** `movement.random_turn_with_special_direction();`

**Inputs:**
- None.

### `5A` — `ResetActorMovementState`

Handler: `0x8009524C`; encoded length: 1 byte(s).

**Effect:** clears current movement state, marks movement inactive, yields, and advances one byte.

**XGS form:** `movement.reset_actor_movement_state();`

**Inputs:**
- None.

### `5B` — `ParkActorMovementUpdate`

Handler: `0x80095284`; encoded length: 1 byte(s).

**Effect:** clears movement vectors and render offsets, marks movement and rotation inactive, and yields without advancing, so repeated selection parks the invocation on this opcode without releasing its slot.

**XGS form:** `movement.park_actor_movement_update();`

**Inputs:**
- None.

### `5C` — `InitializePartySlotActor`

Handler: `0x800A0228`; encoded length: 3 byte(s).

**Effect:** binds the current actor to a selected party slot, restores its saved map placement when applicable, initializes its sprite, and hides unavailable or off-map members.

**XGS form:** `actor.bind_party_slot(<party_slot>);`

**Inputs:**
- `party_slot` (byte +1): primary handler 0x800A0228 operand: party slot; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `5D` — `PlayAnimationAndClearCompletion`

Handler: `0x8009A174`; encoded length: 2 byte(s).

**Effect:** starts the immediate actor animation and clears its completion latch.

**XGS form:** `event.play_animation_and_clear_completion(<animation_id>);`

**Inputs:**
- `animation_id` (byte +1): primary handler 0x8009A174 operand: animation id; encoded byte (0..255); may be a subfield of a larger value.

### `5E` — `WaitForAnimationCompletion`

Handler: `0x8009A1AC`; encoded length: 1 byte(s).

**Effect:** stalls until the actor animation completion latch is set, then removes the forced animation and advances.

**XGS form:** `flow.wait_for_animation_completion();`

**Inputs:**
- None.

### `5F` — `SetImmediateActorCardinalDirection`

Handler: `0x8009AD6C`; encoded length: 2 byte(s).

**Effect:** rotates the current actor to the immediate world-relative cardinal direction.

**XGS form:** `movement.set_immediate_actor_cardinal_direction(<cardinal_direction>);`

**Inputs:**
- `cardinal_direction` (byte +1): primary handler 0x8009AD6C operand: cardinal direction; encoded byte (0..255); may be a subfield of a larger value.

### `60` — `ResetCameraTargetMovement`

Handler: `0x8008FDD0`; encoded length: 1 byte(s).

**Effect:** resets camera-target movement.

**XGS form:** `camera.reset_camera_target_movement();`

**Inputs:**
- None.

### `61` — `SetCameraTargetMovementFrom`

Handler: `0x8008FE2C`; encoded length: 8 byte(s).

**Effect:** sets camera-target origin.

**XGS form:** `camera.set_camera_target_movement_from(<x>, <z>, <y>);`

**Lossless form:** `camera.set_camera_target_movement_from(<x>, <z>, <y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +7: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x8008FE2C operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x8008FE2C operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x40 selects immediate.
- `y` (byte +5): primary handler 0x8008FE2C operand: y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x20 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +7 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `62` — `SetCameraTargetMovementDestToActor`

Handler: `0x8008FF04`; encoded length: 2 byte(s).

**Effect:** targets an actor.

**XGS form:** `camera.set_camera_target_movement_dest_to_actor(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8008FF04 operand: actor; actor selector (including self/party selectors).

### `63` — `SetCameraTargetMovementDest`

Handler: `0x8008FF90`; encoded length: 8 byte(s).

**Effect:** sets camera-target destination.

**XGS form:** `camera.set_camera_target_movement_dest(<x>, <z>, <y>);`

**Lossless form:** `camera.set_camera_target_movement_dest(<x>, <z>, <y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +7: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x8008FF90 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x8008FF90 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x40 selects immediate.
- `y` (byte +5): primary handler 0x8008FF90 operand: y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x20 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +7 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `64` — `ResetCameraPosMovement`

Handler: `0x80090068`; encoded length: 1 byte(s).

**Effect:** resets camera-position movement.

**XGS form:** `camera.reset_camera_pos_movement();`

**Inputs:**
- None.

### `65` — `SetCameraPosMovementFrom`

Handler: `0x800900C4`; encoded length: 8 byte(s).

**Effect:** sets camera-position origin.

**XGS form:** `camera.set_camera_pos_movement_from(<x>, <z>, <y>);`

**Lossless form:** `camera.set_camera_pos_movement_from(<x>, <z>, <y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +7: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x800900C4 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x800900C4 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x40 selects immediate.
- `y` (byte +5): primary handler 0x800900C4 operand: y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x20 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +7 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `66` — `SetCameraPosMovementDestToActor`

Handler: `0x8009019C`; encoded length: 2 byte(s).

**Effect:** moves camera to an actor.

**XGS form:** `camera.set_camera_pos_movement_dest_to_actor(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009019C operand: actor; actor selector (including self/party selectors).

### `67` — `SetActorDirection`

Handler: `0x8009ABFC`; encoded length: 4 byte(s).

**Effect:** sets actor direction.

**XGS form:** `movement.set_actor_direction(<actor>, <direction>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009ABFC operand: actor; actor selector (including self/party selectors).
- `direction` (byte +2): primary handler 0x8009ABFC operand: direction; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `68` — `SetTargetActorCameraRelativeDirection`

Handler: `0x8009AC34`; encoded length: 4 byte(s).

**Effect:** rotates a selected actor to a scripted cardinal direction relative to the camera.

**XGS form:** `camera.set_target_actor_camera_relative_direction(<actor>, <direction>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009AC34 operand: actor; actor selector (including self/party selectors).
- `direction` (byte +2): primary handler 0x8009AC34 operand: direction; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `69` — `SetCurActorRotation`

Handler: `0x8009AC7C`; encoded length: 3 byte(s).

**Effect:** sets current rotation.

**XGS form:** `movement.set_cur_actor_rotation(<rotation_sector>);`

**Inputs:**
- `rotation_sector` (byte +1): primary handler 0x8009AC7C operand: rotation sector; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `6A` — `SetCameraRelativeActorDirection`

Handler: `0x8009ACB4`; encoded length: 3 byte(s).

**Effect:** maps a direction to camera-relative actor rotation.

**XGS form:** `camera.set_camera_relative_actor_direction(<camera_relative_direction>);`

**Inputs:**
- `camera_relative_direction` (byte +1): primary handler 0x8009ACB4 operand: camera relative direction; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `6B` — `RotateActorClockwise`

Handler: `0x8009AB5C`; encoded length: 3 byte(s).

**Effect:** rotates actor clockwise.

**XGS form:** `actor.rotate_actor_clockwise(<turn_sector>);`

**Inputs:**
- `turn_sector` (byte +1): primary handler 0x8009AB5C operand: turn sector; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `6C` — `RotateActorCounterClockwise`

Handler: `0x8009ABAC`; encoded length: 3 byte(s).

**Effect:** rotates actor counter-clockwise.

**XGS form:** `actor.rotate_actor_counter_clockwise(<turn_sector>);`

**Inputs:**
- `turn_sector` (byte +1): primary handler 0x8009ABAC operand: turn sector; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `6D` — `Cos`

Handler: `0x8009A6AC`; encoded length: 8 byte(s).

**Effect:** Field script cosine helper.

**XGS form:** `event.cos(<angle>, <magnitude>) -> (<cosine_result>);`

**Lossless form:** `event.cos(<angle>, <magnitude>, reserved_flags=<reserved_flags>) -> (<cosine_result>);`

- Additional format field at byte +7: `flag subset 0x9F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `angle` (byte +3): primary handler 0x8009A6AC operand: angle; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x40 selects immediate.
- `magnitude` (byte +5): primary handler 0x8009A6AC operand: magnitude; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x20 selects immediate.

**VM writes:**
- `cosine_result` (byte +1): VM output: cosine result; declared VM slot.

**Encoding:** The primary handler's separate control byte(s) at +7 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `6E` — `Sin`

Handler: `0x8009A768`; encoded length: 8 byte(s).

**Effect:** Field script sine helper.

**XGS form:** `event.sin(<angle>, <magnitude>) -> (<sine_result>);`

**Lossless form:** `event.sin(<angle>, <magnitude>, reserved_flags=<reserved_flags>) -> (<sine_result>);`

- Additional format field at byte +7: `flag subset 0x9F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `angle` (byte +3): primary handler 0x8009A768 operand: angle; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x40 selects immediate.
- `magnitude` (byte +5): primary handler 0x8009A768 operand: magnitude; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x20 selects immediate.

**VM writes:**
- `sine_result` (byte +1): VM output: sine result; declared VM slot.

**Encoding:** The primary handler's separate control byte(s) at +7 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `6F` — `FaceActor`

Handler: `0x8009A2A8`; encoded length: 2 byte(s).

**Effect:** turns the current actor toward the selected actor when that actor is valid.

**XGS form:** `actor.face_actor(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009A2A8 operand: actor; actor selector (including self/party selectors).

### `70` — `FacePartyMember`

Handler: `0x8009A1E4`; encoded length: 2 byte(s).

**Effect:** turns the current actor toward the mapped party member when that member is present.

**XGS form:** `actor.face_party_member(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009A1E4 operand: actor; actor selector (including self/party selectors).

### `71` — `StartBattle`

Handler: `0x80093568`; encoded length: 3 byte(s).

**Effect:** waits for coordinator readiness, stores the requested battle configuration, marks the battle handoff pending, yields, and advances three bytes.

**XGS form:** `battle.start_battle(<battle_configuration>);`

**Inputs:**
- `battle_configuration` (byte +1): primary handler 0x80093568 operand: battle configuration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `72` — `PlayMusicModeZero`

Handler: `0x8008F724`; encoded length: 3 byte(s).

**Effect:** waits for music authorization then selects request mode zero and processes the requested music ID.

**XGS form:** `audio.play_music_mode_zero(<music_id>);`

**Inputs:**
- `music_id` (byte +1): primary handler 0x8008F724 operand: music id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `73` — `InitializeParticleSystemCommand`

Handler: `0x80086C34`; encoded length: 2 or 8 byte(s).

**Effect:** skips the disabled subcommand or initializes default particle banks and global particle parameters from three script arguments.

**XGS form:** `visual.initialize_particle_system_command(<subcommand>);`

**Mode 0 (2 bytes):** `visual.initialize_particle_system_command(<subcommand>);`
**Mode 1 (8 bytes):** `visual.initialize_particle_system_command(<subcommand>, <particle_default_preset>);`
**Mode 2 (2 bytes):** `stall_forever;`
**Mode 3 (2 bytes):** `stall_forever;`

**Inputs:**
- `subcommand` (byte +1): particle initialization subcommand; encoded byte (0..255); may be a subfield of a larger value.

**Handler evidence:** 80086C34 selects the two-byte disabled form for subcommand zero. Mode 1 evaluates +2/+4/+6 but only +4 affects the preset; the other evaluated results are discarded. Modes 2/3 retain their named subcommand and original encoding but do not advance in the handler.

### `74` — `PlaySoundEffect`

Handler: `0x8008F668`; encoded length: 3 byte(s).

**Effect:** starts or stops the requested sound effect on channel 3 and advances three bytes.

**XGS form:** `audio.play_sound_effect(<sound_effect_id>);`

**Inputs:**
- `sound_effect_id` (byte +1): primary handler 0x8008F668 operand: sound effect id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `75` — `PlayMusicModeMinusOne`

Handler: `0x8008F76C`; encoded length: 3 byte(s).

**Effect:** waits for music authorization then selects request mode minus one and processes the requested music ID.

**XGS form:** `audio.play_music_mode_minus_one(<music_id>);`

**Inputs:**
- `music_id` (byte +1): primary handler 0x8008F76C operand: music id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `76` — `ClearSceneFlag8000`

Handler: `0x80093A68`; encoded length: 1 byte(s).

**Effect:** clears scene flag 0x8000.

**XGS form:** `world.clear_scene_flag8000();`

**Inputs:**
- None.

### `77` — `SetSceneFlag8000`

Handler: `0x80093A98`; encoded length: 1 byte(s).

**Effect:** sets scene flag 0x8000.

**XGS form:** `world.set_scene_flag8000();`

**Inputs:**
- None.

### `78` — `WaitForArchiveFile`

Handler: `0x800973A4`; encoded length: 4 byte(s).

**Effect:** requests or waits for the selected archive file.

**XGS form:** `flow.wait_for_archive_file(<archive>, <file_id>);`

**Inputs:**
- `archive` (byte +1): primary handler 0x800973A4 operand: archive; encoded byte (0..255); may be a subfield of a larger value.
- `file_id` (byte +2): primary handler 0x800973A4 operand: file id; unsigned 16-bit integer (0..65535).

### `79` — `RestoreHp`

Handler: `0x80097264`; encoded length: 1 byte(s).

**Effect:** restores HP.

**XGS form:** `event.restore_hp();`

**Inputs:**
- None.

### `7A` — `RestoreMp`

Handler: `0x800972AC`; encoded length: 1 byte(s).

**Effect:** restores MP.

**XGS form:** `event.restore_mp();`

**Inputs:**
- None.

### `7B` — `DecreasePartyHp`

Handler: `0x800969FC`; encoded length: 4 byte(s).

**Effect:** VM party-HP decrease handler.

**XGS form:** `actor.decrease_party_hp(<party_hp_amount>, <party_member_mask_index>);`

**Lossless form:** `actor.decrease_party_hp(<party_hp_amount>, <party_member_mask_index>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +3: `flag subset 0x7C`; encoded from the named argument; concise default `0`.

**Inputs:**
- `party_hp_amount` (byte +1): primary handler 0x800969FC operand: party hp amount; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +3 bit 0x80 selects immediate.
- `party_member_mask_index` (byte +3): primary handler 0x800969FC operand: party member mask index; low two bits of a control byte (0..3).

**Encoding:** The primary handler's separate control byte(s) at +3 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `7C` — `IncreasePartyMp`

Handler: `0x80096F18`; encoded length: 4 byte(s).

**Effect:** increases party MP.

**XGS form:** `actor.increase_party_mp(<party_mp_amount>, <party_member_mask_index>);`

**Lossless form:** `actor.increase_party_mp(<party_mp_amount>, <party_member_mask_index>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +3: `flag subset 0x7C`; encoded from the named argument; concise default `0`.

**Inputs:**
- `party_mp_amount` (byte +1): primary handler 0x80096F18 operand: party mp amount; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +3 bit 0x80 selects immediate.
- `party_member_mask_index` (byte +3): primary handler 0x80096F18 operand: party member mask index; low two bits of a control byte (0..3).

**Encoding:** The primary handler's separate control byte(s) at +3 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `7D` — `DecreasePartyMp`

Handler: `0x80097010`; encoded length: 4 byte(s).

**Effect:** decreases party MP.

**XGS form:** `actor.decrease_party_mp(<party_mp_amount>, <party_member_mask_index>);`

**Lossless form:** `actor.decrease_party_mp(<party_mp_amount>, <party_member_mask_index>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +3: `flag subset 0x7C`; encoded from the named argument; concise default `0`.

**Inputs:**
- `party_mp_amount` (byte +1): primary handler 0x80097010 operand: party mp amount; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +3 bit 0x80 selects immediate.
- `party_member_mask_index` (byte +3): primary handler 0x80097010 operand: party member mask index; low two bits of a control byte (0..3).

**Encoding:** The primary handler's separate control byte(s) at +3 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `7E` — `IncreasePartyHp`

Handler: `0x80097108`; encoded length: 4 byte(s).

**Effect:** increases HP for each valid party member selected by the script mask, caps each result at maximum HP, and advances four bytes.

**XGS form:** `actor.increase_party_hp(<party_hp_amount>, <party_member_mask_index>);`

**Lossless form:** `actor.increase_party_hp(<party_hp_amount>, <party_member_mask_index>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +3: `flag subset 0x7C`; encoded from the named argument; concise default `0`.

**Inputs:**
- `party_hp_amount` (byte +1): primary handler 0x80097108 operand: party hp amount; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +3 bit 0x80 selects immediate.
- `party_member_mask_index` (byte +3): primary handler 0x80097108 operand: party member mask index; low two bits of a control byte (0..3).

**Encoding:** The primary handler's separate control byte(s) at +3 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `7F` — `SetWalkmeshMaterialMotionAngleOffset`

Handler: `0x80095300`; encoded length: 3 byte(s).

**Effect:** sets the global angular offset added to the current walkmesh material's motion direction before its XZ vector is accumulated.

**XGS form:** `movement.set_walkmesh_material_motion_angle_offset(<motion_angle_offset>);`

**Inputs:**
- `motion_angle_offset` (byte +1): primary handler 0x80095300 operand: motion angle offset; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `80` — `SetWalkmeshMaterialByte`

Handler: `0x80092664`; encoded length: 5 byte(s).

**Effect:** writes a resolved value to the selected byte lane of a walkmesh material flag and advances five bytes.

**XGS form:** `movement.set_walkmesh_material_byte(<walkmesh_material>, <byte_lane>, <material_value>);`

**Inputs:**
- `walkmesh_material` (byte +1): primary handler 0x80092664 operand: walkmesh material; encoded byte (0..255); may be a subfield of a larger value.
- `byte_lane` (byte +2): primary handler 0x80092664 operand: byte lane; encoded byte (0..255); may be a subfield of a larger value.
- `material_value` (byte +3): primary handler 0x80092664 operand: material value; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `81` — `OrWalkmeshMaterialByte`

Handler: `0x800926C8`; encoded length: 5 byte(s).

**Effect:** applies a bitwise OR with a resolved mask to the selected byte lane of a walkmesh material flag and advances five bytes.

**XGS form:** `movement.or_walkmesh_material_byte(<walkmesh_material>, <byte_lane>, <material_value>);`

**Inputs:**
- `walkmesh_material` (byte +1): primary handler 0x800926C8 operand: walkmesh material; encoded byte (0..255); may be a subfield of a larger value.
- `byte_lane` (byte +2): primary handler 0x800926C8 operand: byte lane; encoded byte (0..255); may be a subfield of a larger value.
- `material_value` (byte +3): primary handler 0x800926C8 operand: material value; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `82` — `WriteWalkmeshNormalByte`

Handler: `0x80093664`; encoded length: 5 byte(s).

**Effect:** reads a selected byte from a packed walkmesh-normal entry, writes it to the requested script variable, and advances five bytes.

**XGS form:** `movement.write_walkmesh_normal_byte(<walkmesh_material>, <byte_lane>) -> (<walkmesh_normal_byte>);`

**Inputs:**
- `walkmesh_material` (byte +1): primary handler 0x80093664 operand: walkmesh material; encoded byte (0..255); may be a subfield of a larger value.
- `byte_lane` (byte +2): primary handler 0x80093664 operand: byte lane; encoded byte (0..255); may be a subfield of a larger value.

**VM writes:**
- `walkmesh_normal_byte` (byte +3): VM output: walkmesh normal byte; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `83` — `AndWalkmeshMaterialByte`

Handler: `0x80092768`; encoded length: 5 byte(s).

**Effect:** applies a bitwise AND with a resolved mask to the selected byte lane of a walkmesh material flag and advances five bytes.

**XGS form:** `movement.and_walkmesh_material_byte(<walkmesh_material>, <byte_lane>, <material_value>);`

**Inputs:**
- `walkmesh_material` (byte +1): primary handler 0x80092768 operand: walkmesh material; encoded byte (0..255); may be a subfield of a larger value.
- `byte_lane` (byte +2): primary handler 0x80092768 operand: byte lane; encoded byte (0..255); may be a subfield of a larger value.
- `material_value` (byte +3): primary handler 0x80092768 operand: material value; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `84` — `CheckScenarioFlagsLessThan`

Handler: `0x80096644`; encoded length: 5 byte(s).

**Effect:** compares scenario flags.

**XGS form:** `state.check_scenario_flags_less_than(<scenario_flags>, <target>);`

**Inputs:**
- `scenario_flags` (byte +1): primary handler 0x80096644 operand: scenario flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target` (byte +3): primary handler 0x80096644 operand: target; symbolic bytecode target, relocated during linking.

### `85` — `CheckScenarioFlagsGreaterThan`

Handler: `0x800966B4`; encoded length: 5 byte(s).

**Effect:** compares scenario flags.

**XGS form:** `state.check_scenario_flags_greater_than(<scenario_flags>, <target>);`

**Inputs:**
- `scenario_flags` (byte +1): primary handler 0x800966B4 operand: scenario flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target` (byte +3): primary handler 0x800966B4 operand: target; symbolic bytecode target, relocated during linking.

### `86` — `CheckScenarioFlagsEqual`

Handler: `0x80096724`; encoded length: 5 byte(s).

**Effect:** compares scenario flags.

**XGS form:** `state.check_scenario_flags_equal(<scenario_flags>, <target>);`

**Inputs:**
- `scenario_flags` (byte +1): primary handler 0x80096724 operand: scenario flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target` (byte +3): primary handler 0x80096724 operand: target; symbolic bytecode target, relocated during linking.

### `87` — `SetScenarioFlags`

Handler: `0x80096790`; encoded length: 3 byte(s).

**Effect:** writes scenario flags.

**XGS form:** `state.set_scenario_flags(<scenario_flags>) -> (<story_progress>);`

**Inputs:**
- `scenario_flags` (byte +1): primary handler 0x80096790 operand: scenario flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `story_progress` (fixed VM slot 0x0000): fixed VM output: story progress; declared VM slot (implicit).

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `88` — `GetScenarioFlags`

Handler: `0x800967E8`; encoded length: 3 byte(s).

**Effect:** reads scenario flags.

**XGS form:** `state.get_scenario_flags() -> (<scenario_flags>);`

**Inputs:**
- None.

**VM writes:**
- `scenario_flags` (byte +1): VM output: scenario flags; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `89` — `CheckActorDistance`

Handler: `0x80095E48`; encoded length: 6 byte(s).

**Effect:** tests actor distance.

**XGS form:** `actor.check_actor_distance(<actor>, <distance>, <target>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x80095E48 operand: actor; actor selector (including self/party selectors).
- `distance` (byte +2): primary handler 0x80095E48 operand: distance; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target` (byte +4): primary handler 0x80095E48 operand: target; symbolic bytecode target, relocated during linking.

### `8A` — `CheckActorOnScreen`

Handler: `0x80095C00`; encoded length: 4 byte(s).

**Effect:** tests actor screen visibility.

**XGS form:** `actor.check_actor_on_screen(<actor>, <target>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x80095C00 operand: actor; actor selector (including self/party selectors).
- `target` (byte +2): primary handler 0x80095C00 operand: target; symbolic bytecode target, relocated during linking.

### `8B` — `CheckInventoryObject`

Handler: `0x800962C0`; encoded length: 5 byte(s).

**Effect:** advances five bytes when the encoded object is present, otherwise branches to the encoded destination.

**XGS form:** `inventory.check_inventory_object(<inventory_object>, <target>);`

**Inputs:**
- `inventory_object` (byte +1): primary handler 0x800962C0 operand: inventory object; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target` (byte +3): primary handler 0x800962C0 operand: target; symbolic bytecode target, relocated during linking.

### `8C` — `AddInventoryObject`

Handler: `0x8009631C`; encoded length: 3 byte(s).

**Effect:** adds one unit of the encoded object and advances three bytes.

**XGS form:** `inventory.add_inventory_object(<inventory_object>);`

**Inputs:**
- `inventory_object` (byte +1): primary handler 0x8009631C operand: inventory object; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `8D` — `RemoveInventoryObject`

Handler: `0x8009640C`; encoded length: 3 byte(s).

**Effect:** decrements an encoded object's quantity, clears its identifier when the quantity reaches zero, and advances three bytes.

**XGS form:** `inventory.remove_inventory_object(<inventory_object>);`

**Inputs:**
- `inventory_object` (byte +1): primary handler 0x8009640C operand: inventory object; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `8E` — `CheckGoldAmount`

Handler: `0x80095F24`; encoded length: 7 byte(s).

**Effect:** tests the current gold amount.

**XGS form:** `inventory.check_gold_amount(<gold_threshold>, <target>);`

**Inputs:**
- `gold_threshold` (byte +1): primary handler 0x80095F24 operand: gold threshold; unsigned 32-bit integer (0..4294967295).
- `target` (byte +5): primary handler 0x80095F24 operand: target; symbolic bytecode target, relocated during linking.

**Handler evidence:** 80095F24 compares the unsigned 32-bit little-endian amount at +1 against party gold; +5 is a separate branch target.

### `8F` — `IncreaseGold`

Handler: `0x80095FB8`; encoded length: 3 byte(s).

**Effect:** increases party gold.

**XGS form:** `inventory.increase_gold(<gold_delta>);`

**Inputs:**
- `gold_delta` (byte +1): primary handler 0x80095FB8 operand: gold delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `90` — `DecreaseGold`

Handler: `0x8009601C`; encoded length: 3 byte(s).

**Effect:** decreases party gold.

**XGS form:** `inventory.decrease_gold(<gold_delta>);`

**Inputs:**
- `gold_delta` (byte +1): primary handler 0x8009601C operand: gold delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `91` — `CheckPartyMember`

Handler: `0x800964B0`; encoded length: 4 byte(s).

**Effect:** tests party membership.

**XGS form:** `actor.check_party_member(<party_character_id>, <target>);`

**Inputs:**
- `party_character_id` (byte +1): primary handler 0x800964B0 operand: party character id; encoded byte (0..255); may be a subfield of a larger value.
- `target` (byte +2): primary handler 0x800964B0 operand: target; symbolic bytecode target, relocated during linking.

### `92` — `InitializeActorScripts`

Handler: `0x800A19B0`; encoded length: 1 byte(s).

**Effect:** clears and disables all eight script slots of the current actor, resets scheduler state and yields without advancing the instruction pointer; there is no fallthrough to the next byte.

**XGS form:** `actor.initialize_actor_scripts();`

**Inputs:**
- None.

### `93` — `AddCurrentActorToMechaList`

Handler: `0x800A1364`; encoded length: 3 byte(s).

**Effect:** initializes the current actor's base graphic, synchronizes its transform, assigns a mecha-list slot and model identifier, and increments the mecha count.

**XGS form:** `actor.add_current_actor_to_mecha_list(<mecha_graphic>);`

**Inputs:**
- `mecha_graphic` (byte +1): primary handler 0x800A1364 operand: mecha graphic; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `94` — `SetAndPauseEventTimer`

Handler: `0x800945D4`; encoded length: 5 byte(s).

**Effect:** packs two evaluated bytes into VM variable 0x0A, pauses the event timer, and resets its update divider.

**XGS form:** `event.set_and_pause_event_timer(<timer_high_byte>, <timer_low_byte>) -> (<event_timer>);`

**Inputs:**
- `timer_high_byte` (byte +1): primary handler 0x800945D4 operand: timer high byte; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `timer_low_byte` (byte +3): primary handler 0x800945D4 operand: timer low byte; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `event_timer` (fixed VM slot 0x000A): fixed VM output: event timer; declared VM slot (implicit).

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `95` — `ConfigureEventTimer`

Handler: `0x80094650`; encoded length: 2 byte(s).

**Effect:** configures event-timer pause and count direction from the supplied control byte.

**XGS form:** `event.configure_event_timer(<timer_control>);`

**Inputs:**
- `timer_control` (byte +1): primary handler 0x80094650 operand: timer control; encoded byte (0..255); may be a subfield of a larger value.

### `96` — `PauseAndResetEventTimerDivider`

Handler: `0x8009468C`; encoded length: 1 byte(s).

**Effect:** pauses the event timer and resets its update divider.

**XGS form:** `event.pause_and_reset_event_timer_divider();`

**Inputs:**
- None.

### `97` — `SetAutomaticCameraRotationSectorMask`

Handler: `0x8009A634`; encoded length: 3 byte(s).

**Effect:** sets the eight-sector mask that can trigger automatic camera orbit correction.

**XGS form:** `camera.set_automatic_camera_rotation_sector_mask(<camera_sector_mask>);`

**Inputs:**
- `camera_sector_mask` (byte +1): primary handler 0x8009A634 operand: camera sector mask; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `98` — `ChangeFieldWhenReady`

Handler: `0x800932D0`; encoded length: 5 byte(s).

**Effect:** waits for coordinator readiness, optionally saves the current field and directions, stages the destination field and entry parameter, marks replacement pending, then yields and advances five bytes.

**XGS form:** `world.change_field_when_ready(<destination_field>, <entry_point>) -> (<map_entry_point>);`

**Inputs:**
- `destination_field` (byte +1): primary handler 0x800932D0 operand: destination field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `entry_point` (byte +3): primary handler 0x800932D0 operand: entry point; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `map_entry_point` (fixed VM slot 0x0002): fixed VM output: map entry point; declared VM slot (implicit).

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `99` — `EnterScriptCameraMode`

Handler: `0x8008FB98`; encoded length: 1 byte(s).

**Effect:** enters script-controlled camera mode, snapshots yaw, dip, and scaled depth, resets scale, initializes target and eye smoothing to twelve, and locks manual orbit.

**XGS form:** `camera.enter_script_camera_mode();`

**Inputs:**
- None.

### `9A` — `LeaveOrReacquireFollowCamera`

Handler: `0x8008FC4C`; encoded length: 3 byte(s).

**Effect:** in script mode, zero duration returns immediately to normal follow and advances six bytes, while nonzero duration starts asynchronous mode-2 reacquisition and advances three; an invocation already in mode 2 leaves the PC unchanged.

**XGS form:** `camera.leave_or_reacquire_follow_camera(<camera_reacquisition_duration>);`

**Inputs:**
- `camera_reacquisition_duration` (byte +1): primary handler 0x8008FC4C operand: camera reacquisition duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `9B` — `SetCameraSmoothingDivisors`

Handler: `0x8008FD40`; encoded length: 5 byte(s).

**Effect:** sets independent target and eye smoothing divisors, normalizing either zero operand to one.

**XGS form:** `camera.set_camera_smoothing_divisors(<target_smoothing_divisor>, <eye_smoothing_divisor>);`

**Inputs:**
- `target_smoothing_divisor` (byte +1): primary handler 0x8008FD40 operand: target smoothing divisor; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `eye_smoothing_divisor` (byte +3): primary handler 0x8008FD40 operand: eye smoothing divisor; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `9C` — `WaitForOwnedTextBox`

Handler: `0x8009BB0C`; encoded length: 1 byte(s).

**Effect:** waits for the actor's owned text box, conditionally requests its closure, and copies the confirmed choice line to VM variable 0x14 after the slot is released.

**XGS form:** `flow.wait_for_owned_text_box() -> (<dialogue_choice_line>);`

**Inputs:**
- None.

**VM writes:**
- `dialogue_choice_line` (fixed VM slot 0x0014): fixed VM output: dialogue choice line; declared VM slot (implicit).

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `9D` — `InterpolateSceneScale`

Handler: `0x8009A34C`; encoded length: 4 byte(s).

**Effect:** starts interpolation from the current scene scale to the scripted scale over the encoded duration.

**XGS form:** `world.interpolate_scene_scale(<scene_scale>, <duration>);`

**Inputs:**
- `scene_scale` (byte +1): primary handler 0x8009A34C operand: scene scale; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +3): primary handler 0x8009A34C operand: duration; encoded byte (0..255); may be a subfield of a larger value.

### `9E` — `SnapshotCameraGeometry`

Handler: `0x8009B9A0`; encoded length: 1 byte(s).

**Effect:** waits for camera rotation to finish, then saves the current camera direction, projection distance, and depth-cue parameter and advances one byte.

**XGS form:** `camera.snapshot_camera_geometry();`

**Inputs:**
- None.

### `9F` — `RestoreCameraGeometry`

Handler: `0x8009BA0C`; encoded length: 1 byte(s).

**Effect:** waits for camera rotation to finish, then interpolates camera direction, projection distance, and depth-cue parameter back to their saved values over 32 frames.

**XGS form:** `camera.restore_camera_geometry();`

**Inputs:**
- None.

### `A0` — `SetScreenGeometry`

Handler: `0x8009BA7C`; encoded length: 7 byte(s).

**Effect:** sets depth cue, camera direction, and projection distance from three script operands, updates the geometry screen distance, and advances seven bytes.

**XGS form:** `event.set_screen_geometry(<camera_sector>, <camera_dip>, <projection_screen_distance>);`

**Inputs:**
- `camera_sector` (byte +1): primary handler 0x8009BA7C operand: camera sector; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `camera_dip` (byte +3): primary handler 0x8009BA7C operand: camera dip; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `projection_screen_distance` (byte +5): primary handler 0x8009BA7C operand: projection screen distance; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `A1` — `SetBlockedCameraSectorMask`

Handler: `0x8009A670`; encoded length: 3 byte(s).

**Effect:** sets the eight-sector mask used to reject or redirect manual camera orbit.

**XGS form:** `camera.set_blocked_camera_sector_mask(<blocked_camera_sector_mask>);`

**Inputs:**
- `blocked_camera_sector_mask` (byte +1): primary handler 0x8009A670 operand: blocked camera sector mask; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `A2` — `WaitForCameraAnimationMask`

Handler: `0x8009A58C`; encoded length: 2 byte(s).

**Effect:** advances only after every camera animation flag selected by the immediate mask has cleared.

**XGS form:** `flow.wait_for_camera_animation_mask(<camera_animation_mask>);`

**Inputs:**
- `camera_animation_mask` (byte +1): primary handler 0x8009A58C operand: camera animation mask; encoded byte (0..255); may be a subfield of a larger value.

### `A3` — `SetCameraPosMovementDest`

Handler: `0x80090228`; encoded length: 8 byte(s).

**Effect:** sets camera-position destination.

**XGS form:** `camera.set_camera_pos_movement_dest(<x>, <z>, <y>);`

**Lossless form:** `camera.set_camera_pos_movement_dest(<x>, <z>, <y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +7: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +1): primary handler 0x80090228 operand: x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x80 selects immediate.
- `z` (byte +3): primary handler 0x80090228 operand: z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x40 selects immediate.
- `y` (byte +5): primary handler 0x80090228 operand: y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x20 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +7 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `A4` — `InterpolateCameraDip`

Handler: `0x8009A490`; encoded length: 4 byte(s).

**Effect:** starts interpolation from the current camera dip to the scripted dip over the encoded duration.

**XGS form:** `camera.interpolate_camera_dip(<camera_dip>, <duration>);`

**Inputs:**
- `camera_dip` (byte +1): primary handler 0x8009A490 operand: camera dip; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +3 bit 0x80 selects immediate.
- `duration` (byte +3): primary handler 0x8009A490 operand: duration; low seven bits of a control byte (0..127).

**Encoding:** The primary handler's separate control byte(s) at +3 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `A5` — `WriteCameraDirection`

Handler: `0x8009A534`; encoded length: 3 byte(s).

**Effect:** writes camera direction.

**XGS form:** `camera.write_camera_direction() -> (<camera_direction>);`

**Inputs:**
- None.

**VM writes:**
- `camera_direction` (byte +1): VM output: camera direction; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `A6` — `SkipScriptTriplets`

Handler: `0x80097410`; encoded length: 3 byte(s).

**Effect:** advances by three plus three times the evaluated signed count.

**XGS form:** `flow.dispatch_triplet_table(<triplet_skip_count>);`

**Inputs:**
- `triplet_skip_count` (byte +1): primary handler 0x80097410 operand: triplet skip count; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `A7` — `UpdatePlayerCharacter`

Handler: `0x8009F5F4`; encoded length: 1 byte(s).

**Effect:** processes player movement eligibility, idle detection, directional input, movement triggers, and facing before advancing one byte.

**XGS form:** `actor.process_player_control_if_owned();`

**Inputs:**
- None.

### `A8` — `MulVariableWithRand`

Handler: `0x8009D1F0`; encoded length: 5 byte(s).

**Effect:** multiplies by random.

**XGS form:** `state.mul_variable_with_rand(<scale>) -> (<scaled_random_value>);`

**Inputs:**
- `scale` (byte +3): evaluated upper scale for random result; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `scaled_random_value` (byte +1): VM output: scaled random value; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `A9` — `SetupMultichoice`

Handler: `0x8009BC98`; encoded length: 2 byte(s).

**Effect:** waits for the current dialogue to accept choices, configures the packed first and last choice indices, initializes cursor state, and advances two bytes.

**XGS form:** `event.setup_multichoice(<first_choice>, <last_choice>);`

**Inputs:**
- `first_choice` (byte +1): primary handler 0x8009BC98 operand: first choice; high nibble of a packed byte (0..15).
- `last_choice` (byte +1): primary handler 0x8009BC98 operand: last choice; low nibble of a packed byte (0..15).

### `AA` — `SetImmediateCameraRelativeActorDirection`

Handler: `0x8009ACEC`; encoded length: 2 byte(s).

**Effect:** rotates the current actor to an immediate cardinal direction relative to the camera.

**XGS form:** `camera.set_immediate_camera_relative_actor_direction(<cardinal_direction>);`

**Inputs:**
- `cardinal_direction` (byte +1): primary handler 0x8009ACEC operand: cardinal direction; encoded byte (0..255); may be a subfield of a larger value.

### `AB` — `ResetCameraMovements`

Handler: `0x80090300`; encoded length: 1 byte(s).

**Effect:** resets Field camera movements.

**XGS form:** `camera.reset_camera_movements();`

**Inputs:**
- None.

### `AC` — `StartCameraMovement`

Handler: `0x800903BC`; encoded length: 4 byte(s).

**Effect:** starts Field camera movement.

**XGS form:** `camera.start_camera_movement(<camera_movement_mode>, <duration>);`

**Inputs:**
- `camera_movement_mode` (byte +1): primary handler 0x800903BC operand: camera movement mode; encoded byte (0..255); may be a subfield of a larger value.
- `duration` (byte +2): primary handler 0x800903BC operand: duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `AD` — `WriteCameraTweenTarget`

Handler: `0x80090B18`; encoded length: 7 byte(s).

**Effect:** writes tween camera target.

**XGS form:** `camera.write_camera_tween_target() -> (<camera_tween_target_x>, <camera_tween_target_z>, <camera_tween_target_y>);`

**Inputs:**
- None.

**VM writes:**
- `camera_tween_target_x` (byte +1): VM output: camera tween target x; declared VM slot.
- `camera_tween_target_z` (byte +3): VM output: camera tween target z; declared VM slot.
- `camera_tween_target_y` (byte +5): VM output: camera tween target y; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `AE` — `WriteCameraTweenPosition`

Handler: `0x80090B9C`; encoded length: 7 byte(s).

**Effect:** writes tween camera position.

**XGS form:** `camera.write_camera_tween_position() -> (<camera_tween_position_x>, <camera_tween_position_z>, <camera_tween_position_y>);`

**Inputs:**
- None.

**VM writes:**
- `camera_tween_position_x` (byte +1): VM output: camera tween position x; declared VM slot.
- `camera_tween_position_z` (byte +3): VM output: camera tween position z; declared VM slot.
- `camera_tween_position_y` (byte +5): VM output: camera tween position y; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `AF` — `ReadOrWriteCameraYaw`

Handler: `0x80090C20`; encoded length: 4 byte(s).

**Effect:** writes the captured camera yaw to a variable when the control byte is zero or replaces it from the operand otherwise, then advances four bytes.

**XGS form:** `camera.read_or_write_camera_yaw() -> (<destination>);`

**Mode 0 (4 bytes):** `camera.read_or_write_camera_yaw() -> (<destination>);`
**Mode 1 (4 bytes):** `camera.yaw = <value>;`
**Mode 2 (4 bytes):** `camera.yaw = <value>;`
**Mode 3 (4 bytes):** `camera.yaw = <value>;`

**Inputs:**
- None.

**VM writes:**
- `destination` (byte +1): VM slot written with captured camera state; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `B0` — `ReadOrWriteCameraDip`

Handler: `0x80090CB8`; encoded length: 4 byte(s).

**Effect:** writes the captured projection dip to a variable when the control byte is zero or replaces it from the operand otherwise, then advances four bytes.

**XGS form:** `camera.read_or_write_camera_dip() -> (<destination>);`

**Mode 0 (4 bytes):** `camera.read_or_write_camera_dip() -> (<destination>);`
**Mode 1 (4 bytes):** `camera.projection_dip = <value>;`
**Mode 2 (4 bytes):** `camera.projection_dip = <value>;`
**Mode 3 (4 bytes):** `camera.projection_dip = <value>;`

**Inputs:**
- None.

**VM writes:**
- `destination` (byte +1): VM slot written with captured camera state; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `B1` — `ReadOrWriteCameraDepth`

Handler: `0x80090D50`; encoded length: 4 byte(s).

**Effect:** writes the captured scaled projection depth to a variable when the control byte is zero or replaces it with the unsigned operand otherwise, then advances four bytes.

**XGS form:** `camera.read_or_write_camera_depth() -> (<destination>);`

**Mode 0 (4 bytes):** `camera.read_or_write_camera_depth() -> (<destination>);`
**Mode 1 (4 bytes):** `camera.projection_depth = <value>;`
**Mode 2 (4 bytes):** `camera.projection_depth = <value>;`
**Mode 3 (4 bytes):** `camera.projection_depth = <value>;`

**Inputs:**
- None.

**VM writes:**
- `destination` (byte +1): VM slot written with captured camera state; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `B2` — `YieldUntilCameraAnimationMaskClears`

Handler: `0x8009A5E0`; encoded length: 2 byte(s).

**Effect:** yields script execution while any camera animation flag selected by the immediate mask remains set.

**XGS form:** `flow.yield_until_camera_animation_mask_clears(<camera_animation_mask>);`

**Inputs:**
- `camera_animation_mask` (byte +1): primary handler 0x8009A5E0 operand: camera animation mask; encoded byte (0..255); may be a subfield of a larger value.

### `B3` — `FadeOut`

Handler: `0x8009731C`; encoded length: 3 byte(s).

**Effect:** starts a script fade-out.

**XGS form:** `visual.fade_out(<fade_duration>);`

**Inputs:**
- `fade_duration` (byte +1): primary handler 0x8009731C operand: fade duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `B4` — `FadeIn`

Handler: `0x80097364`; encoded length: 3 byte(s).

**Effect:** starts a script fade-in.

**XGS form:** `visual.fade_in(<fade_duration>);`

**Inputs:**
- `fade_duration` (byte +1): primary handler 0x80097364 operand: fade duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `B5` — `SetCameraDirectionTimed`

Handler: `0x8009B8E4`; encoded length: 5 byte(s).

**Effect:** snaps the camera direction when interpolation is disabled, otherwise waits for an active rotation or starts a timed direction change, then advances five bytes and yields.

**XGS form:** `camera.set_camera_direction_timed(<camera_direction>, <duration>);`

**Inputs:**
- `camera_direction` (byte +1): primary handler 0x8009B8E4 operand: camera direction; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +3): primary handler 0x8009B8E4 operand: duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `B6` — `InterpolateProjectionDepth`

Handler: `0x8009B6AC`; encoded length: 5 byte(s).

**Effect:** applies or interpolates projection depth to the scripted value over the scripted duration.

**XGS form:** `camera.interpolate_projection_depth(<projection_depth>, <duration>);`

**Inputs:**
- `projection_depth` (byte +1): primary handler 0x8009B6AC operand: projection depth; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +3): primary handler 0x8009B6AC operand: duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `B7` — `DisableCameraHeightCheck`

Handler: `0x8009ADDC`; encoded length: 1 byte(s).

**Effect:** disables automatic camera height checking and advances the script.

**XGS form:** `camera.disable_camera_height_check();`

**Inputs:**
- None.

### `B8` — `EnableCameraHeightCheck`

Handler: `0x8009AE0C`; encoded length: 1 byte(s).

**Effect:** enables automatic camera height checking and advances the script.

**XGS form:** `camera.enable_camera_height_check();`

**Inputs:**
- None.

### `B9` — `CheckAvailablePartyMember`

Handler: `0x80096534`; encoded length: 4 byte(s).

**Effect:** advances four bytes when the requested character is available, otherwise branches to the encoded destination.

**XGS form:** `actor.check_available_party_member(<availability_bit_index>, <target>);`

**Inputs:**
- `availability_bit_index` (byte +1): primary handler 0x80096534 operand: availability bit index; encoded byte (0..255); may be a subfield of a larger value.
- `target` (byte +2): primary handler 0x80096534 operand: target; symbolic bytecode target, relocated during linking.

### `BA` — `AddAvailablePartyMember`

Handler: `0x800965A8`; encoded length: 2 byte(s).

**Effect:** sets the requested character's availability bit and advances two bytes.

**XGS form:** `actor.add_available_party_member(<character_id>);`

**Inputs:**
- `character_id` (byte +1): primary handler 0x800965A8 operand: character id; encoded byte (0..255); may be a subfield of a larger value.

### `BB` — `RemoveAvailablePartyMember`

Handler: `0x800965F4`; encoded length: 2 byte(s).

**Effect:** clears the requested character's availability bit and advances two bytes.

**XGS form:** `movement.remove_available_party_member(<character_id>);`

**Inputs:**
- `character_id` (byte +1): primary handler 0x800965F4 operand: character id; encoded byte (0..255); may be a subfield of a larger value.

### `BC` — `InitializeActorSprite`

Handler: `0x800A0D3C`; encoded length: 1 byte(s).

**Effect:** creates and initializes actor sprite state.

**XGS form:** `actor.initialize_actor_sprite();`

**Inputs:**
- None.

### `BD` — `IncreaseCurrentActorRotationX`

Handler: `0x80094A5C`; encoded length: 3 byte(s).

**Effect:** increments actor rotation X.

**XGS form:** `movement.increase_current_actor_rotation_x(<rotation_delta>);`

**Inputs:**
- `rotation_delta` (byte +1): primary handler 0x80094A5C operand: rotation delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `BE` — `DecreaseCurrentActorRotationX`

Handler: `0x80094ACC`; encoded length: 3 byte(s).

**Effect:** decrements actor rotation X.

**XGS form:** `movement.decrease_current_actor_rotation_x(<rotation_delta>);`

**Inputs:**
- `rotation_delta` (byte +1): primary handler 0x80094ACC operand: rotation delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `BF` — `IncreaseCurrentActorRotationY`

Handler: `0x80094B3C`; encoded length: 3 byte(s).

**Effect:** increments actor rotation Y.

**XGS form:** `movement.increase_current_actor_rotation_y(<rotation_delta>);`

**Inputs:**
- `rotation_delta` (byte +1): primary handler 0x80094B3C operand: rotation delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `C0` — `DecreaseCurrentActorRotationY`

Handler: `0x80094BAC`; encoded length: 3 byte(s).

**Effect:** decrements actor rotation Y.

**XGS form:** `movement.decrease_current_actor_rotation_y(<rotation_delta>);`

**Inputs:**
- `rotation_delta` (byte +1): primary handler 0x80094BAC operand: rotation delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `C1` — `IncreaseCurrentActorRotationZ`

Handler: `0x80094C1C`; encoded length: 3 byte(s).

**Effect:** increments actor rotation Z.

**XGS form:** `movement.increase_current_actor_rotation_z(<rotation_delta>);`

**Inputs:**
- `rotation_delta` (byte +1): primary handler 0x80094C1C operand: rotation delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `C2` — `DecreaseCurrentActorRotationZ`

Handler: `0x80094C8C`; encoded length: 3 byte(s).

**Effect:** decrements actor rotation Z.

**XGS form:** `movement.decrease_current_actor_rotation_z(<rotation_delta>);`

**Inputs:**
- `rotation_delta` (byte +1): primary handler 0x80094C8C operand: rotation delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `C3` — `YieldCurrentCycle`

Handler: `0x800972F4`; encoded length: 1 byte(s).

**Effect:** requests a VM yield and advances one byte.

**XGS form:** `flow.yield_current_cycle();`

**Inputs:**
- None.

### `C4` — `RotateActorAndSetStateFlag`

Handler: `0x80093E30`; encoded length: 2 byte(s).

**Effect:** rotates an actor over 30 steps and sets its state flag.

**XGS form:** `actor.rotate_actor_and_set_state_flag(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x80093E30 operand: actor; actor selector (including self/party selectors).

### `C5` — `RotateActorAndClearStateFlag`

Handler: `0x80093FC0`; encoded length: 2 byte(s).

**Effect:** applies the inverse rotation and clears its state flag.

**XGS form:** `actor.rotate_actor_and_clear_state_flag(<actor>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x80093FC0 operand: actor; actor selector (including self/party selectors).

### `C6` — `Yield32`

Handler: `0x800A1E9C`; encoded length: 1 byte(s).

**Effect:** adds 32 instructions to the VM budget and yields.

**XGS form:** `flow.yield32();`

**Inputs:**
- None.

### `C7` — `StartCameraDollyMode0`

Handler: `0x8009B824`; encoded length: 3 byte(s).

**Effect:** starts a positive one-eighth-turn camera dolly rotation over the requested duration when no rotation is active, then advances three bytes and yields.

**XGS form:** `camera.start_camera_dolly_mode0(<dolly_duration>);`

**Inputs:**
- `dolly_duration` (byte +1): primary handler 0x8009B824 operand: dolly duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `C8` — `StartCameraDollyMode0Alias`

Handler: `0x8009B884`; encoded length: 3 byte(s).

**Effect:** starts a positive one-eighth-turn camera dolly rotation over the requested duration when no rotation is active, then advances three bytes and yields.

**XGS form:** `camera.start_camera_dolly_mode0_alias(<dolly_duration>);`

**Inputs:**
- `dolly_duration` (byte +1): primary handler 0x8009B884 operand: dolly duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `C9` — `BranchUnlessInsideTriggerZone2D`

Handler: `0x80095734`; encoded length: 4 byte(s).

**Effect:** advances four bytes when the physically controlled actor lies inside the indexed trigger's inclusive XZ quadrilateral, otherwise branches to the encoded target.

**XGS form:** `if (!inside_trigger_2d(<trigger>)) goto <target>;`

**Inputs:**
- `trigger` (byte +1): indexed 2D/3D trigger record; encoded byte (0..255); may be a subfield of a larger value.
- `target` (byte +2): control-flow destination; symbolic bytecode target, relocated during linking.

### `CA` — `Atan2`

Handler: `0x8009A824`; encoded length: 8 byte(s).

**Effect:** Field script atan2 helper.

**XGS form:** `event.atan2(<angle>, <magnitude>) -> (<atan2_result>);`

**Lossless form:** `event.atan2(<angle>, <magnitude>, reserved_flags=<reserved_flags>) -> (<atan2_result>);`

- Additional format field at byte +7: `flag subset 0x9F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `angle` (byte +3): primary handler 0x8009A824 operand: angle; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x40 selects immediate.
- `magnitude` (byte +5): primary handler 0x8009A824 operand: magnitude; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +7 bit 0x20 selects immediate.

**VM writes:**
- `atan2_result` (byte +1): VM output: atan2 result; declared VM slot.

**Encoding:** The primary handler's separate control byte(s) at +7 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `CB` — `BranchUnlessInsideTriggerZone3D`

Handler: `0x800958C0`; encoded length: 4 byte(s).

**Effect:** advances four bytes when the physically controlled actor straddles the indexed trigger's Y plane and lies inside its inclusive XZ quadrilateral, otherwise branches to the encoded target.

**XGS form:** `if (!inside_trigger_3d(<trigger>)) goto <target>;`

**Inputs:**
- `trigger` (byte +1): indexed 2D/3D trigger record; encoded byte (0..255); may be a subfield of a larger value.
- `target` (byte +2): control-flow destination; symbolic bytecode target, relocated during linking.

### `CC` — `CallTriggerZone3D`

Handler: `0x80095520`; encoded length: 4 byte(s).

**Effect:** conditionally calls the encoded target when the physically controlled actor straddles the indexed trigger's Y plane and lies inside its inclusive XZ quadrilateral, otherwise advances four bytes.

**XGS form:** `if (inside_trigger_3d(<trigger>)) call <target>;`

**Inputs:**
- `trigger` (byte +1): indexed 2D/3D trigger record; encoded byte (0..255); may be a subfield of a larger value.
- `target` (byte +2): control-flow destination; symbolic bytecode target, relocated during linking.

### `CD` — `DisableAutomaticContactScript`

Handler: `0x8009DA70`; encoded length: 1 byte(s).

**Effect:** suppresses automatic contact routine 3 while leaving explicit interaction routine 2 available.

**XGS form:** `event.disable_automatic_contact_script();`

**Inputs:**
- None.

### `CE` — `EnableAutomaticContactScript`

Handler: `0x8009DA98`; encoded length: 1 byte(s).

**Effect:** re-enables automatic contact routine 3.

**XGS form:** `event.enable_automatic_contact_script();`

**Inputs:**
- None.

### `CF` — `SetDialogueWindowImmediate`

Handler: `0x8009CE48`; encoded length: 5 byte(s).

**Effect:** stores immediate forced X and Y positions, width in three-pixel character units, and height, then advances five bytes.

**XGS form:** `dialogue.set_dialogue_window_immediate(<window_x_half_pixels>, <window_y>, <window_width_three_pixel_units>, <window_height>);`

**Inputs:**
- `window_x_half_pixels` (byte +1): primary handler 0x8009CE48 operand: window x half pixels; encoded byte (0..255); may be a subfield of a larger value.
- `window_y` (byte +2): primary handler 0x8009CE48 operand: window y; encoded byte (0..255); may be a subfield of a larger value.
- `window_width_three_pixel_units` (byte +3): primary handler 0x8009CE48 operand: window width three pixel units; encoded byte (0..255); may be a subfield of a larger value.
- `window_height` (byte +4): primary handler 0x8009CE48 operand: window height; encoded byte (0..255); may be a subfield of a larger value.

### `D0` — `SetDialogueWindowEvaluated`

Handler: `0x8009CEE0`; encoded length: 11 byte(s).

**Effect:** evaluates and stores forced X and Y positions, width in three-pixel character units, height, and window flags, then advances eleven bytes.

**XGS form:** `dialogue.set_dialogue_window_evaluated(<window_x>, <window_y>, <window_width_three_pixel_units>, <window_height>, <window_flags>);`

**Inputs:**
- `window_x` (byte +1): primary handler 0x8009CEE0 operand: window x; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `window_y` (byte +3): primary handler 0x8009CEE0 operand: window y; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `window_width_three_pixel_units` (byte +5): primary handler 0x8009CEE0 operand: window width three pixel units; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `window_height` (byte +7): primary handler 0x8009CEE0 operand: window height; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `window_flags` (byte +9): primary handler 0x8009CEE0 operand: window flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `D1` — `ImmediateNoop`

Handler: `0x8009CF70`; encoded length: stall byte(s).

**Effect:** returns immediately without changing script or field state.

**XGS form:** `stall_forever;`

**Lossless form:** `flow.stall_d1();`


**Inputs:**
- None.

### `D2` — `OpenActorDialogueMode0`

Handler: `0x8009C0B4`; encoded length: 4 byte(s).

**Effect:** opens the selected dialogue block in actor-anchored mode 0 using the trailing control byte.

**XGS form:** `dialogue.open_actor_dialogue_mode0(<dialogue_id>, <dialogue_control>);`

**Inputs:**
- `dialogue_id` (byte +1): primary handler 0x8009C0B4 operand: dialogue id; unsigned 16-bit integer (0..65535).
- `dialogue_control` (byte +3): primary handler 0x8009C0B4 operand: dialogue control; encoded byte (0..255); may be a subfield of a larger value.

### `D3` — `OpenDialogueMode1`

Handler: `0x8009C0DC`; encoded length: 4 byte(s).

**Effect:** opens the selected dialogue block in window mode 1 using the trailing control byte.

**XGS form:** `dialogue.open_dialogue_mode1(<dialogue_id>, <dialogue_control>);`

**Inputs:**
- `dialogue_id` (byte +1): primary handler 0x8009C0DC operand: dialogue id; unsigned 16-bit integer (0..65535).
- `dialogue_control` (byte +3): primary handler 0x8009C0DC operand: dialogue control; encoded byte (0..255); may be a subfield of a larger value.

### `D4` — `OpenDialogueAtActorMode0`

Handler: `0x8009C01C`; encoded length: 5 byte(s).

**Effect:** resolves the selected actor as the placement anchor for the current actor's mode-zero dialogue, skips six bytes for an invalid actor, or rewinds one byte to retry deferred creation.

**XGS form:** `dialogue.open_dialogue_at_actor_mode0(<actor>, <dialogue_id>, <dialogue_control>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009C01C operand: actor; actor selector (including self/party selectors).
- `dialogue_id` (byte +2): primary handler 0x8009C01C operand: dialogue id; unsigned 16-bit integer (0..65535).
- `dialogue_control` (byte +4): primary handler 0x8009C01C operand: dialogue control; encoded byte (0..255); may be a subfield of a larger value.

### `D5` — `SetControllerBtnMask`

Handler: `0x80092628`; encoded length: 3 byte(s).

**Effect:** sets controller mask.

**XGS form:** `input.set_controller_btn_mask(<controller_button_mask>);`

**Inputs:**
- `controller_button_mask` (byte +1): primary handler 0x80092628 operand: controller button mask; unsigned 16-bit integer (0..65535).

### `D6` — `SetDialogAnimationSpeed`

Handler: `0x800925A0`; encoded length: 3 byte(s).

**Effect:** stores the dialog animation mode and selects eight, six, or four opening frames for modes zero, one, or two before advancing three bytes.

**XGS form:** `dialogue.set_dialog_animation_speed(<dialogue_animation_mode>);`

**Inputs:**
- `dialogue_animation_mode` (byte +1): primary handler 0x800925A0 operand: dialogue animation mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `D7` — `SetObjectSwivelXAxis`

Handler: `0x800946BC`; encoded length: 3 byte(s).

**Effect:** selects object swivel around X and stores the evaluated angle.

**XGS form:** `event.set_object_swivel_x_axis(<rotation_angle>);`

**Inputs:**
- `rotation_angle` (byte +1): primary handler 0x800946BC operand: rotation angle; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `D8` — `SetObjectSwivelYAxis`

Handler: `0x80094710`; encoded length: 3 byte(s).

**Effect:** selects object swivel around Y and stores the evaluated angle.

**XGS form:** `event.set_object_swivel_y_axis(<rotation_angle>);`

**Inputs:**
- `rotation_angle` (byte +1): primary handler 0x80094710 operand: rotation angle; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `D9` — `SetObjectSwivelZAxis`

Handler: `0x80094764`; encoded length: 3 byte(s).

**Effect:** selects object swivel around Z and stores the evaluated angle.

**XGS form:** `event.set_object_swivel_z_axis(<rotation_angle>);`

**Inputs:**
- `rotation_angle` (byte +1): primary handler 0x80094764 operand: rotation angle; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `DA` — `CreateLineScrollEffect`

Handler: `0x800921E8`; encoded length: 17 byte(s).

**Effect:** allocates and fills a line-scroll byte buffer, allocates and initializes its descriptor from six geometry parameters, registers up to 32 effects, and advances seventeen bytes.

**XGS form:** `visual.create_line_scroll_effect(<source_x>, <source_y>, <source_width>, <source_total_height>, <row_count>, <destination_x>, <destination_y>, <initial_scroll_byte>);`

**Lossless form:** `visual.create_line_scroll_effect(<source_x>, <source_y>, <source_width>, <source_total_height>, <row_count>, <destination_x>, <destination_y>, <initial_scroll_byte>, reserved_tail=<reserved_tail>);`

- Additional format field at byte +16: `u8 (0..255)`; encoded from the named argument; concise default `0`.

**Inputs:**
- `source_x` (byte +1): primary handler 0x800921E8 operand: source x; unsigned 16-bit integer (0..65535).
- `source_y` (byte +3): primary handler 0x800921E8 operand: source y; unsigned 16-bit integer (0..65535).
- `source_width` (byte +5): primary handler 0x800921E8 operand: source width; unsigned 16-bit integer (0..65535).
- `source_total_height` (byte +7): primary handler 0x800921E8 operand: source total height; unsigned 16-bit integer (0..65535).
- `row_count` (byte +9): primary handler 0x800921E8 operand: row count; unsigned 16-bit integer (0..65535).
- `destination_x` (byte +11): primary handler 0x800921E8 operand: destination x; unsigned 16-bit integer (0..65535).
- `destination_y` (byte +13): primary handler 0x800921E8 operand: destination y; unsigned 16-bit integer (0..65535).
- `initial_scroll_byte` (byte +15): primary handler 0x800921E8 operand: initial scroll byte; encoded byte (0..255); may be a subfield of a larger value.

### `DB` — `SetDeformationStrength`

Handler: `0x80091F84`; encoded length: 5 byte(s).

**Effect:** caps a deformation strength at 0xFFF, stores it in the selected deformation slot when the current model supports deformation, and advances five bytes.

**XGS form:** `event.set_deformation_strength(<deformation_slot>, <strength>);`

**Inputs:**
- `deformation_slot` (byte +1): primary handler 0x80091F84 operand: deformation slot; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `strength` (byte +3): primary handler 0x80091F84 operand: strength; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `DC` — `SwapVariables`

Handler: `0x80092044`; encoded length: 5 byte(s).

**Effect:** exchanges the values of two script variables and advances five bytes.

**XGS form:** `state.swap(<left>, <right>);`

**Inputs:**
- `left` (byte +1): first VM slot; declared VM slot.
- `right` (byte +3): second VM slot; declared VM slot.

### `DD` — `SetCurrentActorBlendParameters`

Handler: `0x80091E00`; encoded length: 6 byte(s).

**Effect:** replaces rendering blend bits 5 through 6 from the low two operand bits, stores the accompanying rendering value, and advances six bytes.

**XGS form:** `actor.set_current_actor_blend_parameters(<blend_mode>, <blend_value>);`

**Lossless form:** `actor.set_current_actor_blend_parameters(<blend_mode>, <blend_value>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `blend_mode` (byte +1): primary handler 0x80091E00 operand: blend mode; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x80 selects immediate.
- `blend_value` (byte +3): primary handler 0x80091E00 operand: blend value; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +5 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `DE` — `VariableMul`

Handler: `0x8009D6D8`; encoded length: 6 byte(s).

**Effect:** multiplies a variable.

**XGS form:** `<destination> *= <source>;`

**Lossless form:** `state.multiply(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `DF` — `VariableDiv`

Handler: `0x8009D768`; encoded length: 6 byte(s).

**Effect:** divides a variable.

**XGS form:** `<destination> /= <source>;`

**Lossless form:** `state.divide(<destination>, <source>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +5: `flag subset 0xBF`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination` (byte +1): VM slot being updated; declared VM slot.
- `source` (byte +3): value applied to destination; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +5 bit 0x40 selects immediate.

### `E0` — `SetSelectedActorBlendParameters`

Handler: `0x80091E98`; encoded length: 7 byte(s).

**Effect:** resolves an actor and, when valid, replaces its rendering blend bits and accompanying rendering value before advancing seven bytes.

**XGS form:** `actor.set_selected_actor_blend_parameters(<actor>, <blend_mode>, <blend_value>);`

**Lossless form:** `actor.set_selected_actor_blend_parameters(<actor>, <blend_mode>, <blend_value>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +6: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `actor` (byte +1): primary handler 0x80091E98 operand: actor; actor selector (including self/party selectors).
- `blend_mode` (byte +2): primary handler 0x80091E98 operand: blend mode; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x80 selects immediate.
- `blend_value` (byte +4): primary handler 0x80091E98 operand: blend value; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x40 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +6 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `E1` — `MoveOrClearVramRectangle`

Handler: `0x80091BBC`; encoded length: 14 byte(s).

**Effect:** clears the rectangle defined by the final four operands when the first coordinate pair is zero, otherwise moves the rectangle defined by the first four operands to the final coordinate pair, then advances fourteen bytes.

**XGS form:** `movement.move_or_clear_vram_rectangle(<source_x>, <source_y>, <width>, <height>, <destination_x>, <destination_y>);`

**Lossless form:** `movement.move_or_clear_vram_rectangle(<source_x>, <source_y>, <width>, <height>, <destination_x>, <destination_y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +13: `flag subset 0x03`; encoded from the named argument; concise default `0`.

**Inputs:**
- `source_x` (byte +1): primary handler 0x80091BBC operand: source x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x80 selects immediate.
- `source_y` (byte +3): primary handler 0x80091BBC operand: source y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x40 selects immediate.
- `width` (byte +5): primary handler 0x80091BBC operand: width; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x20 selects immediate.
- `height` (byte +7): primary handler 0x80091BBC operand: height; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x10 selects immediate.
- `destination_x` (byte +9): primary handler 0x80091BBC operand: destination x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x08 selects immediate.
- `destination_y` (byte +11): primary handler 0x80091BBC operand: destination y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x04 selects immediate.

**Encoding:** The primary handler's separate control byte(s) at +13 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

### `E2` — `CheckCurrentInputExact`

Handler: `0x80096150`; encoded length: 5 byte(s).

**Effect:** conditionally branches on exact current input.

**XGS form:** `input.check_current_input_exact(<input_mask>, <target>);`

**Inputs:**
- `input_mask` (byte +1): primary handler 0x80096150 operand: input mask; unsigned 16-bit integer (0..65535).
- `target` (byte +3): primary handler 0x80096150 operand: target; symbolic bytecode target, relocated during linking.

### `E3` — `CheckAccumulatedInputExact`

Handler: `0x80096178`; encoded length: 5 byte(s).

**Effect:** conditionally branches on exact accumulated input.

**XGS form:** `input.check_accumulated_input_exact(<input_mask>, <target>);`

**Inputs:**
- `input_mask` (byte +1): primary handler 0x80096178 operand: input mask; unsigned 16-bit integer (0..65535).
- `target` (byte +3): primary handler 0x80096178 operand: target; symbolic bytecode target, relocated during linking.

### `E4` — `OpcodeE4NoOp`

Handler: `0x80091AD4`; encoded length: stall byte(s).

**Effect:** returns immediately without changing state or advancing script execution.

**XGS form:** `stall_forever;`

**Lossless form:** `flow.stall_e4();`


**Inputs:**
- None.

### `E5` — `ConfigureFog`

Handler: `0x80091944`; encoded length: 17 byte(s).

**Effect:** stores the base depth-cue RGB color, far-fog RGB color, and near/far fog distances, enables fog, applies the new configuration, and advances seventeen bytes.

**XGS form:** `event.configure_fog(<depth_cue_base_red>, <depth_cue_base_green>, <depth_cue_base_blue>, <fog_far_red>, <fog_far_green>, <fog_far_blue>, <fog_near_distance>, <fog_far_distance>);`

**Inputs:**
- `depth_cue_base_red` (byte +1): primary handler 0x80091944 operand: depth cue base red; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `depth_cue_base_green` (byte +3): primary handler 0x80091944 operand: depth cue base green; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `depth_cue_base_blue` (byte +5): primary handler 0x80091944 operand: depth cue base blue; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `fog_far_red` (byte +7): primary handler 0x80091944 operand: fog far red; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `fog_far_green` (byte +9): primary handler 0x80091944 operand: fog far green; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `fog_far_blue` (byte +11): primary handler 0x80091944 operand: fog far blue; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `fog_near_distance` (byte +13): primary handler 0x80091944 operand: fog near distance; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `fog_far_distance` (byte +15): primary handler 0x80091944 operand: fog far distance; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `E6` — `SetCameraLimits`

Handler: `0x80091A08`; encoded length: 9 byte(s).

**Effect:** stores the signed camera-limit origin and extents while negating the final extent and advances nine bytes.

**XGS form:** `camera.set_camera_limits(<limit_x>, <limit_z>, <extent_x>, <extent_z>);`

**Inputs:**
- `limit_x` (byte +1): primary handler 0x80091A08 operand: limit x; signed 16-bit integer (-32768..32767).
- `limit_z` (byte +3): primary handler 0x80091A08 operand: limit z; signed 16-bit integer (-32768..32767).
- `extent_x` (byte +5): primary handler 0x80091A08 operand: extent x; signed 16-bit integer (-32768..32767).
- `extent_z` (byte +7): primary handler 0x80091A08 operand: extent z; signed 16-bit integer (-32768..32767).

### `E7` — `SetExtendedBackgroundClearColor`

Handler: `0x80091A78`; encoded length: 7 byte(s).

**Effect:** stores the red, green, and blue background clear components and advances seven bytes.

**XGS form:** `dialogue.set_extended_background_clear_color(<background_clear_red>, <background_clear_green>, <background_clear_blue>);`

**Inputs:**
- `background_clear_red` (byte +1): primary handler 0x80091A78 operand: background clear red; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `background_clear_green` (byte +3): primary handler 0x80091A78 operand: background clear green; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `background_clear_blue` (byte +5): primary handler 0x80091A78 operand: background clear blue; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `E8` — `MoveActorAndSetStateFlag`

Handler: `0x80094158`; encoded length: 7 byte(s).

**Effect:** moves an actor for a scripted step count and sets its state flag.

**XGS form:** `movement.move_actor_and_set_state_flag(<movement_step>, <duration>, <turn_angle_or_axis_mode>);`

**Inputs:**
- `movement_step` (byte +1): primary handler 0x80094158 operand: movement step; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +3): primary handler 0x80094158 operand: duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `turn_angle_or_axis_mode` (byte +5): primary handler 0x80094158 operand: turn angle or axis mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `E9` — `MoveActorAndClearStateFlag`

Handler: `0x800943AC`; encoded length: 7 byte(s).

**Effect:** applies complementary movement and clears its state flag.

**XGS form:** `movement.move_actor_and_clear_state_flag(<movement_step>, <duration>, <turn_angle_or_axis_mode>);`

**Inputs:**
- `movement_step` (byte +1): primary handler 0x800943AC operand: movement step; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +3): primary handler 0x800943AC operand: duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `turn_angle_or_axis_mode` (byte +5): primary handler 0x800943AC operand: turn angle or axis mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `EA` — `WalkPlayerToAlignedExit`

Handler: `0x80092DFC`; encoded length: 6 byte(s).

**Effect:** waits for transition systems, enables scripted player control, and walks the player toward the current actor's aligned exit point while committing any pending field transition.

**XGS form:** `event.walk_player_to_aligned_exit(<destination_field>, <entry_point>) -> (<map_entry_point>);`

**Lossless form:** `event.walk_player_to_aligned_exit(<destination_field>, <entry_point>, reserved=<reserved>) -> (<map_entry_point>);`

- Additional format field at byte +1: `u8 (0..255)`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination_field` (byte +2): primary handler 0x80092DFC operand: destination field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `entry_point` (byte +4): primary handler 0x80092DFC operand: entry point; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `map_entry_point` (fixed VM slot 0x0002): fixed VM output: map entry point; declared VM slot (implicit).

**Handler evidence:** 80092DFC delegates to 80092894: the words at +2/+4 are evaluated; byte +1 has no verified semantic read.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `EB` — `ComputeOrbitPointFromCoordinates`

Handler: `0x800910C0`; encoded length: 20 byte(s).

**Effect:** builds a camera-scale-adjusted orbit point around explicit coordinates from yaw, pitch, and magnitude inputs, writes its X, Z, and Y coordinates, and advances twenty bytes.

**XGS form:** `movement.compute_orbit_point_from_coordinates(<origin_x>, <origin_z>, <origin_y>, <yaw>, <pitch>, <radius>) -> (<orbit_position_x>, <orbit_position_z>, <orbit_position_y>);`

**Lossless form:** `movement.compute_orbit_point_from_coordinates(<origin_x>, <origin_z>, <origin_y>, <yaw>, <pitch>, <radius>, reserved_flags=<reserved_flags>) -> (<orbit_position_x>, <orbit_position_z>, <orbit_position_y>);`

- Additional format field at byte +13: `flag subset 0x03`; encoded from the named argument; concise default `0`.

**Inputs:**
- `origin_x` (byte +1): primary handler 0x800910C0 operand: origin x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x80 selects immediate.
- `origin_z` (byte +3): primary handler 0x800910C0 operand: origin z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x40 selects immediate.
- `origin_y` (byte +5): primary handler 0x800910C0 operand: origin y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x20 selects immediate.
- `yaw` (byte +7): primary handler 0x800910C0 operand: yaw; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x10 selects immediate.
- `pitch` (byte +9): primary handler 0x800910C0 operand: pitch; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x08 selects immediate.
- `radius` (byte +11): primary handler 0x800910C0 operand: radius; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +13 bit 0x04 selects immediate.

**VM writes:**
- `orbit_position_x` (byte +14): VM output: orbit position x; declared VM slot.
- `orbit_position_z` (byte +16): VM output: orbit position z; declared VM slot.
- `orbit_position_y` (byte +18): VM output: orbit position y; declared VM slot.

**Encoding:** The primary handler's separate control byte(s) at +13 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `EC` — `ComputeCameraOrbitPoint`

Handler: `0x80091318`; encoded length: 15 byte(s).

**Effect:** computes a rotated camera endpoint.

**XGS form:** `camera.compute_camera_orbit_point(<camera_orbit_selector>, <yaw>, <pitch>, <radius>) -> (<camera_orbit_position_x>, <camera_orbit_position_z>, <camera_orbit_position_y>);`

**Lossless form:** `camera.compute_camera_orbit_point(<camera_orbit_selector>, <yaw>, <pitch>, <radius>, reserved_flags=<reserved_flags>) -> (<camera_orbit_position_x>, <camera_orbit_position_z>, <camera_orbit_position_y>);`

- Additional format field at byte +8: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `camera_orbit_selector` (byte +1): primary handler 0x80091318 operand: camera orbit selector; encoded byte (0..255); may be a subfield of a larger value.
- `yaw` (byte +2): primary handler 0x80091318 operand: yaw; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x80 selects immediate.
- `pitch` (byte +4): primary handler 0x80091318 operand: pitch; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x40 selects immediate.
- `radius` (byte +6): primary handler 0x80091318 operand: radius; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x20 selects immediate.

**VM writes:**
- `camera_orbit_position_x` (byte +9): VM output: camera orbit position x; declared VM slot.
- `camera_orbit_position_z` (byte +11): VM output: camera orbit position z; declared VM slot.
- `camera_orbit_position_y` (byte +13): VM output: camera orbit position y; declared VM slot.

**Encoding:** The primary handler's separate control byte(s) at +8 select variable vs immediate for the listed words; unused bits are explicit reserved_flags in the lossless form; every field is supplied by the source.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `ED` — `WriteCameraMovementParameter`

Handler: `0x800915C4`; encoded length: 8 byte(s).

**Effect:** writes a camera parameter.

**XGS form:** `camera.write_camera_movement_parameter(<camera_vector_selector>) -> (<camera_parameter_x>, <camera_parameter_z>, <camera_parameter_y>);`

**Inputs:**
- `camera_vector_selector` (byte +1): primary handler 0x800915C4 operand: camera vector selector; encoded byte (0..255); may be a subfield of a larger value.

**VM writes:**
- `camera_parameter_x` (byte +2): VM output: camera parameter x; declared VM slot.
- `camera_parameter_z` (byte +4): VM output: camera parameter z; declared VM slot.
- `camera_parameter_y` (byte +6): VM output: camera parameter y; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `EE` — `SetCameraMovementParameter`

Handler: `0x80091720`; encoded length: 3 byte(s).

**Effect:** sets a camera parameter.

**XGS form:** `camera.set_camera_movement_parameter(<source_camera_vector_selector>, <destination_camera_vector_selector>);`

**Inputs:**
- `source_camera_vector_selector` (byte +1): primary handler 0x80091720 operand: source camera vector selector; encoded byte (0..255); may be a subfield of a larger value.
- `destination_camera_vector_selector` (byte +2): primary handler 0x80091720 operand: destination camera vector selector; encoded byte (0..255); may be a subfield of a larger value.

### `EF` — `WaitForCameraMovement`

Handler: `0x8008FA38`; encoded length: 3 byte(s).

**Effect:** waits for camera movement.

**XGS form:** `flow.wait_for_camera_movement(<camera_movement_mask>);`

**Inputs:**
- `camera_movement_mask` (byte +1): primary handler 0x8008FA38 operand: camera movement mask; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `F0` — `WriteCameraProjectionParameters`

Handler: `0x80090DEC`; encoded length: 7 byte(s).

**Effect:** writes captured camera yaw, projection dip, and scaled projection depth to three variables and advances seven bytes.

**XGS form:** `camera.write_camera_projection_parameters() -> (<camera_yaw>, <camera_projection_dip>, <camera_projection_depth>);`

**Inputs:**
- None.

**VM writes:**
- `camera_yaw` (byte +1): VM output: camera yaw; declared VM slot.
- `camera_projection_dip` (byte +3): VM output: camera projection dip; declared VM slot.
- `camera_projection_depth` (byte +5): VM output: camera projection depth; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `F1` — `SetupRgbCalculationMode1`

Handler: `0x8008B248`; encoded length: 11 byte(s).

**Effect:** configures RGB calculation mode 1 from five script parameters.

**XGS form:** `event.setup_rgb_calculation_mode1(<semi_transparency_abr_mode>, <target_red>, <target_green>, <target_blue>, <duration>);`

**Inputs:**
- `semi_transparency_abr_mode` (byte +1): primary handler 0x8008B248 operand: semi transparency abr mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target_red` (byte +3): primary handler 0x8008B248 operand: target red; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target_green` (byte +5): primary handler 0x8008B248 operand: target green; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target_blue` (byte +7): primary handler 0x8008B248 operand: target blue; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +9): primary handler 0x8008B248 operand: duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `F2` — `SetupCameraShake`

Handler: `0x8008F90C`; encoded length: 9 byte(s).

**Effect:** configures target X, Z, and Y shake offsets and per-frame deltas for a nonzero duration, with an extended return-to-zero phase when every target is zero.

**XGS form:** `camera.setup_camera_shake(<shake_x>, <shake_z>, <shake_y>, <duration>);`

**Inputs:**
- `shake_x` (byte +1): primary handler 0x8008F90C operand: shake x; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `shake_z` (byte +3): primary handler 0x8008F90C operand: shake z; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `shake_y` (byte +5): primary handler 0x8008F90C operand: shake y; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +7): primary handler 0x8008F90C operand: duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `F3` — `WriteCameraOrbitParameters`

Handler: `0x80090E70`; encoded length: 7 byte(s).

**Effect:** writes yaw, pitch, and half-distance.

**XGS form:** `camera.write_camera_orbit_parameters() -> (<camera_orbit_yaw>, <camera_orbit_pitch>, <camera_orbit_half_distance>);`

**Inputs:**
- None.

**VM writes:**
- `camera_orbit_yaw` (byte +1): VM output: camera orbit yaw; declared VM slot.
- `camera_orbit_pitch` (byte +3): VM output: camera orbit pitch; declared VM slot.
- `camera_orbit_half_distance` (byte +5): VM output: camera orbit half distance; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `F4` — `CloseDialogueOrResetWindowConfig`

Handler: `0x8009BE9C`; encoded length: 2 byte(s).

**Effect:** control value zero requests closure of the actor's owned dialogue, while a nonzero value clears configured width, height, forced position, and window flags.

**XGS form:** `dialogue.close_dialogue_or_reset_window_config(<mode>);`

**Inputs:**
- `mode` (byte +1): primary handler 0x8009BE9C operand: mode; encoded byte (0..255); may be a subfield of a larger value.

### `F5` — `OpenCenteredDialogueMode3`

Handler: `0x8009C12C`; encoded length: 4 byte(s).

**Effect:** opens the selected dialogue block in centered mode 3 using the trailing control byte.

**XGS form:** `dialogue.open_centered_dialogue_mode3(<dialogue_id>, <dialogue_control>);`

**Inputs:**
- `dialogue_id` (byte +1): primary handler 0x8009C12C operand: dialogue id; unsigned 16-bit integer (0..65535).
- `dialogue_control` (byte +3): primary handler 0x8009C12C operand: dialogue control; encoded byte (0..255); may be a subfield of a larger value.

### `F6` — `ConfigureActorRotationLockMode`

Handler: `0x8008E8C8`; encoded length: 2 byte(s).

**Effect:** clears rotation locking and residual motion for mode zero, locks and snapshots rotation for mode one, enables deferred motion cleanup for mode two, and advances two bytes.

**XGS form:** `movement.configure_actor_rotation_lock_mode(<mode>);`

**Inputs:**
- `mode` (byte +1): primary handler 0x8008E8C8 operand: mode; encoded byte (0..255); may be a subfield of a larger value.

### `F7` — `ConfigureRandomEncounterTimers`

Handler: `0x8008E85C`; encoded length: 5 byte(s).

**Effect:** evaluates the encounter timer range and active-timer count, caps the count at 32, and regenerates that many unique randomized countdowns.

**XGS form:** `world.configure_random_encounter_timers(<encounter_timer_range>, <active_timer_count>);`

**Inputs:**
- `encounter_timer_range` (byte +1): primary handler 0x8008E85C operand: encounter timer range; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `active_timer_count` (byte +3): primary handler 0x8008E85C operand: active timer count; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `F8` — `UpdateCurrentActorFlagQuarter`

Handler: `0x8008E59C`; encoded length: 4 byte(s).

**Effect:** sets or clears the selected lower or upper 16-bit quarter of either current actor flag group and advances four bytes.

**XGS form:** `actor.update_current_actor_flag_quarter(<flag_quarter_and_action>, <flag_mask>);`

**Inputs:**
- `flag_quarter_and_action` (byte +1): primary handler 0x8008E59C operand: flag quarter and action; encoded byte (0..255); may be a subfield of a larger value.
- `flag_mask` (byte +2): primary handler 0x8008E59C operand: flag mask; unsigned 16-bit integer (0..65535).

### `F9` — `SetParentActor`

Handler: `0x8008DE64`; encoded length: 2 byte(s).

**Effect:** assigns an actor parent.

**XGS form:** `actor.set_parent_actor(<parent_actor>);`

**Inputs:**
- `parent_actor` (byte +1): primary handler 0x8008DE64 operand: parent actor; actor selector (including self/party selectors).

### `FA` — `AdjustActorAxisRotation`

Handler: `0x800947B0`; encoded length: 5 byte(s).

**Effect:** adjusts a selected actor rotation axis.

**XGS form:** `movement.adjust_actor_axis_rotation(<actor>, <rotation_axis>, <rotation_delta>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x800947B0 operand: actor; actor selector (including self/party selectors).
- `rotation_axis` (byte +2): primary handler 0x800947B0 operand: rotation axis; encoded byte (0..255); may be a subfield of a larger value.
- `rotation_delta` (byte +3): primary handler 0x800947B0 operand: rotation delta; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FB` — `JumpIfIndexedBitClear`

Handler: `0x8008D780`; encoded length: 5 byte(s).

**Effect:** conditionally branches on an indexed bit.

**XGS form:** `flow.jump_if_indexed_bit_clear(<indexed_flags>, <bit_index>, <target>);`

**Inputs:**
- `indexed_flags` (byte +1): primary handler 0x8008D780 operand: indexed flags; declared VM slot.
- `bit_index` (byte +1): primary handler 0x8008D780 operand: bit index; bit index (0..15) in the low nibble.
- `target` (byte +3): primary handler 0x8008D780 operand: target; symbolic bytecode target, relocated during linking.

### `FC` — `OpenDialogueAtActorWithCopiedPortrait`

Handler: `0x8009BF8C`; encoded length: 5 byte(s).

**Effect:** resolves the selected actor, copies its portrait ID into the current actor, and opens the current actor's mode-zero dialogue anchored to the selected actor; an invalid selector skips six bytes and deferred creation retries.

**XGS form:** `dialogue.open_dialogue_at_actor_with_copied_portrait(<actor>, <dialogue_id>, <dialogue_control>);`

**Inputs:**
- `actor` (byte +1): primary handler 0x8009BF8C operand: actor; actor selector (including self/party selectors).
- `dialogue_id` (byte +2): primary handler 0x8009BF8C operand: dialogue id; unsigned 16-bit integer (0..65535).
- `dialogue_control` (byte +4): primary handler 0x8009BF8C operand: dialogue control; encoded byte (0..255); may be a subfield of a larger value.

### `FD` — `Nop`

Handler: `0x800A2FC0`; encoded length: 1 byte(s).

**Effect:** Field script no-op handler.

**XGS form:** `nop;`

**Lossless form:** `flow.nop_fd();`


**Inputs:**
- None.

### `FE` — `ExtendedDispatch`

Handler: `0x800869B8`; encoded length: prefix byte(s).

**Effect:** executes the secondary Field script VM.

**Form:** secondary dispatcher prefix; see the FE entries.

**Inputs:** The following subopcode byte selects its handler.

**Result:** Dispatch the selected FE handler.

### `FF` — `Nop`

Handler: `0x800A2FC0`; encoded length: 1 byte(s).

**Effect:** Field script no-op handler.

**XGS form:** `nop;`

**Lossless form:** `flow.nop_ff();`


**Inputs:**
- None.

## Extended instructions

FE coverage: 180 table-driven typed/output entries, 35 two-byte entries with no encoded payload, 8 prefix-only fallbacks, and 4 specially encoded/mode-dependent entries (`FE 5C`, `FE 77`, `FE D2`, `FE D7`).
Runtime-dependent or out-of-form reads also occur at `FE 18` and `FE 6C`; see their individual notes.

### `FE 00` — `ExtendedOpcodeZeroNoOp`

Handler: `0x8008D2D8`; encoded length: fallback byte(s).

**Effect:** returns immediately without changing runtime state.

**XGS form:** `flow.extended_prefix();`

**Inputs:** The following opcode byte selects this no-op FE slot;
the linker retains a valid prefix or uses the equivalent one-byte NOP.

**Result:** Consume FE only, then dispatch the following byte as a primary opcode.

### `FE 01` — `RandomTurn`

Handler: `0x8009F424`; encoded length: 2 byte(s).

**Effect:** periodically selects a random turn.

**XGS form:** `state.random_turn();`

**Inputs:**
- None.

### `FE 02` — `FieldActorInnerProximityPredicate`

Handler: `0x80095B3C`; encoded length: 5 byte(s).

**Effect:** checks the near-screen actor range.

**XGS form:** `actor.field_actor_inner_proximity_predicate(<actor>, <target>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `target` (byte +3): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 03` — `SetCurrentActorUniformScale`

Handler: `0x8008D0F4`; encoded length: 4 byte(s).

**Effect:** sets uniform actor scale, applies a three-quarter sprite scale, refreshes rotation, and advances three bytes.

**XGS form:** `actor.set_current_actor_uniform_scale(<actor_uniform_scale>);`

**Inputs:**
- `actor_uniform_scale` (byte +2): actor uniform scale; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 04` — `SetCurrentActorSpriteGeometryScale`

Handler: `0x8008D26C`; encoded length: 4 byte(s).

**Effect:** doubles the supplied value into the current actor's sprite geometry scale and advances three bytes.

**XGS form:** `actor.set_current_actor_sprite_geometry_scale(<actor_sprite_geometry_scale>);`

**Inputs:**
- `actor_sprite_geometry_scale` (byte +2): actor sprite geometry scale; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 05` — `CheckActorWalkmeshId`

Handler: `0x80095CC4`; encoded length: 7 byte(s).

**Effect:** advances six bytes when the selected actor uses the requested walkmesh, otherwise branches to the encoded destination.

**XGS form:** `movement.check_actor_walkmesh_id(<actor>, <walkmesh_id>, <target>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `walkmesh_id` (byte +3): walkmesh id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target` (byte +5): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 06` — `CheckActorWalkmeshMaterial`

Handler: `0x80095D6C`; encoded length: 7 byte(s).

**Effect:** advances six bytes when the selected actor's current triangle has the requested material byte, otherwise branches to the encoded destination.

**XGS form:** `movement.check_actor_walkmesh_material(<actor>, <walkmesh_material>, <target>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `walkmesh_material` (byte +3): walkmesh material; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `target` (byte +5): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 07` — `SetCurrentActorFlag400FromMode`

Handler: `0x8008D604`; encoded length: 3 byte(s).

**Effect:** clears actor flag 0x400 for mode zero, sets it for mode one, and advances two bytes.

**XGS form:** `actor.set_current_actor_flag400_from_mode(<mode>);`

**Inputs:**
- `mode` (byte +2): current actor flag 0x400 mode; encoded byte (0..255); may be a subfield of a larger value.

### `FE 08` — `SetCurrentActorAxisScales`

Handler: `0x8008D180`; encoded length: 8 byte(s).

**Effect:** sets independent actor X, Y, and Z scales, restores the sprite scale to 0xC00, refreshes rotation, and advances seven bytes.

**XGS form:** `actor.set_current_actor_axis_scales(<actor_scale_x>, <actor_scale_y>, <actor_scale_z>);`

**Inputs:**
- `actor_scale_x` (byte +2): actor scale x; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `actor_scale_y` (byte +4): actor scale y; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `actor_scale_z` (byte +6): actor scale z; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 09` — `ToggleActorMechaSuppression`

Handler: `0x8008D078`; encoded length: 4 byte(s).

**Effect:** clears actor flag 0x800 for a zero operand and sets it otherwise, controlling mecha and shadow suppression.

**XGS form:** `actor.toggle_actor_mecha_suppression(<mecha_suppression_mode>);`

**Inputs:**
- `mecha_suppression_mode` (byte +2): mecha suppression mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 0A` — `VariableSetIndexedBit`

Handler: `0x8008D684`; encoded length: 4 byte(s).

**Effect:** sets an indexed script-variable bit.

**XGS form:** `<flags> |= (1 << <bit>);`

**Lossless form:** `state.set_packed_bit(<flags>, <bit>);`


**Inputs:**
- `flags` (byte +2): packed VM slot and bit index; declared VM slot.
- `bit` (byte +2): bit index in the same packed word; bit index (0..15) in the low nibble.

### `FE 0B` — `VariableClearIndexedBit`

Handler: `0x8008D700`; encoded length: 4 byte(s).

**Effect:** clears an indexed script-variable bit.

**XGS form:** `<flags> &= ~(1 << <bit>);`

**Lossless form:** `state.clear_packed_bit(<flags>, <bit>);`


**Inputs:**
- `flags` (byte +2): packed VM slot and bit index; declared VM slot.
- `bit` (byte +2): bit index in the same packed word; bit index (0..15) in the low nibble.

### `FE 0C` — `SetSharedGeometryParameters`

Handler: `0x8008CFEC`; encoded length: 14 byte(s).

**Effect:** sets six signed shared geometry parameters from script operands.

**XGS form:** `event.set_shared_geometry_parameters(<geometry_1>, <geometry_2>, <geometry_3>, <geometry_4>, <geometry_5>, <geometry_6>);`

**Inputs:**
- `geometry_1` (byte +2): shared geometry parameter 1; signed 16-bit integer (-32768..32767).
- `geometry_2` (byte +4): shared geometry parameter 2; signed 16-bit integer (-32768..32767).
- `geometry_3` (byte +6): shared geometry parameter 3; signed 16-bit integer (-32768..32767).
- `geometry_4` (byte +8): shared geometry parameter 4; signed 16-bit integer (-32768..32767).
- `geometry_5` (byte +10): shared geometry parameter 5; signed 16-bit integer (-32768..32767).
- `geometry_6` (byte +12): shared geometry parameter 6; signed 16-bit integer (-32768..32767).

### `FE 0D` — `SetDialogPortraitCharacter`

Handler: `0x8008CF9C`; encoded length: 4 byte(s).

**Effect:** resolves a character selector and stores it as the current actor's dialog portrait identifier.

**XGS form:** `dialogue.set_portrait(<character>);`

**Inputs:**
- `character` (byte +2): portrait character selector; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 0E` — `FadeMusicVolume`

Handler: `0x8008C84C`; encoded length: 6 byte(s).

**Effect:** transitions music volume to the requested target over the requested duration and retries when music startup is incomplete.

**XGS form:** `audio.fade_music_volume(<music_volume_target>, <music_fade_duration>);`

**Inputs:**
- `music_volume_target` (byte +2): music volume target; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `music_fade_duration` (byte +4): music fade duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 0F` — `FadeMusicPitch`

Handler: `0x8008C938`; encoded length: 7 byte(s).

**Effect:** transitions music pitch to a signed target over the requested duration and retries when music startup is incomplete.

**XGS form:** `audio.fade_music_pitch(<target>, <duration>);`

**Lossless form:** `audio.fade_music_pitch(<target>, <duration>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +6: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `target` (byte +2): signed music pitch target; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x80 selects immediate.
- `duration` (byte +4): pitch fade duration; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x40 selects immediate.

### `FE 10` — `FadeMusicTempo`

Handler: `0x8008CA60`; encoded length: 6 byte(s).

**Effect:** transitions music tempo to the requested target over the requested duration and retries when music startup is incomplete.

**XGS form:** `audio.fade_music_tempo(<music_tempo_target>, <music_fade_duration>);`

**Inputs:**
- `music_tempo_target` (byte +2): music tempo target; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `music_fade_duration` (byte +4): music fade duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 11` — `FadeMusicPan`

Handler: `0x8008CB4C`; encoded length: 7 byte(s).

**Effect:** transitions music pan to a signed target over the requested duration and retries when music startup is incomplete.

**XGS form:** `audio.fade_music_pan(<target>, <duration>);`

**Lossless form:** `audio.fade_music_pan(<target>, <duration>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +6: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `target` (byte +2): signed music pan target; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x80 selects immediate.
- `duration` (byte +4): pan fade duration; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x40 selects immediate.

### `FE 12` — `SetMusicChannelMask`

Handler: `0x8008CC74`; encoded length: 4 byte(s).

**Effect:** applies a channel-enable mask to the active music instance and updates affected voices.

**XGS form:** `audio.set_music_channel_mask(<music_channel_mask>);`

**Inputs:**
- `music_channel_mask` (byte +2): music channel mask; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 13` — `ConfigureActorPositionalSound`

Handler: `0x8008CD48`; encoded length: 6 byte(s).

**Effect:** assigns the current actor's positional sound identifier and volume in mode 0, stops its previous channel, and marks zero identifiers inactive.

**XGS form:** `audio.configure_actor_positional_sound(<sound_id>, <volume>);`

**Inputs:**
- `sound_id` (byte +2): current actor's positional sound identifier; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `volume` (byte +4): positional sound volume (stored as a byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

The sound ID is stored as a 16-bit value; volume is truncated to
the low byte. Zero ID disables the sound. The mode (0 or 0x80) is
chosen by the opcode, not by an input argument.
`FE 13 15 80 7F 80` → `audio.configure_actor_positional_sound(21, 127);`.

### `FE 14` — `ConfigureActorPositionalSoundMode80`

Handler: `0x8008CDD4`; encoded length: 6 byte(s).

**Effect:** assigns the current actor's positional sound identifier and volume with mode 0x80, stops its previous channel, and marks zero identifiers inactive.

**XGS form:** `audio.configure_actor_positional_sound_mode80(<sound_id>, <volume>);`

**Inputs:**
- `sound_id` (byte +2): current actor's positional sound identifier; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `volume` (byte +4): positional sound volume (stored as a byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

The sound ID is stored as a 16-bit value; volume is truncated to
the low byte. Zero ID disables the sound. The mode (0 or 0x80) is
chosen by the opcode, not by an input argument.
`FE 14 15 80 7F 80` → `audio.configure_actor_positional_sound_mode80(21, 127);`.

### `FE 15` — `InitializeActorGraphicVariant`

Handler: `0x800A14F0`; encoded length: 6 byte(s).

**Effect:** initializes the current actor from a selected field graphic and variant, synchronizes placement, enables updates and visibility, and advances five bytes.

**XGS form:** `actor.initialize_actor_graphic_variant(<field_graphic>, <graphic_variant>);`

**Inputs:**
- `field_graphic` (byte +2): field graphic; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `graphic_variant` (byte +4): graphic variant; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 16` — `FreeMovementBoundingZone`

Handler: `0x8008C7D8`; encoded length: 2 byte(s).

**Effect:** releases the current actor's allocated movement-boundary vertices and clears their ownership flag.

**XGS form:** `movement.free_movement_bounding_zone();`

**Inputs:**
- None.

### `FE 17` — `FaceActorTowardActor`

Handler: `0x8009AA00`; encoded length: 4 byte(s).

**Effect:** turns the first selected actor toward the second selected actor when both are valid.

**XGS form:** `actor.face_actor_toward_actor(<actor>, <actor>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `actor` (byte +3): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).

### `FE 18` — `AddImmediatePartyCharacter`

Handler: `0x8008BDD8`; encoded length: 3 byte(s).

**Effect:** reserves a staging slot and begins loading an immediate party character, or marks an already staged character as present. The staged path resumes at FE-relative +3; the already-present path skips two subsequent code bytes and resumes at +5. Those bytes are not parameters.

**XGS:** `actor.add_immediate_party_character(character) staged goto staged already_present goto already_present;`

**Inputs:**
- `character`: byte value.
- `staged`: symbolic continuation; may move freely.
- `already_present`: symbolic continuation; may move freely.

**Result:** Terminates or branches to the named continuation; no expression value.


**Lowering:** Operands and control flow are rebuilt from this source using private native data and jumps. There is no dependency on a neighboring source instruction or original encoding. See [shared-byte lowering](SHARED_SCRIPT_BYTES.md).

### `FE 19` — `RemovePartyCharacter`

Handler: `0x8008C334`; encoded length: 3 byte(s).

**Effect:** removes a resolved party member, compacts party resources and slot metadata, and reactivates shifted actors.

**XGS form:** `movement.remove_party_character(<character_id>);`

**Inputs:**
- `character_id` (byte +2): party character byte; encoded byte (0..255); may be a subfield of a larger value.

### `FE 1A` — `FinalizePartyCharacterLoad`

Handler: `0x8008B894`; encoded length: 2 byte(s).

**Effect:** waits for staged loading, decompresses the character resource, releases compressed data, and initializes the first entry actor targeting that character.

**XGS form:** `actor.finalize_party_character_load();`

**Inputs:**
- None.

### `FE 1B` — `AdjustCurrentModelRedGreen`

Handler: `0x8008B5D4`; encoded length: 6 byte(s).

**Effect:** adds signed red and green deltas to every current-model vertex and mirrors the colors into the alternate render buffer.

**XGS form:** `visual.adjust_current_model_red_green(<red_delta>, <green_delta>);`

**Inputs:**
- `red_delta` (byte +2): signed red-vertex increment (low byte used); signed 16-bit integer (-32768..32767).
- `green_delta` (byte +4): signed green-vertex increment (low byte used); signed 16-bit integer (-32768..32767).

### `FE 1C` — `SetActorPosition3DImmediate`

Handler: `0x80098A7C`; encoded length: 9 byte(s).

**Effect:** places the current actor at scripted XYZ coordinates and synchronizes its rendered and physical positions.

**XGS form:** `movement.set_actor_position3d_immediate(<x>, <z>, <y>);`

**Lossless form:** `movement.set_actor_position3d_immediate(<x>, <z>, <y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +8: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `x` (byte +2): X coordinate; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x80 selects immediate.
- `z` (byte +4): Z coordinate; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x40 selects immediate.
- `y` (byte +6): Y/elevation coordinate; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x20 selects immediate.

The engine resolves X, Z and Y in that order, shifts each to 16.16
fixed-point and synchronizes the physical and rendered actor positions.
The control byte is encoding, never an XGS argument. Example:
`FE 1C E3 FE 84 01 E0 01 E0` →
`movement.set_actor_position3d_immediate(-285, 388, 480);`.

### `FE 1D` — `SetGlobalModelTranslationStep`

Handler: `0x800984EC`; encoded length: 9 byte(s).

**Effect:** sets the global XYZ model translation increments and enables their application.

**XGS form:** `visual.set_global_model_translation_step(<translation_x>, <translation_z>, <translation_y>);`

**Lossless form:** `visual.set_global_model_translation_step(<translation_x>, <translation_z>, <translation_y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +8: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `translation_x` (byte +2): translation x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x80 selects immediate.
- `translation_z` (byte +4): translation z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x40 selects immediate.
- `translation_y` (byte +6): translation y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x20 selects immediate.

### `FE 1E` — `SwitchMapToGears`

Handler: `0x8009FB98`; encoded length: 3 byte(s).

**Effect:** synchronizes assets and marks the Field map for Gear mode.

**XGS form:** `actor.switch_map_to_gears(<gear_mode>);`

**Inputs:**
- `gear_mode` (byte +2): requested map Gear mode; encoded byte (0..255); may be a subfield of a larger value.

### `FE 1F` — `MountCurrentPartyActor`

Handler: `0x8009FDD4`; encoded length: 2 byte(s).

**Effect:** maps the current actor to a party slot, mounts it when not already riding a gear, and advances one byte.

**XGS form:** `actor.mount_current_party_actor();`

**Inputs:**
- None.

### `FE 20` — `DismountPartyCharacter`

Handler: `0x8009FE4C`; encoded length: 3 byte(s).

**Effect:** resolves the selected character to a party slot, dismounts it when currently riding a gear, and advances two bytes.

**XGS form:** `actor.dismount_party_character(<party_slot>);`

**Inputs:**
- `party_slot` (byte +2): party slot to dismount; encoded byte (0..255); may be a subfield of a larger value.

### `FE 21` — `InitializePartyCharacterActor`

Handler: `0x800A06E8`; encoded length: 4 byte(s).

**Effect:** resolves a character into the active party, binds its party graphics and synchronized transform, or creates a disabled placeholder when absent.

**XGS form:** `actor.initialize_party_character_actor(<party_character>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 22` — `WriteProjectionDepth`

Handler: `0x8009B664`; encoded length: 4 byte(s).

**Effect:** writes the current projection depth to script memory.

**XGS form:** `camera.write_projection_depth() -> (<camera_projection_depth>);`

**Inputs:**
- None.

**VM writes:**
- `camera_projection_depth` (byte +2): VM output: camera projection depth; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 23` — `MovePartyToFormation`

Handler: `0x8009B398`; encoded length: 21 byte(s).

**Effect:** moves the three party slots toward separate scripted positions and facings until all arrive, while the sentinel mode immediately normalizes their rotations.

**XGS form:** `movement.move_party_to_formation(<party_0_position_x>, <party_0_position_z>, <party_1_position_x>, <party_1_position_z>, <party_2_position_x>, <party_2_position_z>, <party_0_facing>, <party_1_facing>, <party_2_facing>);`

**Lossless form:** `movement.move_party_to_formation(<party_0_position_x>, <party_0_position_z>, <party_1_position_x>, <party_1_position_z>, <party_2_position_x>, <party_2_position_z>, <party_0_facing>, <party_1_facing>, <party_2_facing>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +14: `flag subset 0x03`; encoded from the named argument; concise default `0`.

**Inputs:**
- `party_0_position_x` (byte +2): party slot 0 destination X; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x80 selects immediate.
- `party_0_position_z` (byte +4): party slot 0 destination Z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x40 selects immediate.
- `party_1_position_x` (byte +6): party slot 1 destination X; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x20 selects immediate.
- `party_1_position_z` (byte +8): party slot 1 destination Z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x10 selects immediate.
- `party_2_position_x` (byte +10): party slot 2 destination X; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x08 selects immediate.
- `party_2_position_z` (byte +12): party slot 2 destination Z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x04 selects immediate.
- `party_0_facing` (byte +15): first party slot facing; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `party_1_facing` (byte +17): second party slot facing; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `party_2_facing` (byte +19): third party slot facing; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 24` — `GatherPartyAtLeader`

Handler: `0x8009B210`; encoded length: 2 byte(s).

**Effect:** moves all three party slots to the leader, stalls until every valid member arrives, then resets follow history.

**XGS form:** `actor.gather_party_at_leader();`

**Inputs:**
- None.

### `FE 25` — `SetCameraFollowHeightMode`

Handler: `0x8008D5C8`; encoded length: 3 byte(s).

**Effect:** loads the camera follow-height mode from the next byte and advances two bytes.

**XGS form:** `camera.set_camera_follow_height_mode(<height_mode>);`

**Inputs:**
- `height_mode` (byte +2): camera follow-height mode; encoded byte (0..255); may be a subfield of a larger value.

### `FE 26` — `SetupScreenDistortion`

Handler: `0x8008B2F0`; encoded length: 16 byte(s).

**Effect:** initializes screen distortion mode 0 from seven script parameters.

**XGS form:** `event.setup_screen_distortion(<distortion_target_1>, <distortion_target_2>, <distortion_target_3>, <distortion_target_4>, <distortion_target_5>, <distortion_target_6>, <duration>);`

**Inputs:**
- `distortion_target_1` (byte +2): distortion target state field 1; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `distortion_target_2` (byte +4): distortion target state field 2; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `distortion_target_3` (byte +6): distortion target state field 3; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `distortion_target_4` (byte +8): distortion target state field 4; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `distortion_target_5` (byte +10): distortion target state field 5; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `distortion_target_6` (byte +12): distortion target state field 6; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +14): fade duration, zero promoted to one; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

Six evaluated targets become 16.16 distortion interpolation
targets at 800B2080..800B2094. The seventh evaluated word is
the duration; zero is promoted to one before calculating deltas.

### `FE 27` — `ControlScreenDistortion`

Handler: `0x8008B328`; encoded length: 3 or 5 byte(s).

**Effect:** starts distortion fade-out, waits for completion, stops updates, or releases distortion resources according to its subcommand.

**XGS form:** `event.control_screen_distortion(<mode>, <distortion_parameter>);`

**Mode 0 (5 bytes):** `event.control_screen_distortion(<mode>, <distortion_parameter>);`
**Mode 1 (3 bytes):** `event.control_screen_distortion(<mode>);`
**Mode 2 (3 bytes):** `event.control_screen_distortion(<mode>);`
**Mode 3 (3 bytes):** `event.control_screen_distortion(<mode>);`

**Inputs:**
- `mode` (byte +2): distortion subcommand; only mode 0 reads a duration; encoded byte (0..255); may be a subfield of a larger value.
- `distortion_parameter` (byte +3): distortion parameter; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 28` — `WriteCurrentActorFlags1`

Handler: `0x8008E4EC`; encoded length: 4 byte(s).

**Effect:** writes current actor flag word 1 to the indexed script variable and advances three bytes.

**XGS form:** `actor.write_current_actor_flags1() -> (<actor_flags_1>);`

**Inputs:**
- None.

**VM writes:**
- `actor_flags_1` (byte +2): VM output: actor flags 1; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 29` — `WriteCurrentActorFlags2`

Handler: `0x8008E518`; encoded length: 4 byte(s).

**Effect:** writes current actor flag word 2 to the indexed script variable and advances three bytes.

**XGS form:** `actor.write_current_actor_flags2() -> (<actor_flags_2>);`

**Inputs:**
- None.

**VM writes:**
- `actor_flags_2` (byte +2): VM output: actor flags 2; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 2A` — `WriteCurrentActorFlags3`

Handler: `0x8008E544`; encoded length: 4 byte(s).

**Effect:** writes current actor flag word 3 to the indexed script variable and advances three bytes.

**XGS form:** `actor.write_current_actor_flags3() -> (<actor_flags_3>);`

**Inputs:**
- None.

**VM writes:**
- `actor_flags_3` (byte +2): VM output: actor flags 3; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 2B` — `WriteCurrentActorFlags4`

Handler: `0x8008E570`; encoded length: 4 byte(s).

**Effect:** writes current actor flag word 4 to the indexed script variable and advances three bytes.

**XGS form:** `actor.write_current_actor_flags4() -> (<actor_flags_4>);`

**Inputs:**
- None.

**VM writes:**
- `actor_flags_4` (byte +2): VM output: actor flags 4; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 2C` — `WriteActorFlags1`

Handler: `0x8008DEBC`; encoded length: 4 byte(s).

**Effect:** writes actor flag group 1.

**XGS form:** `actor.write_actor_flags1() -> (<target_actor_flags_1>);`

**Inputs:**
- None.

**VM writes:**
- `target_actor_flags_1` (byte +2): VM output: target actor flags 1; declared VM slot.

The output word is also used as an actor selector:
its low byte is resolved through 8009CDB4(1).
These are overlapping views of one encoded field.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 2D` — `WriteActorFlags2`

Handler: `0x8008DF44`; encoded length: 4 byte(s).

**Effect:** writes actor flag group 2.

**XGS form:** `actor.write_actor_flags2() -> (<target_actor_flags_2>);`

**Inputs:**
- None.

**VM writes:**
- `target_actor_flags_2` (byte +2): VM output: target actor flags 2; declared VM slot.

The output word is also used as an actor selector:
its low byte is resolved through 8009CDB4(1).
These are overlapping views of one encoded field.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 2E` — `WriteActorFlags3`

Handler: `0x8008DFCC`; encoded length: 4 byte(s).

**Effect:** writes actor flag group 3.

**XGS form:** `actor.write_actor_flags3() -> (<target_actor_flags_3>);`

**Inputs:**
- None.

**VM writes:**
- `target_actor_flags_3` (byte +2): VM output: target actor flags 3; declared VM slot.

The output word is also used as an actor selector:
its low byte is resolved through 8009CDB4(1).
These are overlapping views of one encoded field.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 2F` — `WriteActorFlags4`

Handler: `0x8008E054`; encoded length: 4 byte(s).

**Effect:** writes actor flag group 4.

**XGS form:** `actor.write_actor_flags4() -> (<target_actor_flags_4>);`

**Inputs:**
- None.

**VM writes:**
- `target_actor_flags_4` (byte +2): VM output: target actor flags 4; declared VM slot.

The output word is also used as an actor selector:
its low byte is resolved through 8009CDB4(1).
These are overlapping views of one encoded field.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 30` — `JumpUnlessCurrentActorFlags1Set`

Handler: `0x8008E3E8`; encoded length: 6 byte(s).

**Effect:** branches to the encoded destination when the requested mask does not intersect current actor flag word 1, otherwise advances five bytes.

**XGS form:** `flow.jump_unless_current_actor_flags1_set(<actor_flag_mask>, <target>);`

**Inputs:**
- `actor_flag_mask` (byte +2): actor flag mask; unsigned 16-bit integer (0..65535).
- `target` (byte +4): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 31` — `JumpUnlessCurrentActorFlags2Set`

Handler: `0x8008E414`; encoded length: 6 byte(s).

**Effect:** branches to the encoded destination when the requested mask does not intersect current actor flag word 2, otherwise advances five bytes.

**XGS form:** `flow.jump_unless_current_actor_flags2_set(<actor_flag_mask>, <target>);`

**Inputs:**
- `actor_flag_mask` (byte +2): actor flag mask; unsigned 16-bit integer (0..65535).
- `target` (byte +4): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 32` — `JumpUnlessCurrentActorFlags3Set`

Handler: `0x8008E440`; encoded length: 6 byte(s).

**Effect:** branches to the encoded destination when the requested mask does not intersect current actor flag word 3, otherwise advances five bytes.

**XGS form:** `flow.jump_unless_current_actor_flags3_set(<actor_flag_mask>, <target>);`

**Inputs:**
- `actor_flag_mask` (byte +2): actor flag mask; unsigned 16-bit integer (0..65535).
- `target` (byte +4): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 33` — `JumpUnlessCurrentActorFlags4Set`

Handler: `0x8008E46C`; encoded length: 6 byte(s).

**Effect:** branches to the encoded destination when the requested mask does not intersect current actor flag word 4, otherwise advances five bytes.

**XGS form:** `flow.jump_unless_current_actor_flags4_set(<actor_flag_mask>, <target>);`

**Inputs:**
- `actor_flag_mask` (byte +2): actor flag mask; unsigned 16-bit integer (0..65535).
- `target` (byte +4): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 34` — `JumpUnlessTargetActorFlags1Set`

Handler: `0x8008E298`; encoded length: 7 byte(s).

**Effect:** branches to the encoded destination when the requested mask does not intersect target actor flag word 1, otherwise advances six bytes.

**XGS form:** `flow.jump_unless_target_actor_flags1_set(<actor_flag_mask>, <actor>, <target>);`

**Inputs:**
- `actor_flag_mask` (byte +2): actor flag mask; unsigned 16-bit integer (0..65535).
- `actor` (byte +4): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `target` (byte +5): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 35` — `JumpUnlessTargetActorFlags2Set`

Handler: `0x8008E2EC`; encoded length: 7 byte(s).

**Effect:** branches to the encoded destination when the requested mask does not intersect target actor flag word 2, otherwise advances six bytes.

**XGS form:** `flow.jump_unless_target_actor_flags2_set(<actor_flag_mask>, <actor>, <target>);`

**Inputs:**
- `actor_flag_mask` (byte +2): actor flag mask; unsigned 16-bit integer (0..65535).
- `actor` (byte +4): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `target` (byte +5): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 36` — `JumpUnlessTargetActorFlags3Set`

Handler: `0x8008E340`; encoded length: 7 byte(s).

**Effect:** branches to the encoded destination when the requested mask does not intersect target actor flag word 3, otherwise advances six bytes.

**XGS form:** `flow.jump_unless_target_actor_flags3_set(<actor_flag_mask>, <actor>, <target>);`

**Inputs:**
- `actor_flag_mask` (byte +2): actor flag mask; unsigned 16-bit integer (0..65535).
- `actor` (byte +4): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `target` (byte +5): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 37` — `JumpUnlessTargetActorFlags4Set`

Handler: `0x8008E394`; encoded length: 7 byte(s).

**Effect:** branches to the encoded destination when the requested mask does not intersect target actor flag word 4, otherwise advances six bytes.

**XGS form:** `flow.jump_unless_target_actor_flags4_set(<actor_flag_mask>, <actor>, <target>);`

**Inputs:**
- `actor_flag_mask` (byte +2): actor flag mask; unsigned 16-bit integer (0..65535).
- `actor` (byte +4): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `target` (byte +5): control-flow destination; symbolic bytecode target, relocated during linking.

### `FE 38` — `WriteActorDistance`

Handler: `0x8008E1B4`; encoded length: 6 byte(s).

**Effect:** resolves two actor selectors, computes their planar distance from fixed-point X/Z positions, writes zero if either actor is absent, and stores the result in the selected script variable.

**XGS form:** `actor.write_actor_distance([<actor>, <actor>]) -> (<actor_distance>);`

**Inputs:**
- `actor` (byte +4): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `actor` (byte +5): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).

**VM writes:**
- `actor_distance` (byte +2): VM output: actor distance; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 39` — `SetActorAnimationOffsetScale`

Handler: `0x8008D230`; encoded length: 4 byte(s).

**Effect:** sets the global multiplier used to convert animation displacement samples into planar actor offsets and advances three bytes.

**XGS form:** `actor.set_actor_animation_offset_scale(<animation_offset_scale>);`

**Inputs:**
- `animation_offset_scale` (byte +2): animation offset scale; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 3A` — `SetPartyFrameMask`

Handler: `0x8008CED0`; encoded length: 4 byte(s).

**Effect:** resolves a character selector and sets that character's bit in the party frame mask.

**XGS form:** `actor.set_party_frame_mask(<party_character>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 3B` — `ClearPartyFrameMask`

Handler: `0x8008CE64`; encoded length: 4 byte(s).

**Effect:** resolves a character selector and clears that character's bit in the party frame mask.

**XGS form:** `actor.clear_party_frame_mask(<party_character>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 3C` — `PlayMechaAnimation`

Handler: `0x8008B180`; encoded length: 6 byte(s).

**Effect:** starts the selected animation on a mecha slot and records its current animation identifier.

**XGS form:** `event.play_mecha_animation(<mecha_slot>, <animation_id>);`

**Inputs:**
- `mecha_slot` (byte +2): mecha slot; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `animation_id` (byte +4): animation id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 3D` — `SetPrimaryMechaMatrixRow`

Handler: `0x8008AEC8`; encoded length: 11 byte(s).

**Effect:** writes three signed values into a selected row of the primary mecha field matrix.

**XGS form:** `event.set_primary_mecha_matrix_row(<matrix_row>, <matrix_value_1>, <matrix_value_2>, <matrix_value_3>);`

**Lossless form:** `event.set_primary_mecha_matrix_row(<matrix_row>, <matrix_value_1>, <matrix_value_2>, <matrix_value_3>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +10: `flag subset 0x0F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `matrix_row` (byte +2): matrix row; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x80 selects immediate.
- `matrix_value_1` (byte +4): matrix value 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x40 selects immediate.
- `matrix_value_2` (byte +6): matrix value 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x20 selects immediate.
- `matrix_value_3` (byte +8): matrix value 3; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x10 selects immediate.

### `FE 3E` — `SetSecondaryMechaMatrixRow`

Handler: `0x8008AFD8`; encoded length: 11 byte(s).

**Effect:** writes three signed values into a selected row of the secondary mecha field matrix.

**XGS form:** `event.set_secondary_mecha_matrix_row(<matrix_row>, <matrix_value_1>, <matrix_value_2>, <matrix_value_3>);`

**Lossless form:** `event.set_secondary_mecha_matrix_row(<matrix_row>, <matrix_value_1>, <matrix_value_2>, <matrix_value_3>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +10: `flag subset 0x0F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `matrix_row` (byte +2): matrix row; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x80 selects immediate.
- `matrix_value_1` (byte +4): matrix value 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x40 selects immediate.
- `matrix_value_2` (byte +6): matrix value 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x20 selects immediate.
- `matrix_value_3` (byte +8): matrix value 3; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x10 selects immediate.

### `FE 3F` — `SetBackgroundClearColor`

Handler: `0x8008B0E8`; encoded length: 8 byte(s).

**Effect:** sets the background clear red, green, and blue components.

**XGS form:** `visual.set_background_clear_color(<background_red>, <background_green>, <background_blue>);`

**Inputs:**
- `background_red` (byte +2): background red; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `background_green` (byte +4): background green; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `background_blue` (byte +6): background blue; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 40` — `WriteLineScrollByte`

Handler: `0x80092148`; encoded length: 8 byte(s).

**Effect:** writes one byte at the requested offset of a line-scroll buffer when the offset is below its configured length and advances seven bytes.

**XGS form:** `event.write_line_scroll_byte(<line_scroll_buffer>, <line_scroll_offset>, <line_scroll_byte>);`

**Inputs:**
- `line_scroll_buffer` (byte +2): line scroll buffer; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `line_scroll_offset` (byte +4): line scroll offset; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `line_scroll_byte` (byte +6): line scroll byte; unsigned 16-bit integer (0..65535).

### `FE 41` — `PartyMemberRideGear`

Handler: `0x8009FC48`; encoded length: 4 byte(s).

**Effect:** mounts a party member in Gear.

**XGS form:** `actor.party_member_ride_gear(<party_character>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 42` — `PartyMemberDisembarkGear`

Handler: `0x8009FCAC`; encoded length: 4 byte(s).

**Effect:** disembarks a party member.

**XGS form:** `actor.party_member_disembark_gear(<party_character>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 43` — `DisablePartyFollow`

Handler: `0x8009B15C`; encoded length: 2 byte(s).

**Effect:** disables party members following the leader.

**XGS form:** `actor.disable_party_follow();`

**Inputs:**
- None.

### `FE 44` — `EnablePartyFollow`

Handler: `0x8009B184`; encoded length: 2 byte(s).

**Effect:** enables party following, clears follow state, and seeds the complete movement history with the leader's current state.

**XGS form:** `actor.enable_party_follow();`

**Inputs:**
- None.

### `FE 45` — `SetAnimationOverride`

Handler: `0x8009A0FC`; encoded length: 3 byte(s).

**Effect:** replaces the current actor animation override with the immediate animation identifier.

**XGS form:** `event.set_animation_override(<animation_id>);`

**Inputs:**
- `animation_id` (byte +2): current actor animation override; encoded byte (0..255); may be a subfield of a larger value.

### `FE 46` — `SetMechaRotationAuthority`

Handler: `0x8008AE5C`; encoded length: 3 byte(s).

**Effect:** selects whether the current actor follows its mecha root rotation or drives that rotation.

**XGS form:** `movement.set_mecha_rotation_authority(<rotation_mode>);`

**Inputs:**
- `rotation_mode` (byte +2): mecha rotation authority mode; encoded byte (0..255); may be a subfield of a larger value.

### `FE 47` — `SetDefaultModelTurnRate`

Handler: `0x8008B144`; encoded length: 4 byte(s).

**Effect:** sets the default angular step used when rotating field models toward their targets.

**XGS form:** `visual.set_default_model_turn_rate(<default_turn_rate>);`

**Inputs:**
- `default_turn_rate` (byte +2): default turn rate; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 48` — `SetCameraProjectionAngles`

Handler: `0x8008B518`; encoded length: 9 byte(s).

**Effect:** sets the three signed camera projection angles.

**XGS form:** `camera.set_camera_projection_angles(<projection_yaw>, <projection_dip>, <projection_depth>);`

**Lossless form:** `camera.set_camera_projection_angles(<projection_yaw>, <projection_dip>, <projection_depth>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +8: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `projection_yaw` (byte +2): projection yaw; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x80 selects immediate.
- `projection_dip` (byte +4): projection dip; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x40 selects immediate.
- `projection_depth` (byte +6): projection depth; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x20 selects immediate.

### `FE 49` — `ClearCurrentActorParent`

Handler: `0x8008DAFC`; encoded length: 2 byte(s).

**Effect:** sets the current actor's parent identifier to 0xFF and advances one byte.

**XGS form:** `actor.clear_current_actor_parent();`

**Inputs:**
- None.

### `FE 4A` — `BeginSpecialAnimationLoad`

Handler: `0x8008ACE8`; encoded length: 4 byte(s).

**Effect:** replaces the current actor's special-animation allocation and begins loading the selected animation resource.

**XGS form:** `event.begin_special_animation_load(<special_animation>);`

**Inputs:**
- `special_animation` (byte +2): special animation; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 4B` — `FinalizeSpecialAnimationLoad`

Handler: `0x8008A9AC`; encoded length: 2 byte(s).

**Effect:** waits for loading to finish, attaches the special animation to the current sprite, and retries while busy.

**XGS form:** `event.finalize_special_animation_load();`

**Inputs:**
- None.

### `FE 4C` — `SetForcedAnimationAndReleaseOverride`

Handler: `0x8008A974`; encoded length: 3 byte(s).

**Effect:** sets the complemented forced-animation identifier and clears actor flag 0x10000.

**XGS form:** `event.set_forced_animation_and_release_override(<animation_id>);`

**Inputs:**
- `animation_id` (byte +2): forced animation identifier; encoded byte (0..255); may be a subfield of a larger value.

### `FE 4D` — `SetForcedAnimationComplement`

Handler: `0x8008A93C`; encoded length: 3 byte(s).

**Effect:** stores the bitwise complement of an immediate animation identifier as the current actor's forced animation.

**XGS form:** `event.set_forced_animation_complement(<animation_id>);`

**Inputs:**
- `animation_id` (byte +2): forced animation identifier; encoded byte (0..255); may be a subfield of a larger value.

### `FE 4E` — `FreeSpecialAnimation`

Handler: `0x8008AA60`; encoded length: 2 byte(s).

**Effect:** releases the current actor's special-animation allocation and resets its resource identifier.

**XGS form:** `event.free_special_animation();`

**Inputs:**
- None.

### `FE 4F` — `EnableFieldMenu`

Handler: `0x80093BB0`; encoded length: 2 byte(s).

**Effect:** allows the player to open the normal Field menu.

**XGS form:** `inventory.enable_field_menu();`

**Inputs:**
- None.

### `FE 50` — `DisableFieldMenu`

Handler: `0x80093BD4`; encoded length: 2 byte(s).

**Effect:** prevents the player from opening the normal Field menu.

**XGS form:** `inventory.disable_field_menu();`

**Inputs:**
- None.

### `FE 51` — `EnableCompass`

Handler: `0x80093BFC`; encoded length: 2 byte(s).

**Effect:** enables the Field compass.

**XGS form:** `world.enable_compass();`

**Inputs:**
- None.

### `FE 52` — `DisableCompass`

Handler: `0x80093C20`; encoded length: 2 byte(s).

**Effect:** disables the Field compass.

**XGS form:** `world.disable_compass();`

**Inputs:**
- None.

### `FE 53` — `EnableEncountersFieldMenuAndCompass`

Handler: `0x80093AC8`; encoded length: 2 byte(s).

**Effect:** enables random encounters, the player-opened Field menu, and the Field compass.

**XGS form:** `world.enable_encounters_field_menu_and_compass();`

**Inputs:**
- None.

### `FE 54` — `DisableEncountersFieldMenuAndCompass`

Handler: `0x80093B10`; encoded length: 2 byte(s).

**Effect:** disables random encounters, the player-opened Field menu, and the Field compass.

**XGS form:** `world.disable_encounters_field_menu_and_compass();`

**Inputs:**
- None.

### `FE 55` — `OpenNormalMenu`

Handler: `0x80093740`; encoded length: 2 byte(s).

**Effect:** queues menu mode 0 with the configured menu argument, yields, increments the open-menu count, and advances one byte.

**XGS form:** `inventory.open_normal_menu();`

**Inputs:**
- None.

### `FE 56` — `OpenMenuMode1WithSelection`

Handler: `0x80093930`; encoded length: 4 byte(s).

**Effect:** copies the evaluated selection into menu/game-state fields, queues menu mode 1, yields, and advances three bytes; it does not write an encoded or fixed VM output slot.

**XGS form:** `inventory.open_menu_mode1_with_selection(<menu_selection>);`

**Inputs:**
- `menu_selection` (byte +2): menu selection; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 57` — `OpenLoadGameMenu`

Handler: `0x800937E0`; encoded length: 2 byte(s).

**Effect:** queues menu mode 2, yields, increments the open-menu count, and advances one byte.

**XGS form:** `inventory.open_load_game_menu();`

**Inputs:**
- None.

### `FE 58` — `OpenEnterNameMenu`

Handler: `0x80093824`; encoded length: 4 byte(s).

**Effect:** queues the Enter Name menu for the selected character or name record and yields the current script.

**XGS form:** `inventory.open_enter_name_menu(<name_record>);`

**Inputs:**
- `name_record` (byte +2): name record; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 59` — `OpenShopMenu`

Handler: `0x800939A0`; encoded length: 4 byte(s).

**Effect:** queues the selected shop inventory and yields the current script.

**XGS form:** `inventory.open_shop_menu(<shop_inventory>);`

**Inputs:**
- `shop_inventory` (byte +2): shop inventory; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 5A` — `OpenGearShopMenu`

Handler: `0x80093A04`; encoded length: 4 byte(s).

**Effect:** queues the selected Gear shop inventory and yields the current script.

**XGS form:** `inventory.open_gear_shop_menu(<gear_shop_inventory>);`

**Inputs:**
- `gear_shop_inventory` (byte +2): gear shop inventory; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 5B` — `SetActorModelTurnRate`

Handler: `0x8008B210`; encoded length: 4 byte(s).

**Effect:** sets the current actor's model-specific angular rotation step.

**XGS form:** `visual.set_actor_model_turn_rate(<model_turn_rate>);`

**Inputs:**
- `model_turn_rate` (byte +2): model turn rate; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 5C` — `LoadCurrentActorMecha`

Handler: `0x800A0FD8`; encoded length: 3, 4 or 5 byte(s).

**Effect:** waits for I/O, hides or frees the indexed mecha, asynchronously loads replacement files, then constructs and binds the replacement with the actor's scale and position. Mode 1 reads its resource word at FE-relative +6. Unsupported modes return at the subopcode and execute primary 5C, giving the combined escape a four-byte footprint.

**XGS:** `actor.load_current_actor_mecha(1, resource);`

**Inputs:**
- `resource`: 0..32767 immediate or VM variable.

**Result:** Native VM state effects; no expression value.

**XGS:** `actor.load_current_actor_mecha_reinterpret(party_selector);`

**Inputs:**
- `party_selector`: 0..32767 immediate or VM variable.

**Result:** Native VM state effects; no expression value.

Modes 0 (hide) and 2 (construct/finalize) use `actor.load_current_actor_mecha(mode)`. Mode 2 also accepts `discarded_value=value` to retain its evaluated-but-discarded operand. The reinterpret form requires an encoded party-selector low byte above 2.

**Lowering:** Operands and control flow are rebuilt from this source using private native data and jumps. There is no dependency on a neighboring source instruction or original encoding. See [shared-byte lowering](SHARED_SCRIPT_BYTES.md).

### `FE 5D` — `PlaySoundEffectWithParameters`

Handler: `0x8008F6AC`; encoded length: 8 byte(s).

**Effect:** plays the requested sound effect on channel 3 with resolved volume and pan values and advances seven bytes.

**XGS form:** `audio.play_sound_effect_with_parameters(<sound_id>, <pan>, <volume>);`

**Inputs:**
- `sound_id` (byte +2): sound id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `pan` (byte +4): pan; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `volume` (byte +6): volume; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 5E` — `SetCurrentActorTransparencyMode`

Handler: `0x8008F2D8`; encoded length: 4 byte(s).

**Effect:** applies the selected transparency mode to the current actor and advances three bytes.

**XGS form:** `actor.set_current_actor_transparency_mode(<transparency_mode>);`

**Inputs:**
- `transparency_mode` (byte +2): transparency mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 5F` — `SetCurrentActorDualLightingColors`

Handler: `0x8008F1C8`; encoded length: 9 byte(s).

**Effect:** conditionally assigns either or both RGB lighting triplets to the current actor and advances eight bytes.

**XGS form:** `visual.set_current_actor_dual_lighting_colors(<write_mask>, <lighting_red>, <lighting_green>, <lighting_blue>);`

**Inputs:**
- `write_mask` (byte +2): bit 0 writes the first RGB triplet; bit 1 the second; encoded byte (0..255); may be a subfield of a larger value.
- `lighting_red` (byte +3): lighting red; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `lighting_green` (byte +5): lighting green; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `lighting_blue` (byte +7): lighting blue; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

Mask bit 0 writes RGB to the first actor lighting triplet;
bit 1 writes to the second. RGB words are evaluated only when
at least one selected triplet is written and are stored as bytes.

### `FE 60` — `StartPreset2DPresentation`

Handler: `0x8008EC30`; encoded length: 10 byte(s).

**Effect:** starts a 2D presentation using resource, offset, limit, and mode arguments with mode-specific viewport defaults, or rewinds until resources are available.

**XGS form:** `event.start_preset2d_presentation(<presentation_resource>, <offset>, <limit>, <mode>);`

**Inputs:**
- `presentation_resource` (byte +2): presentation resource; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `offset` (byte +4): offset; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `limit` (byte +6): limit; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `mode` (byte +8): mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 61` — `WaitFor2DPresentationReady`

Handler: `0x8008E9F8`; encoded length: 2 byte(s).

**Effect:** rewinds while presentation readiness is clear, otherwise clears readiness and advances one byte, yielding after every check.

**XGS form:** `flow.wait_for2d_presentation_ready();`

**Inputs:**
- None.

### `FE 62` — `SetSoundChannelVolume`

Handler: `0x8008F444`; encoded length: 6 byte(s).

**Effect:** immediately sets the selected sound channel volume and advances five bytes.

**XGS form:** `audio.set_sound_channel_volume(<volume>, <sound_channel>);`

**Inputs:**
- `volume` (byte +2): volume; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `sound_channel` (byte +4): sound channel; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 63` — `SetSoundChannelPan`

Handler: `0x8008F4A0`; encoded length: 6 byte(s).

**Effect:** immediately sets the selected sound channel pan and advances five bytes.

**XGS form:** `audio.set_sound_channel_pan(<pan>, <sound_channel>);`

**Inputs:**
- `pan` (byte +2): pan; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `sound_channel` (byte +4): sound channel; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 64` — `WaitForSoundChannelMaskClear`

Handler: `0x8008F5E4`; encoded length: 4 byte(s).

**Effect:** rewinds while any selected sound channel remains active, otherwise advances three bytes, yielding after every check.

**XGS form:** `flow.wait_for_sound_channel_mask_clear(<sound_channel_mask>);`

**Inputs:**
- `sound_channel_mask` (byte +2): sound channel mask; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 65` — `PlayOrStopSoundEffectChannel`

Handler: `0x8008F4FC`; encoded length: 6 byte(s).

**Effect:** stops the selected channel when the effect identifier is zero, otherwise starts that effect with default volume and pan, then advances five bytes.

**XGS form:** `audio.play_or_stop_sound_effect_channel(<sound_id>, <sound_channel>);`

**Inputs:**
- `sound_id` (byte +2): sound id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `sound_channel` (byte +4): sound channel; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 66` — `PlaySoundEffectChannelCustomized`

Handler: `0x8008F558`; encoded length: 10 byte(s).

**Effect:** restarts the selected channel with the supplied effect identifier, volume, and pan, then advances nine bytes.

**XGS form:** `audio.play_sound_effect_channel_customized(<sound_id>, <pan>, <volume>, <sound_channel>);`

**Inputs:**
- `sound_id` (byte +2): sound id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `pan` (byte +4): pan; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `volume` (byte +6): volume; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `sound_channel` (byte +8): sound channel; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 67` — `StartExplicit2DPresentation`

Handler: `0x8008EE14`; encoded length: 20 byte(s).

**Effect:** starts a 2D presentation with explicit resource, offsets, mode, dimensions, and scale values, then advances 19 bytes.

**XGS form:** `event.start_explicit2d_presentation(<resource>, <configuration_1>, <configuration_2>, <configuration_3>, <mode>, <viewport_1>, <viewport_2>, <dimension>, <scale>);`

**Inputs:**
- `resource` (byte +2): presentation resource; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `configuration_1` (byte +4): first presentation configuration field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `configuration_2` (byte +6): second presentation configuration field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `configuration_3` (byte +8): third presentation configuration field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `mode` (byte +10): low nibble and bit 0x40 control presentation mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `viewport_1` (byte +12): first viewport/offset field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `viewport_2` (byte +14): second viewport/offset field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `dimension` (byte +16): presentation dimension field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `scale` (byte +18): presentation scale field; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

The viewport/offset words at +12/+14 each feed two state
fields; +10 contributes both its low nibble and bit 0x40 to
presentation mode. The nine words are not interchangeable.

### `FE 68` — `WalkPlayerToPositionAndWait`

Handler: `0x80092C20`; encoded length: 7 byte(s).

**Effect:** waits for transition systems to become ready, walks the player toward resolved X and Z coordinates, preserves the player's preexisting control flag, and completes after reaching or failing to reach the destination.

**XGS form:** `movement.walk_player_to_position_and_wait(<destination_x>, <destination_z>);`

**Lossless form:** `movement.walk_player_to_position_and_wait(<destination_x>, <destination_z>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +6: `flag subset 0x3F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `destination_x` (byte +2): destination x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x80 selects immediate.
- `destination_z` (byte +4): destination z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +6 bit 0x40 selects immediate.

### `FE 69` — `GetPartyProgressTotal`

Handler: `0x8008A6E0`; encoded length: 6 byte(s).

**Effect:** writes a selected character's base-plus-remainder progression to a script variable, or zero when no character resolves.

**XGS form:** `actor.get_party_progress_total(<character>) -> (<party_progress_total>);`

**Inputs:**
- `character` (byte +4): character selector for progression lookup; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `party_progress_total` (byte +2): VM output: party progress total; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 6A` — `SetLinkOrderingTableIndex`

Handler: `0x8008A604`; encoded length: 4 byte(s).

**Effect:** sets the ordering-table link index from an immediate or variable operand.

**XGS form:** `event.set_link_ordering_table_index(<ordering_table_index>);`

**Inputs:**
- `ordering_table_index` (byte +2): ordering table index; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 6B` — `SetPartyProgressRemainder`

Handler: `0x8008A640`; encoded length: 6 byte(s).

**Effect:** sets a selected character's progression remainder to the nonnegative difference between a requested total and its base progression.

**XGS form:** `actor.set_party_progress_remainder(<progress_total>, <party_character>);`

**Inputs:**
- `progress_total` (byte +2): progress total; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `party_character` (byte +4): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 6C` — `ClearControllerEnableFlag`

Handler: `0x8008A5A0`; encoded length: 2 byte(s).

**Effect:** tests the byte immediately after this two-byte instruction and clears controller enable when it is zero; advances one byte after the FE prefix, leaving the tested byte for subsequent dispatch.

**XGS:** `event.clear_controller_enable_flag_and_stop();`

**Inputs:**
- None.

**Result:** Terminates or branches to the named continuation; no expression value.

**XGS:** `event.keep_controller_enable_flag();`

**Inputs:**
- None.

**Result:** Native VM state effects; no expression value.


**Lowering:** Operands and control flow are rebuilt from this source using private native data and jumps. There is no dependency on a neighboring source instruction or original encoding. See [shared-byte lowering](SHARED_SCRIPT_BYTES.md).

### `FE 6D` — `SnapshotCameraProjectionBaseline`

Handler: `0x8008FB28`; encoded length: 2 byte(s).

**Effect:** snapshots the current scaled projection depth, projection dip, and camera yaw as script baselines, resets camera scale to 0x1000, and advances one byte.

**XGS form:** `camera.snapshot_camera_projection_baseline();`

**Inputs:**
- None.

### `FE 6E` — `SetSceneAngleY`

Handler: `0x8008FABC`; encoded length: 5 byte(s).

**Effect:** assigns both scene Y-angle fields.

**XGS form:** `world.set_scene_angle_y(<scene_y_angle>);`

**Lossless form:** `world.set_scene_angle_y(<scene_y_angle>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +4: `flag subset 0x7F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `scene_y_angle` (byte +2): scene y angle; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +4 bit 0x80 selects immediate.

### `FE 6F` — `SetGlobalModelRotation`

Handler: `0x8008B45C`; encoded length: 9 byte(s).

**Effect:** sets the three signed global model-rotation angles used to rebuild the rendering matrix.

**XGS form:** `visual.set_global_model_rotation(<model_rotation_x>, <model_rotation_z>, <model_rotation_y>);`

**Lossless form:** `visual.set_global_model_rotation(<model_rotation_x>, <model_rotation_z>, <model_rotation_y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +8: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `model_rotation_x` (byte +2): model rotation x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x80 selects immediate.
- `model_rotation_z` (byte +4): model rotation z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x40 selects immediate.
- `model_rotation_y` (byte +6): model rotation y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x20 selects immediate.

### `FE 70` — `SetBackgroundModelRenderMode`

Handler: `0x80089F54`; encoded length: 4 byte(s).

**Effect:** stores the script byte minus 0x80 as the mode controlling camera-offset application and forced rendering for background models, then advances three bytes.

**XGS form:** `visual.set_background_model_render_mode(<background_render_mode>);`

**Inputs:**
- `background_render_mode` (byte +2): background render mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 71` — `WriteCurrentActorRotationAngle`

Handler: `0x8009899C`; encoded length: 4 byte(s).

**Effect:** writes the current actor rotation modulo one revolution to script memory.

**XGS form:** `movement.write_current_actor_rotation_angle() -> (<current_actor_rotation_angle>);`

**Inputs:**
- None.

**VM writes:**
- `current_actor_rotation_angle` (byte +2): VM output: current actor rotation angle; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 72` — `WriteInterpolatedAngle`

Handler: `0x800988B8`; encoded length: 11 byte(s).

**Effect:** interpolates between two scripted angles by a scripted step and writes the resulting angle to script memory.

**XGS form:** `event.write_interpolated_angle(<from_angle>, <to_angle>, <interpolation_step>) -> (<interpolated_angle>);`

**Lossless form:** `event.write_interpolated_angle(<from_angle>, <to_angle>, <interpolation_step>, reserved_flags=<reserved_flags>) -> (<interpolated_angle>);`

- Additional format field at byte +10: `flag subset 0x8F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `from_angle` (byte +4): from angle; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x40 selects immediate.
- `to_angle` (byte +6): to angle; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x20 selects immediate.
- `interpolation_step` (byte +8): interpolation step; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x10 selects immediate.

**VM writes:**
- `interpolated_angle` (byte +2): VM output: interpolated angle; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 73` — `WriteDistanceBetween2DPoints`

Handler: `0x8009861C`; encoded length: 13 byte(s).

**Effect:** computes the planar distance between two scripted points and writes the result to script memory.

**XGS form:** `event.write_distance_between2d_points(<point_1_x>, <point_1_z>, <point_2_x>, <point_2_z>) -> (<distance_between_2d_points>);`

**Lossless form:** `event.write_distance_between2d_points(<point_1_x>, <point_1_z>, <point_2_x>, <point_2_z>, reserved_flags=<reserved_flags>) -> (<distance_between_2d_points>);`

- Additional format field at byte +12: `flag subset 0x87`; encoded from the named argument; concise default `0`.

**Inputs:**
- `point_1_x` (byte +4): point 1 x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x40 selects immediate.
- `point_1_z` (byte +6): point 1 z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x20 selects immediate.
- `point_2_x` (byte +8): point 2 x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x10 selects immediate.
- `point_2_z` (byte +10): point 2 z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x08 selects immediate.

**VM writes:**
- `distance_between_2d_points` (byte +2): VM output: distance between 2d points; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 74` — `DebugPrintVariableHexAndDecimal`

Handler: `0x800985BC`; encoded length: 4 byte(s).

**Effect:** evaluates the encoded operand and prints it in hexadecimal and decimal when Field debug output is enabled.

**XGS form:** `state.debug_print_variable_hex_and_decimal(<source>);`

**Inputs:**
- `source` (byte +2): VM slot printed in debug builds; declared VM slot.

### `FE 75` — `WriteActorRotationAngle`

Handler: `0x800989F0`; encoded length: 5 byte(s).

**Effect:** writes a selected actor rotation modulo one revolution to script memory when that actor is valid.

**XGS form:** `movement.write_actor_rotation_angle(<actor>) -> (<actor_rotation_angle>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).

**VM writes:**
- `actor_rotation_angle` (byte +3): VM output: actor rotation angle; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 76` — `WriteDistanceBetween3DPoints`

Handler: `0x80098738`; encoded length: 17 byte(s).

**Effect:** computes the spatial distance between two scripted points and writes the result to script memory.

**XGS form:** `event.write_distance_between3d_points(<point_1_x>, <point_1_z>, <point_1_y>, <point_2_x>, <point_2_z>, <point_2_y>) -> (<distance_between_3d_points>);`

**Lossless form:** `event.write_distance_between3d_points(<point_1_x>, <point_1_z>, <point_1_y>, <point_2_x>, <point_2_z>, <point_2_y>, reserved_flags=<reserved_flags>) -> (<distance_between_3d_points>);`

- Additional format field at byte +16: `flag subset 0x87`; encoded from the named argument; concise default `0`.

**Inputs:**
- `point_1_x` (byte +4): point 1 x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +16 bit 0x40 selects immediate.
- `point_1_z` (byte +6): point 1 z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +16 bit 0x20 selects immediate.
- `point_1_y` (byte +8): point 1 y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +16 bit 0x20 selects immediate.
- `point_2_x` (byte +10): point 2 x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +16 bit 0x10 selects immediate.
- `point_2_z` (byte +12): point 2 z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +16 bit 0x08 selects immediate.
- `point_2_y` (byte +14): point 2 y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +16 bit 0x08 selects immediate.

**VM writes:**
- `distance_between_3d_points` (byte +2): VM output: distance between 3d points; declared VM slot.

The third coordinate of the first point shares control bit 0x20
with its second coordinate; the last two coordinates of the second
point share bit 0x08. Each pair must use the same immediate/variable mode.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 77` — `ManageOverlayImageAsset`

Handler: `0x8008A2E8`; encoded length: 3 or 12 byte(s).

**Effect:** waits for archive I/O, then loads an indexed image into memory, uploads it with optional VRAM backup, or releases it according to the mode, and stops the current VM cycle. In short mode 0 it reads a control byte beyond the three-byte encoding; preserve the neighboring bytes.

**XGS:** `event.manage_overlay_image_asset(0, resource);`

**Inputs:**
- `resource`: signed 16-bit value or VM variable.

**Result:** Native VM state effects; no expression value.

Mode 1 uses `event.manage_overlay_image_asset(1, x, y, placement)` and accepts `reserved=word, reserved_flags=flags` for its skipped word and unused mask bits; other nonzero modes release the asset.

**Lowering:** Operands and control flow are rebuilt from this source using private native data and jumps. There is no dependency on a neighboring source instruction or original encoding. See [shared-byte lowering](SHARED_SCRIPT_BYTES.md).

### `FE 78` — `ReservedOpcode78NoOp`

Handler: `0x8008A4F0`; encoded length: fallback byte(s).

**Effect:** returns immediately without changing state as the reserved opcode 0x78 entry.

**XGS form:** `flow.extended_prefix();`

**Inputs:** The following opcode byte selects this no-op FE slot;
the linker retains a valid prefix or uses the equivalent one-byte NOP.

**Result:** Consume FE only, then dispatch the following byte as a primary opcode.

### `FE 79` — `ReservedOpcode79NoOp`

Handler: `0x8008A4E8`; encoded length: fallback byte(s).

**Effect:** returns immediately without changing state as the reserved opcode 0x79 entry.

**XGS form:** `flow.extended_prefix();`

**Inputs:** The following opcode byte selects this no-op FE slot;
the linker retains a valid prefix or uses the equivalent one-byte NOP.

**Result:** Consume FE only, then dispatch the following byte as a primary opcode.

### `FE 7A` — `ReservedOpcode7ANoOp`

Handler: `0x8008A4E0`; encoded length: fallback byte(s).

**Effect:** returns immediately without changing state as the reserved opcode 0x7A entry.

**XGS form:** `flow.extended_prefix();`

**Inputs:** The following opcode byte selects this no-op FE slot;
the linker retains a valid prefix or uses the equivalent one-byte NOP.

**Result:** Consume FE only, then dispatch the following byte as a primary opcode.

### `FE 7B` — `ReservedOpcode7BNoOp`

Handler: `0x8008A518`; encoded length: fallback byte(s).

**Effect:** returns immediately without changing state as the reserved opcode 0x7B entry.

**XGS form:** `flow.extended_prefix();`

**Inputs:** The following opcode byte selects this no-op FE slot;
the linker retains a valid prefix or uses the equivalent one-byte NOP.

**Result:** Consume FE only, then dispatch the following byte as a primary opcode.

### `FE 7C` — `ReservedOpcode7CNoOp`

Handler: `0x8008A500`; encoded length: fallback byte(s).

**Effect:** returns immediately without changing state as the reserved opcode 0x7C entry.

**XGS form:** `flow.extended_prefix();`

**Inputs:** The following opcode byte selects this no-op FE slot;
the linker retains a valid prefix or uses the equivalent one-byte NOP.

**Result:** Consume FE only, then dispatch the following byte as a primary opcode.

### `FE 7D` — `ReservedOpcode7DNoOp`

Handler: `0x8008A508`; encoded length: fallback byte(s).

**Effect:** returns immediately without changing state as the reserved opcode 0x7D entry.

**XGS form:** `flow.extended_prefix();`

**Inputs:** The following opcode byte selects this no-op FE slot;
the linker retains a valid prefix or uses the equivalent one-byte NOP.

**Result:** Consume FE only, then dispatch the following byte as a primary opcode.

### `FE 7E` — `ReservedOpcode7ENoOp`

Handler: `0x8008A510`; encoded length: fallback byte(s).

**Effect:** returns immediately without changing state as the reserved opcode 0x7E entry.

**XGS form:** `flow.extended_prefix();`

**Inputs:** The following opcode byte selects this no-op FE slot;
the linker retains a valid prefix or uses the equivalent one-byte NOP.

**Result:** Consume FE only, then dispatch the following byte as a primary opcode.

### `FE 7F` — `WaitForVideoPlayback`

Handler: `0x8008A244`; encoded length: 2 byte(s).

**Effect:** yields and repeats while video playback remains active, advances when playback finishes, and stops the current VM cycle.

**XGS form:** `flow.wait_for_video_playback();`

**Inputs:**
- None.

### `FE 80` — `SetPanoramaGeometryParameters`

Handler: `0x80089FD0`; encoded length: 16 byte(s).

**Effect:** stores eight panorama geometry and texture parameters, forces a zero third parameter to one, clears the fifth parameter, and advances fifteen bytes.

**XGS form:** `event.set_panorama_geometry_parameters(<geometry_state_0080>, <geometry_state_0082>, <geometry_state_0084>, <geometry_state_0086>, <geometry_state_008A>, <geometry_state_008C>, <geometry_state_008E>);`

**Inputs:**
- `geometry_state_0080` (byte +2): direct panorama geometry field 800B0080; unsigned 16-bit integer (0..65535).
- `geometry_state_0082` (byte +4): direct panorama geometry field 800B0082; unsigned 16-bit integer (0..65535).
- `geometry_state_0084` (byte +6): direct panorama geometry field 800B0084; unsigned 16-bit integer (0..65535).
- `geometry_state_0086` (byte +8): direct panorama geometry field 800B0086; unsigned 16-bit integer (0..65535).
- `geometry_state_008A` (byte +10): direct panorama geometry field 800B008A; unsigned 16-bit integer (0..65535).
- `geometry_state_008C` (byte +12): direct panorama geometry field 800B008C; unsigned 16-bit integer (0..65535).
- `geometry_state_008E` (byte +14): direct panorama geometry field 800B008E; unsigned 16-bit integer (0..65535).

Seven direct words fill panorama geometry fields at
800B0080/82/84/86/8A/8C/8E; the handler clears 800B0088
itself. Bit 15 in these words is data, not an immediate tag.

### `FE 81` — `SetPanoramaOrientationVector`

Handler: `0x8008A08C`; encoded length: 9 byte(s).

**Effect:** evaluates and stores the panorama orientation vector's X, Z, and Y components and advances eight bytes.

**XGS form:** `event.set_panorama_orientation_vector(<orientation_x>, <orientation_z>, <orientation_y>);`

**Lossless form:** `event.set_panorama_orientation_vector(<orientation_x>, <orientation_z>, <orientation_y>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +8: `flag subset 0x1F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `orientation_x` (byte +2): orientation x; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x80 selects immediate.
- `orientation_z` (byte +4): orientation z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x40 selects immediate.
- `orientation_y` (byte +6): orientation y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +8 bit 0x20 selects immediate.

### `FE 82` — `SetPanoramaColorParameters`

Handler: `0x8008A148`; encoded length: 26 byte(s).

**Effect:** stores three RGB triplets and three additional panorama values, enables panorama rendering, and advances twenty-five bytes.

**XGS form:** `visual.set_panorama_color_parameters(<color_1_red>, <color_1_green>, <color_1_blue>, <color_2_red>, <color_2_green>, <color_2_blue>, <color_3_red>, <color_3_green>, <color_3_blue>, <panorama_scalar_1>, <panorama_scalar_2>, <panorama_scalar_3>);`

**Inputs:**
- `color_1_red` (byte +2): panorama color 1 red (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `color_1_green` (byte +4): panorama color 1 green (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `color_1_blue` (byte +6): panorama color 1 blue (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `color_2_red` (byte +8): panorama color 2 red (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `color_2_green` (byte +10): panorama color 2 green (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `color_2_blue` (byte +12): panorama color 2 blue (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `color_3_red` (byte +14): panorama color 3 red (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `color_3_green` (byte +16): panorama color 3 green (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `color_3_blue` (byte +18): panorama color 3 blue (stored as byte); 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `panorama_scalar_1` (byte +20): panorama scalar field 1; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `panorama_scalar_2` (byte +22): panorama scalar field 2; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `panorama_scalar_3` (byte +24): panorama scalar field 3; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 83` — `SetBootModeWhenReady`

Handler: `0x80092FB4`; encoded length: 4 byte(s).

**Effect:** when boot-mode changes are permitted, disables random encounters for the transition, clears the readiness gate, stores the requested boot mode, and advances three bytes.

**XGS form:** `event.set_boot_mode_when_ready(<boot_mode>);`

**Inputs:**
- `boot_mode` (byte +2): boot mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 84` — `StartBattleWithReturnField`

Handler: `0x800933F8`; encoded length: 10 byte(s).

**Effect:** waits for coordinator readiness, stores the requested battle and optional post-battle Field destination and variable 2 value, marks the battle handoff pending, yields, and advances nine bytes.

**XGS form:** `battle.start_battle_with_return_field(<battle_id>, <return_field>, <entry_point>) -> (<map_entry_point>);`

**Lossless form:** `battle.start_battle_with_return_field(<battle_id>, <return_field>, <entry_point>, reserved=<reserved>) -> (<map_entry_point>);`

- Additional format field at byte +4: `u16 (0..65535)`; encoded from the named argument; concise default `0`.

**Inputs:**
- `battle_id` (byte +2): battle configuration identifier; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `return_field` (byte +6): post-battle field; 0x7FFF disables return setup; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `entry_point` (byte +8): value stored in fixed VM slot 2 when return is configured; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `map_entry_point` (fixed VM slot 0x0002): fixed VM output: map entry point; declared VM slot (implicit).

Bytes +4/+5 are skipped. The value at +8 is written to
fixed VM slot 2 only when a return field is installed.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 85` — `WriteVideoPlaybackFrame`

Handler: `0x8008A2A0`; encoded length: 4 byte(s).

**Effect:** writes the current video playback frame to a script variable and advances three bytes.

**XGS form:** `visual.write_video_playback_frame() -> (<video_playback_frame>);`

**Inputs:**
- None.

**VM writes:**
- `video_playback_frame` (byte +2): VM output: video playback frame; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 86` — `SetVideoTransitionMode`

Handler: `0x80089F94`; encoded length: 3 byte(s).

**Effect:** stores the raw video-transition mode byte and advances two bytes.

**XGS form:** `visual.set_video_transition_mode(<transition_mode>);`

**Inputs:**
- `transition_mode` (byte +2): video transition mode; encoded byte (0..255); may be a subfield of a larger value.

### `FE 87` — `WaitForMenuClose`

Handler: `0x800936E4`; encoded length: 2 byte(s).

**Effect:** yields while a menu remains open by rewinding one byte, otherwise advances one byte.

**XGS form:** `flow.wait_for_menu_close();`

**Inputs:**
- None.

### `FE 88` — `ConfigureProximityLightGradient`

Handler: `0x80089BF0`; encoded length: 19 byte(s).

**Effect:** stores indexed near and far RGB triplets plus the interpolation range used for distance-dependent model lighting, then advances eighteen bytes.

**XGS form:** `visual.configure_proximity_light_gradient(<light_index>, <color_1_red>, <color_1_green>, <color_1_blue>, <color_2_red>, <color_2_green>, <color_2_blue>, <interpolation>);`

**Inputs:**
- `light_index` (byte +2): light-gradient index; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x80 selects immediate.
- `color_1_red` (byte +4): light gradient color 1 red; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x40 selects immediate.
- `color_1_green` (byte +6): light gradient color 1 green; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x20 selects immediate.
- `color_1_blue` (byte +8): light gradient color 1 blue; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x10 selects immediate.
- `color_2_red` (byte +10): light gradient color 2 red; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x08 selects immediate.
- `color_2_green` (byte +12): light gradient color 2 green; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x04 selects immediate.
- `color_2_blue` (byte +14): light gradient color 2 blue; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x02 selects immediate.
- `interpolation` (byte +16): light-gradient distance/interpolation field; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x01 selects immediate.

The first masked value indexes a light gradient; the next
six form two RGB triplets. The final value controls the indexed
distance/interpolation field. All eight use control byte +18.

### `FE 89` — `SetProximityLightAnchor`

Handler: `0x80089DCC`; encoded length: 12 byte(s).

**Effect:** stores an indexed XYZ lighting anchor and its resolved actor association, using -1 when resolution fails, then advances eleven bytes.

**XGS form:** `visual.set_proximity_light_anchor(<light_index>, <light_position_1>, <light_position_2>, <light_position_3>, <actor>);`

**Lossless form:** `visual.set_proximity_light_anchor(<light_index>, <light_position_1>, <light_position_2>, <light_position_3>, <actor>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +10: `flag subset 0x0F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `light_index` (byte +2): light index; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x80 selects immediate.
- `light_position_1` (byte +4): light position 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x40 selects immediate.
- `light_position_2` (byte +6): light position 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x20 selects immediate.
- `light_position_3` (byte +8): light position 3; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x10 selects immediate.
- `actor` (byte +11): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).

### `FE 8A` — `SetSpatialAudioListenerSource`

Handler: `0x80089F18`; encoded length: 4 byte(s).

**Effect:** selects the controlled actor, camera eye, or camera target as the spatial-audio listener source and advances three bytes.

**XGS form:** `audio.set_spatial_audio_listener_source(<spatial_listener_source>);`

**Inputs:**
- `spatial_listener_source` (byte +2): spatial listener source; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 8B` — `WriteCurrentActorPartySlot`

Handler: `0x80089B54`; encoded length: 4 byte(s).

**Effect:** writes the current actor's party slot from the three active mappings or 0xFF when absent, then advances three bytes.

**XGS form:** `actor.write_current_actor_party_slot() -> (<current_actor_party_slot>);`

**Inputs:**
- None.

**VM writes:**
- `current_actor_party_slot` (byte +2): VM output: current actor party slot; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE 8C` — `RampSoundChannelVolume`

Handler: `0x8008F3D0`; encoded length: 8 byte(s).

**Effect:** ramps the selected sound channel toward a supplied volume over a supplied duration and advances seven bytes.

**XGS form:** `audio.ramp_sound_channel_volume(<volume>, <sound_channel>, <duration>);`

**Inputs:**
- `volume` (byte +2): volume; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `sound_channel` (byte +4): sound channel; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `duration` (byte +6): duration; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 8D` — `SetSoundChannelPreserveMask`

Handler: `0x8008F394`; encoded length: 4 byte(s).

**Effect:** sets the mask that exempts selected sound channels from the next field audio reset and advances three bytes.

**XGS form:** `audio.set_sound_channel_preserve_mask(<sound_preserve_mask>);`

**Inputs:**
- `sound_preserve_mask` (byte +2): sound preserve mask; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 8E` — `SetScreenBoundsPadding`

Handler: `0x8008F348`; encoded length: 6 byte(s).

**Effect:** sets horizontal and vertical padding used by actor screen-bound checks and advances five bytes.

**XGS form:** `event.set_screen_bounds_padding(<horizontal_padding>, <vertical_padding>);`

**Inputs:**
- `horizontal_padding` (byte +2): horizontal padding; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `vertical_padding` (byte +4): vertical padding; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 8F` — `ResetParticleConfigResolvedActor`

Handler: `0x80088790`; encoded length: 9 byte(s).

**Effect:** resolves an actor with zero fallback, stores three particle modifiers, resets its default particle banks, normalizes mode values 1 through 3 to 0x10 through 0x30, adds four VM cycles, and advances eight bytes.

**XGS form:** `visual.reset_particle_config_resolved_actor(<actor>, <particle_modifier_1>, <particle_modifier_2>, <particle_modifier_3>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `particle_modifier_1` (byte +3): particle modifier 1; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `particle_modifier_2` (byte +5): particle modifier 2; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `particle_modifier_3` (byte +7): particle modifier 3; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 90` — `InitializeParticleBank`

Handler: `0x80089004`; encoded length: 10 byte(s).

**Effect:** initializes a script particle bank.

**XGS form:** `visual.initialize_particle_bank(<bank>, <count>, <bank_field_1>, <bank_field_2>);`

**Inputs:**
- `bank` (byte +2): particle bank index activated by this call; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `count` (byte +4): per-bank particle instance count at state +0x02D2; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `bank_field_1` (byte +6): per-bank field at +0x02CE; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `bank_field_2` (byte +8): per-bank field at +0x02D0; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 91` — `SetParticleBankPosition`

Handler: `0x80089174`; encoded length: 15 byte(s).

**Effect:** sets particle-bank position.

**XGS form:** `visual.set_particle_bank_position(<particle_position_1>, <particle_position_2>, <particle_position_3>, <particle_position_4>, <particle_position_5>, <particle_position_6>);`

**Lossless form:** `visual.set_particle_bank_position(<particle_position_1>, <particle_position_2>, <particle_position_3>, <particle_position_4>, <particle_position_5>, <particle_position_6>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +14: `flag subset 0x03`; encoded from the named argument; concise default `0`.

**Inputs:**
- `particle_position_1` (byte +2): particle position 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x80 selects immediate.
- `particle_position_2` (byte +4): particle position 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x40 selects immediate.
- `particle_position_3` (byte +6): particle position 3; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x20 selects immediate.
- `particle_position_4` (byte +8): particle position 4; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x10 selects immediate.
- `particle_position_5` (byte +10): particle position 5; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x08 selects immediate.
- `particle_position_6` (byte +12): particle position 6; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x04 selects immediate.

Six masked words populate per-bank fields at offsets
+0x02D8/+0x02DA/+0x02DC and +0x02E0/+0x02E2/+0x02E4.
The handler alone does not establish XYZ axis names.

### `FE 92` — `SetParticleBankPhysics`

Handler: `0x80089374`; encoded length: 15 byte(s).

**Effect:** sets particle-bank physics.

**XGS form:** `visual.set_particle_bank_physics(<particle_physics_1>, <particle_physics_2>, <particle_physics_3>, <particle_physics_4>, <particle_physics_5>, <particle_physics_6>);`

**Lossless form:** `visual.set_particle_bank_physics(<particle_physics_1>, <particle_physics_2>, <particle_physics_3>, <particle_physics_4>, <particle_physics_5>, <particle_physics_6>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +14: `flag subset 0x03`; encoded from the named argument; concise default `0`.

**Inputs:**
- `particle_physics_1` (byte +2): particle physics 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x80 selects immediate.
- `particle_physics_2` (byte +4): particle physics 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x40 selects immediate.
- `particle_physics_3` (byte +6): particle physics 3; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x20 selects immediate.
- `particle_physics_4` (byte +8): particle physics 4; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x10 selects immediate.
- `particle_physics_5` (byte +10): particle physics 5; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x08 selects immediate.
- `particle_physics_6` (byte +12): particle physics 6; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x04 selects immediate.

The first masked word is stored as a 32-bit per-bank value
at +0x02D4; the other five fill +0x02E8/+0x02EA/+0x02EC
and +0x02F2/+0x02F4. These are distinct physics fields.

### `FE 93` — `SetParticleBankParameters`

Handler: `0x80089574`; encoded length: 12 byte(s).

**Effect:** sets particle-bank parameters.

**XGS form:** `visual.set_particle_bank_parameters(<bank_field_0322>, <bank_field_0324>, <bank_field_0320>, <flag_1>, <flag_2>);`

**Inputs:**
- `bank_field_0322` (byte +2): particle bank state +0x0322; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `bank_field_0324` (byte +4): particle bank state +0x0324; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `bank_field_0320` (byte +6): particle bank state +0x0320; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `flag_1` (byte +8): first value ORed into particle flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `flag_2` (byte +10): second value shifted left one and ORed into particle flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

The last two evaluated inputs are combined into flags as
value(+8) | (value(+10) << 1) | existing_flags, rather than
stored as independent output fields.

### `FE 94` — `SetParticleBankScale`

Handler: `0x800896D4`; encoded length: 11 byte(s).

**Effect:** sets particle-bank scale.

**XGS form:** `visual.set_particle_bank_scale(<particle_scale_1>, <particle_scale_2>, <particle_scale_3>, <particle_scale_4>);`

**Lossless form:** `visual.set_particle_bank_scale(<particle_scale_1>, <particle_scale_2>, <particle_scale_3>, <particle_scale_4>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +10: `flag subset 0x0F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `particle_scale_1` (byte +2): particle scale 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x80 selects immediate.
- `particle_scale_2` (byte +4): particle scale 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x40 selects immediate.
- `particle_scale_3` (byte +6): particle scale 3; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x20 selects immediate.
- `particle_scale_4` (byte +8): particle scale 4; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x10 selects immediate.

### `FE 95` — `SetParticleBankColor`

Handler: `0x80089880`; encoded length: 15 byte(s).

**Effect:** sets particle-bank color.

**XGS form:** `visual.set_particle_bank_color(<particle_color_1>, <particle_color_2>, <particle_color_3>, <particle_color_4>, <particle_color_5>, <particle_color_6>);`

**Lossless form:** `visual.set_particle_bank_color(<particle_color_1>, <particle_color_2>, <particle_color_3>, <particle_color_4>, <particle_color_5>, <particle_color_6>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +14: `flag subset 0x03`; encoded from the named argument; concise default `0`.

**Inputs:**
- `particle_color_1` (byte +2): particle color 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x80 selects immediate.
- `particle_color_2` (byte +4): particle color 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x40 selects immediate.
- `particle_color_3` (byte +6): particle color 3; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x20 selects immediate.
- `particle_color_4` (byte +8): particle color 4; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x10 selects immediate.
- `particle_color_5` (byte +10): particle color 5; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x08 selects immediate.
- `particle_color_6` (byte +12): particle color 6; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +14 bit 0x04 selects immediate.

The first three masked values form one byte-truncated
particle RGB triplet; the remaining three form a second.

### `FE 96` — `ParticlesInitialize`

Handler: `0x80089A80`; encoded length: 2 byte(s).

**Effect:** initializes script particles.

**XGS form:** `visual.particles_initialize();`

**Inputs:**
- None.

### `FE 97` — `StopParticleActor`

Handler: `0x80089AE4`; encoded length: 3 byte(s).

**Effect:** stops an actor particle effect.

**XGS form:** `visual.stop_particle_actor(<particle_id>);`

**Inputs:**
- `particle_id` (byte +2): current actor particle effect identifier; encoded byte (0..255); may be a subfield of a larger value.

### `FE 98` — `SetSpatialAudioFalloffDistance`

Handler: `0x800884CC`; encoded length: 4 byte(s).

**Effect:** stores the evaluated maximum distance used for spatial-audio attenuation and advances three bytes.

**XGS form:** `audio.set_spatial_audio_falloff_distance(<spatial_audio_falloff>);`

**Inputs:**
- `spatial_audio_falloff` (byte +2): spatial audio falloff; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 99` — `SetMenuOpenArgument`

Handler: `0x8008848C`; encoded length: 3 byte(s).

**Effect:** stores the inverse of the supplied one-bit menu argument and advances two bytes.

**XGS form:** `inventory.set_menu_open_argument(<menu_argument>);`

**Inputs:**
- `menu_argument` (byte +2): one-bit menu argument, inverted for storage; encoded byte (0..255); may be a subfield of a larger value.

### `FE 9A` — `SetTargetActorDualLightingColors`

Handler: `0x8008F0B4`; encoded length: 10 byte(s).

**Effect:** conditionally assigns either or both RGB lighting triplets to a resolved actor and advances nine bytes.

**XGS form:** `visual.set_target_actor_dual_lighting_colors(<write_mask>, <actor>, <lighting_red>, <lighting_green>, <lighting_blue>);`

**Inputs:**
- `write_mask` (byte +2): bit 0 writes first actor RGB; bit 1 writes second; encoded byte (0..255); may be a subfield of a larger value.
- `actor` (byte +3): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).
- `lighting_red` (byte +4): lighting red; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `lighting_green` (byte +6): lighting green; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `lighting_blue` (byte +8): lighting blue; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

Mask bit 0 writes RGB to the first actor lighting triplet;
bit 1 writes to the second. RGB words are evaluated only when
at least one selected triplet is written and are stored as bytes.

### `FE 9B` — `StartTransitionEffectMode1`

Handler: `0x8008EF5C`; encoded length: 4 byte(s).

**Effect:** selects transition effect mode 1 with the supplied effect parameter and advances three bytes.

**XGS form:** `visual.start_transition_effect_mode1(<transition_parameter>);`

**Inputs:**
- `transition_parameter` (byte +2): transition parameter; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 9C` — `StartTransitionEffectMode2`

Handler: `0x8008EFA0`; encoded length: 4 byte(s).

**Effect:** selects transition effect mode 2 with the supplied effect parameter and advances three bytes.

**XGS form:** `visual.start_transition_effect_mode2(<transition_parameter>);`

**Inputs:**
- `transition_parameter` (byte +2): transition parameter; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 9D` — `StartTransitionEffectMode3`

Handler: `0x8008F070`; encoded length: 4 byte(s).

**Effect:** selects transition effect mode 3 with the supplied effect parameter and advances three bytes.

**XGS form:** `visual.start_transition_effect_mode3(<transition_parameter>);`

**Inputs:**
- `transition_parameter` (byte +2): transition parameter; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 9E` — `SetDrawClipRegion`

Handler: `0x8008EFE4`; encoded length: 10 byte(s).

**Effect:** applies four supplied display clipping dimensions and advances nine bytes.

**XGS form:** `event.set_draw_clip_region(<clip_x>, <clip_y>, <clip_width>, <clip_height>);`

**Inputs:**
- `clip_x` (byte +2): clip x; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `clip_y` (byte +4): clip y; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `clip_width` (byte +6): clip width; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `clip_height` (byte +8): clip height; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE 9F` — `SetPartyFrameLock`

Handler: `0x800883D4`; encoded length: 5 byte(s).

**Effect:** resolves a character and sets or clears that character's party-frame-lock bit according to the mode byte, then advances four bytes.

**XGS form:** `actor.set_party_frame_lock(<mode>, <party_character>);`

**Inputs:**
- `mode` (byte +2): zero sets party-frame lock; nonzero clears it; encoded byte (0..255); may be a subfield of a larger value.
- `party_character` (byte +3): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE A0` — `StartSigned2DPresentation`

Handler: `0x8008EA58`; encoded length: 13 byte(s).

**Effect:** starts a signed-parameter 2D presentation with fixed viewport defaults, or rewinds and yields until presentation resources are available.

**XGS form:** `event.start_signed2d_presentation(<resource>, <configuration_1>, <configuration_2>, <configuration_3>, <path>);`

**Lossless form:** `event.start_signed2d_presentation(<resource>, <configuration_1>, <configuration_2>, <configuration_3>, <path>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +12: `flag subset 0x07`; encoded from the named argument; concise default `0`.

**Inputs:**
- `resource` (byte +2): presentation resource at 800C3A20; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x80 selects immediate.
- `configuration_1` (byte +4): presentation field 800C3A2A; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x40 selects immediate.
- `configuration_2` (byte +6): presentation field 800C3A2C; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x20 selects immediate.
- `configuration_3` (byte +8): presentation field 800C3A2E; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x10 selects immediate.
- `path` (byte +10): presentation path field 800C3A38; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x08 selects immediate.

### `FE A1` — `SetCharacterGear`

Handler: `0x80088360`; encoded length: 6 byte(s).

**Effect:** assigns a character Gear.

**XGS form:** `actor.set_character_gear(<party_character>, <gear_id>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `gear_id` (byte +4): gear id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE A2` — `WaitForMusicLoad`

Handler: `0x8008825C`; encoded length: 2 byte(s).

**Effect:** yields and repeats while music loading is pending, advances when loading finishes, and stops the current VM cycle.

**XGS form:** `flow.wait_for_music_load();`

**Inputs:**
- None.

### `FE A3` — `ToggleVramRegionBackup`

Handler: `0x800881E8`; encoded length: 3 byte(s).

**Effect:** captures the fixed VRAM region when the mode is zero or restores and releases it otherwise, then advances two bytes.

**XGS form:** `event.toggle_vram_region_backup(<vram_mode>);`

**Inputs:**
- `vram_mode` (byte +2): zero captures VRAM; nonzero restores it; encoded byte (0..255); may be a subfield of a larger value.

### `FE A4` — `RestoreAllGearFuelAndEther`

Handler: `0x80088198`; encoded length: 2 byte(s).

**Effect:** restores fuel and ether to their maxima for all twenty Gears and advances one byte.

**XGS form:** `actor.restore_all_gear_fuel_and_ether();`

**Inputs:**
- None.

### `FE A5` — `SetParticleRotationAngle`

Handler: `0x80088C1C`; encoded length: 8 byte(s).

**Effect:** stores the active particle slot value, merges evaluated flags into its high byte, stores its rotation angle, adds four VM cycles, and advances seven bytes.

**XGS form:** `visual.set_particle_rotation_angle(<particle_slot>, <particle_flags>, <particle_angle>);`

**Inputs:**
- `particle_slot` (byte +2): particle slot; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `particle_flags` (byte +4): particle flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `particle_angle` (byte +6): particle angle; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE A6` — `ConfigureSingleKeySpriteMotion`

Handler: `0x800888A4`; encoded length: 6 byte(s).

**Effect:** initializes current-actor sprite motion mode 2, stores one nibble selector and one packed 9-bit key value, marks the motion active, and advances five bytes.

**XGS form:** `inventory.configure_single_key_sprite_motion(<selector_and_key_high>, <key_low>);`

**Inputs:**
- `selector_and_key_high` (byte +2): low nibble selects motion key; upper bits supply key high portion; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `key_low` (byte +4): low portion of motion key; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

Each motion key uses a pair of evaluated words: the first
contains a low-nibble selector and upper key portion; the
second supplies the low key portion. Keep the pair packed.

### `FE A7` — `ConfigureDualKeySpriteMotion`

Handler: `0x800889BC`; encoded length: 10 byte(s).

**Effect:** initializes current-actor sprite motion mode 3, stores two nibble selectors and two packed 9-bit key values, marks the motion active, and advances nine bytes.

**XGS form:** `inventory.configure_dual_key_sprite_motion(<selector_1_and_key_high>, <key_1_low>, <selector_2_and_key_high>, <key_2_low>);`

**Inputs:**
- `selector_1_and_key_high` (byte +2): first selector and key high portion; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `key_1_low` (byte +4): first key low portion; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `selector_2_and_key_high` (byte +6): second selector and key high portion; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `key_2_low` (byte +8): second key low portion; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

Each motion key uses a pair of evaluated words: the first
contains a low-nibble selector and upper key portion; the
second supplies the low key portion. Keep the pair packed.

### `FE A8` — `WriteCurCameraTarget`

Handler: `0x80090A10`; encoded length: 8 byte(s).

**Effect:** writes current camera target.

**XGS form:** `camera.write_cur_camera_target() -> (<camera_target_x>, <camera_target_z>, <camera_target_y>);`

**Inputs:**
- None.

**VM writes:**
- `camera_target_x` (byte +2): VM output: camera target x; declared VM slot.
- `camera_target_z` (byte +4): VM output: camera target z; declared VM slot.
- `camera_target_y` (byte +6): VM output: camera target y; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE A9` — `WriteCurCameraPosition`

Handler: `0x80090A94`; encoded length: 8 byte(s).

**Effect:** writes current camera position.

**XGS form:** `camera.write_cur_camera_position() -> (<camera_position_x>, <camera_position_z>, <camera_position_y>);`

**Inputs:**
- None.

**VM writes:**
- `camera_position_x` (byte +2): VM output: camera position x; declared VM slot.
- `camera_position_z` (byte +4): VM output: camera position z; declared VM slot.
- `camera_position_y` (byte +6): VM output: camera position y; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE AA` — `SetCameraTrackedActor`

Handler: `0x8008DB2C`; encoded length: 3 byte(s).

**Effect:** resolves the encoded actor and stores it as the camera tracking subject, then advances two bytes.

**XGS form:** `camera.set_camera_tracked_actor(<actor>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).

### `FE AB` — `IncreasePartyGearHp`

Handler: `0x8008DC74`; encoded length: 5 byte(s).

**Effect:** VM Gear-HP increase handler.

**XGS form:** `actor.increase_party_gear_hp(<gear_hp_delta>, <party_selection>);`

**Lossless form:** `actor.increase_party_gear_hp(<gear_hp_delta>, <party_selection>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +4: `flag subset 0x7C`; encoded from the named argument; concise default `0`.

**Inputs:**
- `gear_hp_delta` (byte +2): gear hp delta; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +4 bit 0x80 selects immediate.
- `party_selection` (byte +4): 0 selects all party members; 1/2/3 select party slot 0/1/2; low two bits of a control byte (0..3).

### `FE AC` — `DecreasePartyGearHp`

Handler: `0x8008DD6C`; encoded length: 5 byte(s).

**Effect:** VM Gear-HP decrease handler.

**XGS form:** `actor.decrease_party_gear_hp(<gear_hp_delta>, <party_selection>);`

**Lossless form:** `actor.decrease_party_gear_hp(<gear_hp_delta>, <party_selection>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +4: `flag subset 0x7C`; encoded from the named argument; concise default `0`.

**Inputs:**
- `gear_hp_delta` (byte +2): gear hp delta; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +4 bit 0x80 selects immediate.
- `party_selection` (byte +4): 0 selects all party members; 1/2/3 select party slot 0/1/2; low two bits of a control byte (0..3).

### `FE AD` — `WritePartyMemberHp`

Handler: `0x80096B58`; encoded length: 5 byte(s).

**Effect:** writes party-member HP.

**XGS form:** `actor.write_party_member_hp(<character>) -> (<party_member_hp>);`

**Inputs:**
- `character` (byte +4): party-character mapping index; encoded byte (0..255); may be a subfield of a larger value.

**VM writes:**
- `party_member_hp` (byte +2): VM output: party member hp; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE AE` — `ConfigureSpecialMovementAnimation`

Handler: `0x80096AF4`; encoded length: 8 byte(s).

**Effect:** sets the special-movement enable value, animation identifier, and countdown reload, clears the active countdown, and advances seven bytes.

**XGS form:** `movement.configure_special_movement_animation(<special_movement_enable>, <animation_id>, <countdown>);`

**Inputs:**
- `special_movement_enable` (byte +2): special movement enable; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `animation_id` (byte +4): animation id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `countdown` (byte +6): countdown; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE AF` — `TransformActorJointOffset`

Handler: `0x8008800C`; encoded length: 19 byte(s).

**Effect:** resolves an actor joint transform, applies it to a script-supplied vector, writes the transformed XYZ coordinates to three script variables, and advances eighteen bytes.

**XGS form:** `actor.transform_actor_joint_offset(<joint_selector_1>, <joint_selector_2>, <vector_x>, <vector_y>, <vector_z>) -> (<transformed_joint_x>, <transformed_joint_y>, <transformed_joint_z>);`

**Lossless form:** `actor.transform_actor_joint_offset(<joint_selector_1>, <joint_selector_2>, <vector_x>, <vector_y>, <vector_z>, reserved_flags=<reserved_flags>) -> (<transformed_joint_x>, <transformed_joint_y>, <transformed_joint_z>);`

- Additional format field at byte +12: `flag subset 0x07`; encoded from the named argument; concise default `0`.

**Inputs:**
- `joint_selector_1` (byte +2): joint transform selector 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x80 selects immediate.
- `joint_selector_2` (byte +4): joint transform selector 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x40 selects immediate.
- `vector_x` (byte +6): input vector X; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x20 selects immediate.
- `vector_y` (byte +8): input vector Y; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x10 selects immediate.
- `vector_z` (byte +10): input vector Z; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +12 bit 0x08 selects immediate.

**VM writes:**
- `transformed_joint_x` (byte +13): VM output: transformed joint x; declared VM slot.
- `transformed_joint_y` (byte +15): VM output: transformed joint y; declared VM slot.
- `transformed_joint_z` (byte +17): VM output: transformed joint z; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE B0` — `LoadWdsSoundBankSlot`

Handler: `0x8008AACC`; encoded length: 3 or 7 byte(s).

**Effect:** replaces a selected WDS sound-bank slot through asynchronous loading and finalizes the bank on re-entry.

**XGS form:** `audio.load_wds_sound_bank_slot(<mode>, <sound_bank_slot>, <sound_bank_id>);`

**Mode 0 (7 bytes):** `audio.load_wds_sound_bank_slot(<mode>, <sound_bank_slot>, <sound_bank_id>);`
**Mode 1 (3 bytes):** `audio.load_wds_sound_bank_slot(<mode>);`
**Mode 2 (7 bytes):** `audio.load_wds_sound_bank_slot(<mode>, <sound_bank_slot>, <sound_bank_id>);`
**Mode 3 (7 bytes):** `audio.load_wds_sound_bank_slot(<mode>, <sound_bank_slot>, <sound_bank_id>);`

**Inputs:**
- `mode` (byte +2): sound-bank subcommand; mode 1 finalizes without operands; encoded byte (0..255); may be a subfield of a larger value.
- `sound_bank_slot` (byte +3): sound bank slot; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `sound_bank_id` (byte +5): sound bank id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE B1` — `InitializeCameraDirectionGauge`

Handler: `0x80087FD4`; encoded length: 2 byte(s).

**Effect:** loads the direction-gauge texture, allocates double-buffered packets, initializes 109 textured quads, and advances one byte.

**XGS form:** `camera.initialize_camera_direction_gauge();`

**Inputs:**
- None.

### `FE B2` — `SetPartyMemberHp`

Handler: `0x80096D28`; encoded length: 5 byte(s).

**Effect:** sets party-member HP.

**XGS form:** `actor.set_party_member_hp(<party_slot>, <party_member_hp>);`

**Inputs:**
- `party_slot` (byte +2): party mapping index for the write; byte +4 is also checked; encoded byte (0..255); may be a subfield of a larger value.
- `party_member_hp` (byte +3): party member hp; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

The handler checks byte +4 as a party-slot index while also
using it as the high byte of the evaluated HP/MP word at +3;
byte +2 supplies a second slot index for the write. These
fields overlap and must not be treated as independent arguments.

### `FE B3` — `SetPartyMemberMp`

Handler: `0x80096E20`; encoded length: 5 byte(s).

**Effect:** sets party-member MP.

**XGS form:** `actor.set_party_member_mp(<party_slot>, <party_member_mp>);`

**Inputs:**
- `party_slot` (byte +2): party mapping index for the write; byte +4 is also checked; encoded byte (0..255); may be a subfield of a larger value.
- `party_member_mp` (byte +3): party member mp; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

The handler checks byte +4 as a party-slot index while also
using it as the high byte of the evaluated HP/MP word at +3;
byte +2 supplies a second slot index for the write. These
fields overlap and must not be treated as independent arguments.

### `FE B4` — `WritePartyMemberMp`

Handler: `0x80096C40`; encoded length: 5 byte(s).

**Effect:** writes party-member MP.

**XGS form:** `actor.write_party_member_mp(<character>) -> (<party_member_mp>);`

**Inputs:**
- `character` (byte +4): party-character mapping index; encoded byte (0..255); may be a subfield of a larger value.

**VM writes:**
- `party_member_mp` (byte +2): VM output: party member mp; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE B5` — `IncrementPartyConvergenceOverride`

Handler: `0x80087FA4`; encoded length: 2 byte(s).

**Effect:** increments the party-convergence override counter and advances one byte.

**XGS form:** `actor.increment_party_convergence_override();`

**Inputs:**
- None.

### `FE B6` — `SetControlledAndTrackedActor`

Handler: `0x80087E98`; encoded length: 3 byte(s).

**Effect:** resolves the requested actor, assigns it as both the physically controlled and camera-tracked actor, records whether control differs from the normal party leader, clears control flags from every actor, assigns control to the selected actor, and advances two bytes.

**XGS form:** `actor.set_controlled_and_tracked_actor(<actor>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).

### `FE B7` — `SetInteractionAvailabilityOverride`

Handler: `0x80087E5C`; encoded length: 4 byte(s).

**Effect:** stores an evaluated interaction-availability override and advances three bytes.

**XGS form:** `event.set_interaction_availability_override(<interaction_override>);`

**Inputs:**
- `interaction_override` (byte +2): interaction override; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE B8` — `SetTransitionMusicId`

Handler: `0x80087DE0`; encoded length: 5 byte(s).

**Effect:** writes an evaluated music ID to the field-transition slot when the selector is zero or the battle-transition slot otherwise, then advances four bytes.

**XGS form:** `audio.set_transition_music_id(<destination>, <transition_music_id>);`

**Inputs:**
- `destination` (byte +2): zero selects field music; nonzero selects battle music; encoded byte (0..255); may be a subfield of a larger value.
- `transition_music_id` (byte +3): transition music id; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE B9` — `WriteCompleteWorldMapPosition`

Handler: `0x80087B5C`; encoded length: 10 byte(s).

**Effect:** writes all four saved world-map position components to script variables and advances nine bytes.

**XGS form:** `movement.write_complete_world_map_position() -> (<world_map_position_x>, <world_map_position_y>, <world_map_position_z>, <world_map_position_direction>);`

**Inputs:**
- None.

**VM writes:**
- `world_map_position_x` (byte +2): VM output: world map position x; declared VM slot.
- `world_map_position_y` (byte +4): VM output: world map position y; declared VM slot.
- `world_map_position_z` (byte +6): VM output: world map position z; declared VM slot.
- `world_map_position_direction` (byte +8): VM output: world map position direction; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE BA` — `SetWorldMapPosition`

Handler: `0x80087C34`; encoded length: 11 byte(s).

**Effect:** evaluates and stores all four saved world-map position components and advances ten bytes.

**XGS form:** `movement.set_world_map_position(<world_position_1>, <world_position_2>, <world_position_3>, <world_position_4>);`

**Lossless form:** `movement.set_world_map_position(<world_position_1>, <world_position_2>, <world_position_3>, <world_position_4>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +10: `flag subset 0x0F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `world_position_1` (byte +2): world position 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x80 selects immediate.
- `world_position_2` (byte +4): world position 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x40 selects immediate.
- `world_position_3` (byte +6): world position 3; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x20 selects immediate.
- `world_position_4` (byte +8): world position 4; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +10 bit 0x10 selects immediate.

### `FE BB` — `WriteWorldMapVehicleState`

Handler: `0x80087D30`; encoded length: 4 byte(s).

**Effect:** writes the saved world-map vehicle state to a script variable and advances three bytes.

**XGS form:** `world.write_world_map_vehicle_state() -> (<world_map_vehicle_state>);`

**Inputs:**
- None.

**VM writes:**
- `world_map_vehicle_state` (byte +2): VM output: world map vehicle state; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE BC` — `SetWorldMapVehicleState`

Handler: `0x80087D80`; encoded length: 5 byte(s).

**Effect:** evaluates and stores the saved world-map vehicle state and advances four bytes.

**XGS form:** `world.set_world_map_vehicle_state(<world_vehicle_state>);`

**Lossless form:** `world.set_world_map_vehicle_state(<world_vehicle_state>, reserved_flags=<reserved_flags>);`

- Additional format field at byte +4: `flag subset 0x7F`; encoded from the named argument; concise default `0`.

**Inputs:**
- `world_vehicle_state` (byte +2): world vehicle state; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +4 bit 0x80 selects immediate.

### `FE BD` — `SetParticleAttachmentMode`

Handler: `0x80088B68`; encoded length: 8 byte(s).

**Effect:** sets particle flag 0x80 for mode 1 or 0x40 for mode 2, adds four VM cycles, and advances seven bytes.

**XGS form:** `visual.set_particle_attachment_mode(<particle_attachment>);`

**Lossless form:** `visual.set_particle_attachment_mode(<particle_attachment>, reserved_1=<reserved_1>, reserved_2=<reserved_2>);`

- Additional format field at byte +4: `u16 (0..65535)`; encoded from the named argument; concise default `0`.
- Additional format field at byte +6: `u16 (0..65535)`; encoded from the named argument; concise default `0`.

**Inputs:**
- `particle_attachment` (byte +2): particle attachment; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

Only the first evaluated word (+2/+3) is read; bytes +4..+7
are skipped payload, not separate attachment arguments.

**Authoring/defaults:** `visual.set_particle_attachment_mode(1);` is valid and
is equivalent to `visual.set_particle_attachment_mode(1, reserved_1=0, reserved_2=0);`.
Both emit `FE BD 01 80 00 00 00 00`. The reserved fields have no handler effect.
For an original containing `FE BD 01 80 00 80 00 80`, the decompiler emits
`visual.set_particle_attachment_mode(1, reserved_1=32768, reserved_2=32768);`.
Each reserved value is an unsigned 16-bit word, written little-endian.
All of these forms compile from code alone; comments are ignored.

### `FE BE` — `EnableVideoFrameTileAnimation`

Handler: `0x80087C0C`; encoded length: 2 byte(s).

**Effect:** enables animated video-frame tile cycling and advances one byte.

**XGS form:** `visual.enable_video_frame_tile_animation();`

**Inputs:**
- None.

### `FE BF` — `SetupBattling`

Handler: `0x80087848`; encoded length: 14 byte(s).

**Effect:** waits for field resources and music readiness, then stores six battle parameters and requests battle setup.

**XGS form:** `event.setup_battling(<match_mode>, <setup_subtype>, <selection_1>, <selection_2>, <handoff_id>, <handoff_value>);`

**Inputs:**
- `match_mode` (byte +2): battle handoff/match mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `setup_subtype` (byte +4): battle setup subtype; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `selection_1` (byte +6): first battle selection; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `selection_2` (byte +8): second battle selection; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `handoff_id` (byte +10): battle handoff identifier; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `handoff_value` (byte +12): battle handoff value; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

All six evaluated words are truncated to bytes in the
battle handoff record. The battle consumer branches on match
mode/subtype and consumes both selections and handoff fields.

### `FE C0` — `WriteBattlingMatchResultCode`

Handler: `0x80087800`; encoded length: 4 byte(s).

**Effect:** copies the Battling mode match-result code to a script variable.

**XGS form:** `event.write_battling_match_result_code() -> (<battling_match_result>);`

**Inputs:**
- None.

**VM writes:**
- `battling_match_result` (byte +2): VM output: battling match result; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE C1` — `QueryPartySpriteAnimationStatus`

Handler: `0x80088508`; encoded length: 8 byte(s).

**Effect:** resolves a party actor, writes its sprite-animation status and actor index to script variables, clears nonterminal status values, adds four VM cycles, and advances seven bytes.

**XGS form:** `actor.query_party_sprite_animation_status(<party_slot>) -> (<party_sprite_animation_status>, <party_actor_index>);`

**Inputs:**
- `party_slot` (byte +6): party-to-actor mapping selector; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `party_sprite_animation_status` (byte +2): VM output: party sprite animation status; declared VM slot.
- `party_actor_index` (byte +4): VM output: party actor index; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE C2` — `ResetParticleConfigImmediateActor`

Handler: `0x80088674`; encoded length: 10 byte(s).

**Effect:** resolves an evaluated actor selector with zero fallback, stores three evaluated particle modifiers, resets its default particle banks, normalizes mode values 1 through 3 to 0x10 through 0x30, adds four VM cycles, and advances nine bytes after the FE prefix.

**XGS form:** `visual.reset_particle_config_immediate_actor(<particle_actor>, <particle_modifier_1>, <particle_modifier_2>, <particle_modifier_3>);`

**Inputs:**
- `particle_actor` (byte +2): particle actor; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `particle_modifier_1` (byte +4): particle modifier 1; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `particle_modifier_2` (byte +6): particle modifier 2; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `particle_modifier_3` (byte +8): particle modifier 3; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

The handler at 80088674 reads four evaluated words and advances
nine bytes after the FE prefix: full instruction length is 10 bytes.

### `FE C3` — `SetCurrentActorFlags02000800`

Handler: `0x8009E014`; encoded length: 2 byte(s).

**Effect:** sets flags 0x02000000 and 0x800 on the current actor.

**XGS form:** `actor.set_current_actor_flags02000800();`

**Inputs:**
- None.

### `FE C4` — `SetActorFlags02000800ById`

Handler: `0x8009DF78`; encoded length: 3 byte(s).

**Effect:** sets actor flags 0x02000000 and 0x800 by ID.

**XGS form:** `actor.set_actor_flags02000800_by_id(<actor>);`

**Inputs:**
- `actor` (byte +2): resolved actor selector (including self/party aliases); actor selector (including self/party selectors).

### `FE C5` — `SetModelAnimation`

Handler: `0x80086F7C`; encoded length: 6 byte(s).

**Effect:** assigns a model animation.

**XGS form:** `visual.set_model_animation(<model_animation>, <animation_mode>);`

**Inputs:**
- `model_animation` (byte +2): model animation; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `animation_mode` (byte +4): animation mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE C6` — `QueueVariablePartyCharacterLoad`

Handler: `0x8008BC80`; encoded length: 4 byte(s).

**Effect:** resolves an immediate-or-variable character, reserves a staging slot, and begins loading when no duplicate or conflicting load exists.

**XGS form:** `actor.queue_variable_party_character_load(<party_character>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE C7` — `WriteActorCharacterGearId`

Handler: `0x800882B8`; encoded length: 6 byte(s).

**Effect:** resolves an actor selector, writes its character's Gear ID or 0xFF to a script variable, and advances five bytes.

**XGS form:** `actor.write_actor_character_gear_id(<character>) -> (<actor_character_gear_id>);`

**Inputs:**
- `character` (byte +2): character selector for Gear ID lookup; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `actor_character_gear_id` (byte +4): VM output: actor character gear id; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE C8` — `SetParticleDirections0To3`

Handler: `0x80088CF8`; encoded length: 19 byte(s).

**Effect:** sets particle directions 0 through 3.

**XGS form:** `visual.set_particle_directions0_to3(<direction_0_component_1>, <direction_0_component_2>, <direction_1_component_1>, <direction_1_component_2>, <direction_2_component_1>, <direction_2_component_2>, <direction_3_component_1>, <direction_3_component_2>);`

**Inputs:**
- `direction_0_component_1` (byte +2): particle direction 0 component 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x80 selects immediate.
- `direction_0_component_2` (byte +4): particle direction 0 component 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x40 selects immediate.
- `direction_1_component_1` (byte +6): particle direction 1 component 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x20 selects immediate.
- `direction_1_component_2` (byte +8): particle direction 1 component 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x10 selects immediate.
- `direction_2_component_1` (byte +10): particle direction 2 component 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x08 selects immediate.
- `direction_2_component_2` (byte +12): particle direction 2 component 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x04 selects immediate.
- `direction_3_component_1` (byte +14): particle direction 3 component 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x02 selects immediate.
- `direction_3_component_2` (byte +16): particle direction 3 component 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x01 selects immediate.

Eight masked signed words make four two-component particle
direction entries; C8 writes entries 0..3 and C9 writes 4..7.

### `FE C9` — `SetParticleDirections4To7`

Handler: `0x80088D18`; encoded length: 19 byte(s).

**Effect:** sets particle directions 4 through 7.

**XGS form:** `visual.set_particle_directions4_to7(<direction_4_component_1>, <direction_4_component_2>, <direction_5_component_1>, <direction_5_component_2>, <direction_6_component_1>, <direction_6_component_2>, <direction_7_component_1>, <direction_7_component_2>);`

**Inputs:**
- `direction_4_component_1` (byte +2): particle direction 4 component 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x80 selects immediate.
- `direction_4_component_2` (byte +4): particle direction 4 component 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x40 selects immediate.
- `direction_5_component_1` (byte +6): particle direction 5 component 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x20 selects immediate.
- `direction_5_component_2` (byte +8): particle direction 5 component 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x10 selects immediate.
- `direction_6_component_1` (byte +10): particle direction 6 component 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x08 selects immediate.
- `direction_6_component_2` (byte +12): particle direction 6 component 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x04 selects immediate.
- `direction_7_component_1` (byte +14): particle direction 7 component 1; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x02 selects immediate.
- `direction_7_component_2` (byte +16): particle direction 7 component 2; signed 16-bit immediate (-32768..32767) or declared VM variable; control byte +18 bit 0x01 selects immediate.

Eight masked signed words make four two-component particle
direction entries; C8 writes entries 0..3 and C9 writes 4..7.

### `FE CA` — `ReleaseCurrentActorMecha`

Handler: `0x800A0EE8`; encoded length: 3 byte(s).

**Effect:** clears the actor's active-mecha flag, then either hides its indexed mecha or frees it and decrements the loaded-mecha count before yielding.

**XGS form:** `actor.release_current_actor_mecha(<release_mode>);`

**Inputs:**
- `release_mode` (byte +2): mecha release mode; encoded byte (0..255); may be a subfield of a larger value.

### `FE CB` — `RequestDirectorModeExit`

Handler: `0x800A0EB0`; encoded length: 2 byte(s).

**Effect:** increments the director-mode exit signal, advances one byte, and yields the current script.

**XGS form:** `event.request_director_mode_exit();`

**Inputs:**
- None.

### `FE CC` — `WaitForDirectorModeEnd`

Handler: `0x800A0E54`; encoded length: 2 byte(s).

**Effect:** advances when director-only mode is inactive, otherwise rewinds one byte and yields so the instruction is retried.

**XGS form:** `flow.wait_for_director_mode_end();`

**Inputs:**
- None.

### `FE CD` — `WriteCurrentDiscNumber`

Handler: `0x800A0DFC`; encoded length: 4 byte(s).

**Effect:** queries the current disc number, writes it to the selected script variable, and advances three bytes.

**XGS form:** `event.write_current_disc_number() -> (<current_disc_number>);`

**Inputs:**
- None.

**VM writes:**
- `current_disc_number` (byte +2): VM output: current disc number; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE CE` — `SetMaxMechaCount`

Handler: `0x800A0DC0`; encoded length: 4 byte(s).

**Effect:** sets the maximum mecha overlay entry count from a script operand and advances three bytes.

**XGS form:** `event.set_max_mecha_count(<max_mecha_count>);`

**Inputs:**
- `max_mecha_count` (byte +2): max mecha count; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE CF` — `OpenMenuMode1WithFieldContext`

Handler: `0x80093888`; encoded length: 6 byte(s).

**Effect:** disables field controls, saves field and direction state, installs the requested field and variable 2 value, queues menu mode 1, yields, and advances five bytes.

**XGS form:** `dialogue.open_menu_mode1_with_field_context(<field_id>, <entry_point>) -> (<map_entry_point>);`

**Inputs:**
- `field_id` (byte +2): destination field identifier; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `entry_point` (byte +4): value stored in fixed VM slot 2; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `map_entry_point` (fixed VM slot 0x0002): fixed VM output: map entry point; declared VM slot (implicit).

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE D0` — `CloneGearAndAbilityState`

Handler: `0x8008764C`; encoded length: 6 byte(s).

**Effect:** copies one Gear's persistent state and associated ability blocks to another slot and marks special destination slots present.

**XGS form:** `actor.clone_gear_and_ability_state(<source_gear>, <destination_gear>);`

**Inputs:**
- `source_gear` (byte +2): source gear; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `destination_gear` (byte +4): destination gear; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE D1` — `SetGameStateFlag4000`

Handler: `0x8008754C`; encoded length: 2 byte(s).

**Effect:** sets game-state flag 0x4000.

**XGS form:** `state.set_game_state_flag4000();`

**Inputs:**
- None.

### `FE D2` — `SkipThreeByteInstruction`

Handler: `0x8008752C`; encoded length: 4 byte(s).

**Effect:** advances past a three-byte no-op instruction.

**XGS form:** `flow.skip_three_byte_instruction();`

**Lossless form:** `flow.skip_three_byte_instruction(reserved=<reserved>);`

- Additional format field at byte +2: `u16 (0..65535)`; encoded from the named argument; concise default `0`.

**Inputs:**
- None.

The handler only advances the VM PC; the following payload bytes
are not read as separate semantic parameters.

### `FE D3` — `ScaleTwoRatios`

Handler: `0x80087420`; encoded length: 18 byte(s).

**Effect:** scales two script ratios and writes both results.

**XGS form:** `event.scale_two_ratios(<scale_1>, <scale_2>, <divisor_1>, <divisor_2>, <numerator_1>, <numerator_2>) -> (<scaled_ratio_1>, <scaled_ratio_2>);`

**Inputs:**
- `scale_1` (byte +2): scale of first ratio; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `scale_2` (byte +4): scale of second ratio; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `divisor_1` (byte +6): divisor of first ratio; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `divisor_2` (byte +8): divisor of second ratio; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `numerator_1` (byte +10): numerator of first ratio; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `numerator_2` (byte +12): numerator of second ratio; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

**VM writes:**
- `scaled_ratio_1` (byte +14): VM output: scaled ratio 1; declared VM slot.
- `scaled_ratio_2` (byte +16): VM output: scaled ratio 2; declared VM slot.

Each VM result computes (((numerator << 16) / divisor)
* scale) >> 16 from its corresponding three inputs; the two
output destinations are encoded at +14 and +16.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE D4` — `ManageSpriteOverlayList`

Handler: `0x80086FD0`; encoded length: 3 or 11 byte(s).

**Effect:** mode 0 allocates and initializes a 33-entry sprite-overlay list, mode 1 links an indexed entry at evaluated screen coordinates, mode 2 frees the list, and mode 3 sets an indexed entry's RGB color.

**XGS form:** `actor.manage_sprite_overlay_list(<mode>);`

**Mode 0 (3 bytes):** `actor.manage_sprite_overlay_list(<mode>);`
**Mode 1 (11 bytes):** `actor.manage_sprite_overlay_list(<mode>, <overlay_parameter_1>, <overlay_parameter_2>, <overlay_parameter_3>, <overlay_parameter_4>);`
**Mode 2 (3 bytes):** `actor.manage_sprite_overlay_list(<mode>);`
**Mode 3 (11 bytes):** `actor.manage_sprite_overlay_list(<mode>, <overlay_parameter_1>, <overlay_parameter_2>, <overlay_parameter_3>, <overlay_parameter_4>);`

**Inputs:**
- `mode` (byte +2): overlay-list subcommand; see mode-specific forms; encoded byte (0..255); may be a subfield of a larger value.

Mode 0 allocates the list and mode 2 frees it, with no more
inputs. Mode 1 uses the four evaluated words at +3/+5/+7/+9
as entry index, screen X, screen Y and placement variant.
Mode 3 uses them as entry index, red, green and blue; the colors
are byte-truncated. Other modes do not advance normally.

### `FE D5` — `WriteWorldMapPosition`

Handler: `0x80087960`; encoded length: 6 byte(s).

**Effect:** writes both persistent world-map position values to script variables.

**XGS form:** `movement.write_world_map_position() -> (<world_map_position_x>, <world_map_position_y>);`

**Inputs:**
- None.

**VM writes:**
- `world_map_position_x` (byte +2): VM output: world map position x; declared VM slot.
- `world_map_position_y` (byte +4): VM output: world map position y; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE D6` — `WriteGameState184EAnd1852`

Handler: `0x800879D0`; encoded length: 6 byte(s).

**Effect:** writes two game-state fields to script memory.

**XGS form:** `event.write_game_state184e_and1852() -> (<game_state_184e>, <game_state_1852>);`

**Inputs:**
- None.

**VM writes:**
- `game_state_184e` (byte +2): VM output: game state 184e; declared VM slot.
- `game_state_1852` (byte +4): VM output: game state 1852; declared VM slot.

**Result:** Writes the VM output slot(s) shown after `->`; no expression return.

### `FE D7` — `SetWorldMapMarkerPositionXZ`

Handler: `0x80087AB8`; encoded length: 7 byte(s).

**Effect:** writes X and Z coordinates into the saved world-map marker position, clears Y and padding, marks the position valid, and advances six bytes after FE. Its masked readers use a control byte at FE-relative +10, beyond the declared seven-byte encoding; preserve the neighboring bytes.

**XGS:** `movement.set_world_map_marker_position_xz(x, z);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.

**Result:** Native VM state effects; no expression value.

**XGS:** `movement.set_world_map_marker_position_xz(x, z, reserved=reserved);`

**Inputs:**
- `x`: signed 16-bit value or VM variable.
- `z`: signed 16-bit value or VM variable.
- `reserved`: byte value; concise default `0`.

**Result:** Native VM state effects; no expression value.


**Lowering:** Operands and control flow are rebuilt from this source using private native data and jumps. There is no dependency on a neighboring source instruction or original encoding. See [shared-byte lowering](SHARED_SCRIPT_BYTES.md).

### `FE D8` — `SetSpriteLightingBypass`

Handler: `0x80087A40`; encoded length: 3 byte(s).

**Effect:** stores a script byte that enables or bypasses dynamic sprite color lighting.

**XGS form:** `visual.set_sprite_lighting_bypass(<lighting_mode>);`

**Inputs:**
- `lighting_mode` (byte +2): sprite lighting bypass mode; encoded byte (0..255); may be a subfield of a larger value.

### `FE D9` — `SetRandomTurnDirectionTable`

Handler: `0x80087A7C`; encoded length: 3 byte(s).

**Effect:** stores a script byte selecting the direction table used for random actor turns.

**XGS form:** `movement.set_random_turn_direction_table(<direction_table>);`

**Inputs:**
- `direction_table` (byte +2): random-turn direction table index; encoded byte (0..255); may be a subfield of a larger value.

### `FE DA` — `OpenMenuMode6`

Handler: `0x80093790`; encoded length: 2 byte(s).

**Effect:** queues menu mode 6 with argument 1, yields, increments the open-menu count, and advances one byte.

**XGS form:** `inventory.open_menu_mode6();`

**Inputs:**
- None.

### `FE DB` — `RestoreCharacterHpAndMp`

Handler: `0x80097200`; encoded length: 4 byte(s).

**Effect:** restores HP and MP.

**XGS form:** `actor.restore_character_hp_and_mp(<party_character>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE DC` — `SetPartySpriteColumnOffset`

Handler: `0x800873C4`; encoded length: 6 byte(s).

**Effect:** writes an indexed byte controlling a party sprite's horizontal render column.

**XGS form:** `actor.set_party_sprite_column_offset(<party_slot>, <column_offset>);`

**Inputs:**
- `party_slot` (byte +2): party slot; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `column_offset` (byte +4): column offset; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE DD` — `VramSnapshotCommand`

Handler: `0x800871B0`; encoded length: 3 or 7 byte(s).

**Effect:** allocates and captures a 256-pixel-wide VRAM region, applies indexed brightness conversion, or releases the working buffers according to subcommand.

**XGS form:** `event.vram_snapshot_command(<mode>, <vram_parameter_1>, <vram_parameter_2>);`

**Mode 0 (7 bytes):** `event.vram_snapshot_command(<mode>, <vram_parameter_1>, <vram_parameter_2>);`
**Mode 1 (7 bytes):** `event.vram_snapshot_command(<mode>, <vram_parameter_1>, <vram_parameter_2>);`
**Mode 2 (3 bytes):** `event.vram_snapshot_command(<mode>);`
**Mode 3 (3 bytes):** `event.vram_snapshot_command(<mode>);`

**Inputs:**
- `mode` (byte +2): VRAM-snapshot subcommand; see mode-specific forms; encoded byte (0..255); may be a subfield of a larger value.
- `vram_parameter_1` (byte +3): vram parameter 1; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `vram_parameter_2` (byte +5): vram parameter 2; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

Mode 0 uses +3 as snapshot origin and +5 as allocation
height/count (shifted left 9). Mode 1 uses +3 as a 0x200-byte
buffer slice index and passes +5 to the conversion routine.
Modes 2/3 have no additional operands and yield/free resources.

### `FE DE` — `OrCharacterRecordFlags`

Handler: `0x80087148`; encoded length: 6 byte(s).

**Effect:** applies bitwise OR with a script-supplied mask to the selected character's persistent flag field.

**XGS form:** `actor.or_character_record_flags(<party_character>, <record_flags>);`

**Inputs:**
- `party_character` (byte +2): party character; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `record_flags` (byte +4): record flags; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE DF` — `ConfigureDisplayMode`

Handler: `0x80086E1C`; encoded length: 4 byte(s).

**Effect:** configures display/draw environments.

**XGS form:** `event.configure_display_mode(<display_mode>);`

**Inputs:**
- `display_mode` (byte +2): display mode; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE E0` — `SetPauseDisabled`

Handler: `0x80086DE0`; encoded length: 3 byte(s).

**Effect:** sets the Field pause-disable state.

**XGS form:** `event.set_pause_disabled(<pause_disabled>);`

**Inputs:**
- `pause_disabled` (byte +2): Field pause-disable byte; encoded byte (0..255); may be a subfield of a larger value.

### `FE E1` — `CopyGear`

Handler: `0x80087580`; encoded length: 6 byte(s).

**Effect:** copies Gear state from a Field script.

**XGS form:** `actor.copy_gear(<source_gear>, <destination_gear>);`

**Inputs:**
- `source_gear` (byte +2): source gear; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.
- `destination_gear` (byte +4): destination gear; 0..32767 immediate or declared VM variable; word bit 15 selects immediate.

### `FE E2` — `SoftReset`

Handler: `0x80086D4C`; encoded length: 2 byte(s).

**Effect:** performs a software reset, requests script suspension, and advances the instruction pointer.

**XGS form:** `event.soft_reset();`

**Inputs:**
- None.

## Regeneration

```bash
python3 tools/build_dsl_documentation.py
```
